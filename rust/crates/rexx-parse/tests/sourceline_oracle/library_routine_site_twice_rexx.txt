count 6
/* A ::ROUTINE bound to the REXX package's Filespec, called a second time from
   one expression call site, is reported under the directive's name. */
do a over .array~of('N', 'Z')
  say fs(a, '/a/b.c')
end
::requires 'pk.cls'
