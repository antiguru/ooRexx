count 5
/* Task 23 fix round 3: the same lock, a second mutator, and a class from the
   other embedded file that declares one -- `.Stream` is a `::CLASS` in
   `StreamClasses.orx`. Without the flag on the library's own classes this is
   a silent rc 0 against the oracle's rc 158. */
.Stream~define('ZZZ')
