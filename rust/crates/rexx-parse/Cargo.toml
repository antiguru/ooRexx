[package]
name = "rexx-parse"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true

[dev-dependencies]
# Only the throwaway evaluator in `src/expr/differential.rs` uses this. The
# expression grammar itself does no arithmetic.
rexx-num = { path = "../rexx-num" }

[lints]
workspace = true
