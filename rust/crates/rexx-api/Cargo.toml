[package]
name = "rexx-api"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true

[dependencies]
# `dlopen`/`dlsym` provider for `load.rs`; `rustix` does not wrap either (D-U1,
# 2026-09-14).
libloading = "0.8.9"

[lints]
workspace = true
