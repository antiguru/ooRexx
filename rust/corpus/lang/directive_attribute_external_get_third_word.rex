/* A GET or SET style resolves an explicit third word unchanged: the prefix
   is for the default procedure alone, so this file's report names the third
   word and not GETzzz_no_entry. */
say 'prolog ran'

::class k

::attribute at get external 'LIBRARY REXX zzz_no_entry'
