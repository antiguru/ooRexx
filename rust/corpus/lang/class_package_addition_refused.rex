/* Task 21: `~addClass` sent to the package the primitive classes belong to.
   A different code from the class mutators' own lock, and a different
   message, so it is its own row. */
.Array~package~addClass("ZZ", .Array)
