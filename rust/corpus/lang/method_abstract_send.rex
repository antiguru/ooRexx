/* An ABSTRACT method installs and is refused when it is SENT, which the first
   line is here to show: the program's own output reaches stdout before the
   send fails. The report names the message rather than the directive or the
   declaring class, and carries no frame of its own. Phase 5a Task 15. */

say 'installed'
say .K~m

::class K

::method m class abstract
