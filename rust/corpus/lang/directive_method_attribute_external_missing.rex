/* ::METHOD ... ATTRIBUTE EXTERNAL is the same mechanism under a different
   keyword, and it has no GET or SET style: both accessors take the prefix
   whatever the third word is. */
say 'prolog ran'

::class k

::method m attribute external 'LIBRARY REXX file_separator'
