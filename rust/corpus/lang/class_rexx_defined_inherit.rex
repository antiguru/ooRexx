/* Task 21: the REXX_DEFINED lock under INHERIT, sent with no argument at
   all -- the lock is tested before the arguments are, so this is 98.985 and
   not the 88.901 the same send to a user class gives. */
.Array~inherit()
