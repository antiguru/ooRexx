# The oracle's own builds of the extensions these programs load. The spawned
# side is given this directory anyway; the in-process side reads it from here
# because the process loader cannot be told later.
LD_LIBRARY_PATH={oraclelib}
