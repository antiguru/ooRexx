/* An attribute-shaped EXTERNAL names one procedure per accessor, each with
   the accessor's own prefix, and the getter is the one reported first. */
say 'the prolog ran'
::class k
::attribute at external "LIBRARY rxregexp NoSuchGet"
