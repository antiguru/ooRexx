/* Fix round 1, finding 3: the same forwarded frame as
 * operator_frame_stem_do_initial.rex, at the DO header's TO bound instead
 * of its initial value. Measured, rc 215, stderr opening
 * `       *-* Compiled method "+" with scope "String".`.
 */

do i = 1 to b.
end
