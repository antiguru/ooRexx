/* Whether a GET or SET style prepends is decided by comparing the procedure
   with the attribute's upcased name, over strings the C++ interns, so a
   third word spelled exactly like that name is the default and takes the
   prefix. */
say 'prolog ran'

::class k

::attribute at set external 'LIBRARY REXX AT'
