# The oracle's own build of librxregexp.so, which the program copies under
# another name into a directory it then writes into LD_LIBRARY_PATH itself.
WIDEN_FROM={oraclelib}/librxregexp.so
WIDEN_TO={run}/widened
