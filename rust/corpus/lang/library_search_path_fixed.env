# The oracle's own build of librxregexp.so is the library on the search path
# the interpreter starts with; the program copies it under another name into a
# directory it then writes into LD_LIBRARY_PATH itself.
LD_LIBRARY_PATH={oraclelib}
WIDEN_FROM={oraclelib}/librxregexp.so
WIDEN_TO={run}/widened
