# The oracle's own builds of librxregexp.so and librxmath.so. The spawned side
# is given this directory anyway; the in-process side reads it from here
# because the process loader cannot be told later.
LD_LIBRARY_PATH={oraclelib}
