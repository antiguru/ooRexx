/* A native routine's refusal of its argument is reported against the package
   its EXTERNAL directive was written in, with no line: a name that is not a
   stem's is 40.919. */
say 'before'
say st('a.b')
say 'after'

::requires 'pk.cls'
