# Phase 5a corpus subset -- the union with phase-4a.txt, phase-4b.txt and
# phase-4c.txt is what the harnesses read, not this file alone. Every entry
# in those three stays in force.
#
# One path per line, relative to this directory (rust/corpus/). Lines starting
# with # are comments; blank lines are ignored. Same format as the earlier
# phase files, and read by the same `read_subset`, which unions the files in
# first-seen order and drops a repeat.
#
# EMPTY AS OF TASK 1. Phase 5a's own scope -- `::CLASS`/`::METHOD`
# installation, Rexx method-body invocation, EXPOSE and class-scope instance
# variables, up to `CoreClasses.orx`'s prologue completing -- is not
# implemented by any task yet, so there is nothing this crate can run to
# agreement with the oracle. Task 1 only builds the harness plumbing later
# tasks need (an unnormalised stderr comparison mode, a directive walker that
# admits `::CLASS`/`::METHOD` without panicking, and this file wired into
# every harness that reads a phase subset list); it adds no corpus program of
# its own. Each later task appends its own witness here as its own construct
# starts agreeing with the oracle.
