# Phase 5a corpus subset -- the union with phase-4a.txt, phase-4b.txt and
# phase-4c.txt is what the harnesses read, not this file alone. Every entry
# in those three stays in force.
#
# One path per line, relative to this directory (rust/corpus/). Lines starting
# with # are comments; blank lines are ignored. Same format as the earlier
# phase files, and read by the same `read_subset`, which unions the files in
# first-seen order and drops a repeat.
#
# Task 1 built the harness plumbing this file needs (an unnormalised stderr
# comparison mode, a directive walker admitting `::CLASS`/`::METHOD` without
# panicking, and this file wired into every harness that reads a phase subset
# list) and added no corpus program of its own. Each later task appends its
# own witness here as its own construct starts agreeing with the oracle.

# Task 4: the directives that install. `::CLASS`, `::METHOD` and `::ATTRIBUTE`
# installing under it, and `::CONSTANT`'s two shapes -- a well-formed
# parenthesised expression (evaluated and discarded, since nothing can read
# it back before message dispatch exists) and the two ways it fails: the
# expression itself raising, and the structural "no preceding ::CLASS" check.
lang/directive_class_installs.rex
lang/directive_method_installs.rex
lang/directive_attribute_installs.rex
lang/directive_constant_expression_installs.rex
lang/directive_constant_expression_fails.rex
lang/directive_constant_expression_needs_class.rex
lang/directive_constant_expression_blames_the_last_class.rex

# Task 5: the message send, the scope override, and the dispatch seam. Sends
# to each value kind this crate can build, resolution from the receiver's own
# class and from an inherited scope, the three ways a primitive method's
# arguments can be wrong, the unknown-method condition, the scope-override
# refusal, the instruction form's own RESULT, and the message-assignment form.
lang/message_send_native.rex
lang/message_send_inherited_scope.rex
lang/message_send_unknown_method.rex
lang/message_send_unknown_method_on_nil.rex
lang/message_send_unknown_method_on_a_number.rex
lang/message_send_scope_override.rex
lang/message_send_too_many_arguments.rex
lang/message_send_missing_argument.rex
lang/message_send_argument_not_a_string.rex
lang/message_instruction.rex
lang/message_assignment_form.rex

# Task 6: the four directories, and the seam's second site. The four objects
# and the reflection names beside them, both of VALUE's one-argument routes,
# a class this file declares resolved by name from inside it, the package
# table shadowing the environment, the three names the parser resolves before
# either route can, and the package method table in both of its states.
lang/environment_symbols.rex
lang/environment_symbols_through_value.rex
lang/environment_package_class.rex
lang/environment_package_class_shadows_the_environment.rex
lang/environment_special_dot_variables_are_not_resolved.rex
lang/environment_methods_table_unattached.rex
lang/environment_methods_table_attached.rex

# Task 6 fix round 1 (ruling R12): what an operator, a condition and a builtin
# do with one of these objects as an operand -- the half the two
# implementations agree on. The half they do not is loud, which a corpus file
# cannot witness, and `eval.rs`'s `object_operand_tests` carries instead.
lang/environment_object_operands.rex

# Task 6 fix round 2: the DO header's numeric positions convert their value
# through a unary operator, so R12 reaches them too. This is the boundary
# either side of the three it refuses -- FOR, a bare repeat count, NUMERIC
# DIGITS and a builtin argument all read the value's text instead and go on
# agreeing.
lang/environment_object_in_a_loop_header.rex

# Task 6 fix round 4: RAISE substitutes its additional information, and the
# oracle array-converts it only under a SYNTAX condition. This is the side
# that does not convert, and every line of it was refused by the first
# version of that fix.
lang/environment_object_in_a_raise.rex

# Task 7: invoking a Rexx method body. A class method is the one kind a
# program can reach in this phase -- every other receiver whose behaviour
# could answer a ::METHOD is an instance, and nothing builds one until ~new
# lands -- so these witness a class object as a receiver, the activation a
# ::METHOD body runs in, `self` and `super`, RETURN with and without a value,
# EXIT, an untrapped condition inside a body, a `::ATTRIBUTE GET` whose
# accessor is written in Rexx, and the `>I>`/`<I<` pair a method activation
# adds around the callee's own clause echoes. The traced pair is compared raw
# (see corpus.rs's RAW_STDERR_COMPARISON), because the indents are the point.
lang/method_class_body.rex
lang/method_returns_no_value.rex
lang/method_no_result_is_an_error.rex
lang/method_exit_returns_to_the_sender.rex
lang/method_body_raises.rex
lang/method_class_side_lookup.rex
lang/method_attribute_body.rex
lang/method_trace_invocation.rex
lang/method_trace_nested.rex

# Task 7 fix round 1: the `::ATTRIBUTE SET` body nothing was running. Its
# resolved name carries an appended `=` that the getter's does not, and the
# >I>/<I< pair is the only place a program can read that name itself rather
# than infer it from which body ran, so this one joins corpus.rs's
# RAW_STDERR_COMPARISON.
lang/method_attribute_set_body.rex

# Task 7 fix round 1: the calling context a method activation answers to
# PARSE SOURCE, which was `COMMAND` here against the oracle's `METHOD` -- rc 0
# and empty stderr on both sides, so no refusal named an owner and nothing in
# the tree ran the program that would have shown it.
# corpus/lang/parse_sources.rex carries the contexts a method is not, and the
# pair is what stops a single fixed word passing both.
lang/method_parse_source.rex

# Task 7 fix round 2: what a primitive method does with an argument that has
# no string value. `.nil` was already refused; a class object and one of the
# interpreter's own objects were answered `0` at rc 0 where the oracle raises
# 88.909, and a stem answers as whatever its default holds. The rows that
# answer are in the same program, so a build refusing every unrecognised
# argument fails it too.
lang/message_send_argument_object_not_a_string.rex

# Task 8: EXPOSE and scope-keyed instance variables. A class object's own
# variable pool round-tripped across sends, with the uninitialised state, DROP
# and the two builtins that read a variable by name; a stem as an instance
# variable, tails and object identity included; the four ways an internal CALL
# meets an exposed name; the EXPOSE (list) form, whose selector is bound before
# its value is read; and EXPOSE where there is no method invocation to expose
# into.
lang/expose_class_variable.rex
lang/expose_stem.rex
lang/expose_internal_call.rex
lang/expose_indirect_list.rex
lang/expose_outside_a_method.rex

# Task 8: the coercion audit's own question, asked of the position EXPOSE
# adds. An instance variable is somewhere one of the interpreter's own objects
# can be put and taken out again, and the table's rule is whether this crate
# already produces the oracle's bytes there.
lang/expose_object_value.rex

# Task 8, ruling R26: `::CLASS ... SUBCLASS <class>`, which is what makes a
# second scope reach one object at all -- and is used by `CoreClasses.orx`
# itself. A parent's class method answering through the child, a target
# declared later in the same file, a chain of three, a package class winning
# over the environment entry of the same name, and the two ways a target
# fails: no name resolves to it, and the targets cannot be put in an order.
lang/class_subclass.rex
lang/class_subclass_not_found.rex
lang/class_subclass_cycle.rex

# Task 8's own acceptance criterion, which needs the three above to run: a
# subclass and its parent each exposing the same name, both sends addressed to
# the subclass's class object, holding different values at once.
lang/expose_two_scopes.rex

# Task 8 fix round 1: which ::CLASS a failing ::CONSTANT expression is blamed
# against once the file's classes no longer install in source order. The blame
# is the class installed last, which this program makes the first one in the
# file. That alone is also what reading source order backwards gives, so this
# program pins the rule only together with
# directive_constant_expression_blames_the_last_class.rex above, which reddens
# under exactly the backwards reading this one is green under. Measured, each
# positional rule built in turn: 105 of 106, one program differing each time.
lang/directive_constant_blames_the_last_installed_class.rex

# Phase 5a (2026-08-17 plan) Task 6: the native-method frame in a traceback,
# the operator half. An error raised inside a native method an *operator*
# invoked carries the same `*-* Compiled method "X" with scope "Y".` frame
# line the oracle emits, when the operator reaches that method through a
# stem's own forward to its default value rather than running directly on a
# `String`/`Number` value -- `b.~class` is `The Stem class` while the frame
# reads scope `String`, the class of the method that actually raises.
# Varies the operator across the programs below: infix `+`, infix `**` so
# the frame's own name is read rather than assumed, and prefix `-`, whose
# dispatch path is different from infix `-` and happens to collide with its
# frame text. Compared raw (see corpus.rs's RAW_STDERR_COMPARISON) so the
# comparison asserts the frame's own bytes -- leading whitespace and absent
# line number included -- rather than resting on what DEVIATION 0's
# normaliser happens to do to them; measured, od-verified, that normaliser
# is currently a no-op on every program below's stderr.
lang/operator_frame_stem_plus.rex
lang/operator_frame_stem_power.rex
lang/operator_frame_stem_prefix_minus.rex

# Fix round 1, finding 3: the same forwarded frame reached from a
# controlled `DO` header's numeric position instead of from an operator
# directly. `Interp::header_number` rounds each header position through a
# real unary `+` on the oracle, so a stem in any position carries the
# identical frame, one program per position -- the initial value, the `TO`
# bound and the `BY` increment.
lang/operator_frame_stem_do_initial.rex
lang/operator_frame_stem_do_to.rex
lang/operator_frame_stem_do_by.rex

# Fix round 2, finding 1: the frame belongs to the receiver of a forwarded
# operator whatever step of evaluating it raises, not only its own
# conversion -- `Interp::arith_general`, `Interp::apply_prefix` and
# `Interp::logical_values` (`eval.rs`) each blame once over their whole
# computation rather than only over the conversion `Interp::arith_left_operand`
# itself performs. Each program below reaches the frame from a different
# failing step past that conversion: arithmetic overflow past a valid
# conversion, the power exponent's own range check, a non-logical operand on
# `&` and on prefix `\`, and a DO header's own range check past a valid
# conversion.
lang/operator_frame_stem_divide_by_zero.rex
lang/operator_frame_stem_power_exponent_range.rex
lang/operator_frame_stem_logical_and.rex
lang/operator_frame_stem_prefix_not.rex
lang/operator_frame_stem_do_exponent_range.rex

# Fix round 3, finding 1: the goal's corrected premise (be81ce689) -- the
# operator families split by whether the operator needs a number, not by
# whether it is arithmetic, so a non-strict comparison reaches the same
# forwarded frame once both operands are numeric. `Interp::compare_values`
# joins the same blame-on-any-failure shape the other adapters have. One
# program stands for the operators that reach `compare_values`'s numeric
# path -- `>` here, and an operator is on that path when `is_comparison`
# admits it and `is_strict_compare` rejects it, both in `eval.rs`, which is
# where that set is written down in code rather than in prose. Strict
# comparison and the non-numeric fallback never reach `compare_numbers` and
# stay frameless, which this program does not exercise.
lang/operator_frame_stem_compare_overflow.rex

# Phase 5a (2026-08-17 plan) Task 7: `::CLASS ... MIXINCLASS` and
# `::CLASS ... INHERIT`.
#
# The programs that run carry the discriminators: the merge order (a class's
# own methods, then its explicit superclass, then its mixins leftmost first),
# the diamond, a forward `INHERIT`, and `~baseClass`, which is the only thing
# about an installed class that says whether it is a mixin.
lang/class_mixinclass.rex
lang/class_inherit_order.rex
# The refusal ladder, one program per condition the oracle raises. A
# condition raised by the `INHERIT` message send itself carries a native
# method's traceback frame; one raised while resolving a target, and the
# install-ordering error, do not.
lang/class_inherit_not_found.rex
lang/class_inherit_trailing_keyword.rex
lang/class_inherit_not_a_mixin.rex
lang/class_inherit_base_class.rex
lang/class_inherit_recursive.rex
lang/class_inherit_cycle.rex
# A `METACLASS` target is a dependency too, which is what
# `ClassDirective::addDependencies` asks `checkDependency` about alongside
# the other two. The directive itself is still refused here, so a cycle is
# the only shape of it that can agree with the oracle -- and it is what
# stops the dependency edge being an unwitnessed line in the diff.
lang/class_metaclass_cycle.rex

# Phase 5a (2026-08-17 plan) Task 8: `::CLASS ... METACLASS`, and the rest of
# `::CLASS`'s option surface.
#
# The program that runs carries the donation itself -- a metaclass's
# instance methods reaching the class side -- together with the default
# (a directive with no METACLASS takes its superclass's), a mixin with a
# metaclass, and a metaclass declared later in the file. Beside it sit the
# programs that bound the donation, each one a shape the oracle answers 97.1
# for because the method never reaches the class side at all -- a program
# belongs there when its send resolves to nothing under the donation rule
# the first program measures.
lang/class_metaclass.rex
lang/class_metaclass_class_method_does_not_donate.rex
lang/class_metaclass_superclass_wins.rex
# The refusal ladder, each program shaped so it pins where its check sits as
# well as what it reports. 98.908 is METACLASS's own not-found error and is
# raised ahead of SUBCLASS's 98.909; 99.927 is a target that resolves to a
# class metaclass-ness never reached, tested before deriving from a metaclass
# discards the target altogether.
lang/class_metaclass_not_found.rex
lang/class_metaclass_not_a_metaclass.rex
# ABSTRACT's effect a program can reach before ~new: a metaclass cannot be
# made abstract. Its position in the directive is pinned too -- the INHERIT
# list is walked first.
lang/class_abstract_metaclass.rex
lang/class_abstract_metaclass_subclass.rex
lang/class_abstract_metaclass_after_inherit.rex

# Phase 5a (2026-08-17 plan) Task 9: the Object and Class reflection
# protocol.
#
# The protocol itself, asked of a class a ::CLASS declares and of the
# primitive classes whose superclass list this crate has -- the ones
# CoreClasses.orx sends ~inherit gain a mixin no directive here can add, and
# asking one of those would be asking about the prologue.
lang/class_reflection.rex
lang/class_package.rex
# ~method reads the class's own instance dictionary and nothing else, which is
# the guard against a build that flattens every scope onto one class. There is
# no table C row for the scope question, so these programs are the whole of
# it: one carries the answering rows and ends untrapped on a donated name, the
# other ends untrapped on the class's own class method, and a build reading the
# class behaviour instead answers one and raises the other.
lang/class_method_own_dictionary.rex
lang/class_method_class_side_raises.rex
# The argument refusals of the methods that take one, which name their
# argument where the ones already here number it.
lang/class_reflection_argument_ladder.rex
# ~superClasses answers an array, so Array~makeString is what a program can do
# with one -- both its forms, its separator, the empty list, and the string
# value an array has in a string context, which is a different answer from
# either form.
lang/array_make_string.rex
lang/array_make_string_refusals.rex
# The array's own name in a 97.1, which is stringValue() and not the string
# value a string context asks for.
lang/array_unknown_method.rex

# Phase 5a (2026-08-17 plan) Task 11: the Array and the Directory 5a's own
# mechanisms send to.
#
# The list expression and the Array method set this task implements, the two
# DO OVER shapes -- a variable holding an array, and the comma-separated
# expression list over a line continuation the prologue writes -- and a
# Directory's ~put/[]/~at over both directories this interpreter builds.
lang/array_list_expression.rex
lang/array_do_over.rex
lang/directory_at_and_put.rex
# The refusal ladders, one per receiver class, because the same three message
# names carry different errors on each: an Array's `[]`/`At` are A_COUNT and
# refuse a second subscript from their own body with 93.926, where a
# Directory's declare a count and refuse it from the send with 93.902.
lang/array_index_refusals.rex
lang/directory_index_refusals.rex
# Every trace value line renders through stringValue(), so an array traces as
# `an Array` where the same array in a string context is its items joined.
# Compared raw (see corpus.rs's RAW_STDERR_COMPARISON), because the >A>/>>>
# pair a list emits and the >K>/>=> pair a DO OVER emits are indented lines
# and the indents are the point.
lang/array_trace.rex

# Fix round 1, finding B2: RAISE's two spellings of a substitution list are one
# list on the oracle, because `requestArray` answers an array unchanged. The
# pair is what stops a build implementing one spelling and not the other, and
# each is untrapped because the substituted message is the only channel that
# shows the list -- `condition('A')` would answer the array itself and this
# crate refuses that option. All three are compared raw (see corpus.rs's
# RAW_STDERR_COMPARISON): the traced value lines are indented and the indents
# and the `an Array` renderings are the subject.
lang/raise_additional_array.rex
lang/raise_array_spelling.rex
lang/raise_keyword_object_traces.rex

# Fix round 2: a nested array as a substitution item, which needs a list inside
# a list and so became reachable only once the parenthesised list evaluated --
# measured, both shapes are rc 120 loud at f4b21eadb~1. The item is rendered by
# stringValue(), so it reads `an Array` rather than its inner elements joined.
# One program per spelling, both raw-compared, for the reason the pair above is
# two programs.
lang/raise_array_nested.rex
lang/raise_additional_nested.rex

# Phase 5a (2026-08-17 plan) Task 12: what the documented search order has
# after the class chain.
#
# The UNKNOWN forward and its arguments -- the missed name, then an array of
# the send's own arguments -- as the branch of the miss arm that answers, with
# message_send_unknown_method.rex the branch that raises. Class J's UNKNOWN
# only says where the others return, so its rows are whole clauses: SAY over a
# forward that returned nothing is 91.999 rather than the forward's own answer.
lang/message_send_unknown_forward.rex
# The NOMETHOD condition beneath it, trapped by name, trapped by ANY, and
# degraded to the 97.1 syntax error a SIGNAL ON SYNTAX takes. Whose trap table
# decides is the running activation's: an internal CALL inherits its caller's
# traps by copy, so a caller's NOMETHOD handler does beat a callee's SYNTAX
# handler, while a ::METHOD activation inherits none and the same main-body
# NOMETHOD handler is never reached. A build that asked the whole activation
# stack instead answers that method-body row with the NOMETHOD handler.
lang/condition_nomethod.rex

# Phase 5a (2026-08-17 plan) Task 13: the three access scopes.
#
# PRIVATE is decided by who is sending, and the input is the caller's own
# receiver: self~m is allowed from inside the declaring class and from a
# subclass's own class method alike, and the refusals are a program or routine
# frame, which carries no receiver, and a sibling class the defining scope's
# hierarchy does not contain. A refused send drops the method and enters the
# receiver's own UNKNOWN rather than raising, and becomes a report only for a
# receiver that answers none.
lang/method_access_private.rex
# The same refusal untrapped, for its traceback bytes: two frames, and 97.2
# where a build treating the refusal as a name miss would print 97.1.
lang/method_access_private_refused.rex
# PACKAGE's allowing side, which is every sending position one package can
# offer, and PROTECTED, whose security-manager question has one answer while
# nothing installs a manager. Neither row is a new agreement; both are the
# guard on a send that starts refusing.
lang/method_access_package_and_protected.rex
# The same check on a name a ::ATTRIBUTE generates rather than one a ::METHOD
# declares, over both accessor names: the getter reached through self~a from
# inside, and the setter reached through the message-assignment form from
# outside. The refusing side of this one used to be a rc 120 refusal the oracle
# did not share, so it could not be a row at all.
lang/method_access_private_attribute.rex

# Phase 5a (2026-08-17 plan) Task 14: the required-string protocol.
#
# Every context provide.xml `reqstr` lists and this phase can reach, with a
# receiver whose makeString answers a value that context can use, against the
# same contexts with a receiver that has none -- where the protocol's last limb
# supplies the object's default name and the contexts needing a number, a label
# or a variable symbol raise from that name. The pair is what stops a build
# skipping the conversion: it would pass the second program and fail the first.
lang/required_string_contexts.rex
lang/required_string_default_name.rex
# The NOSTRING condition, trapped by name, trapped by ANY and untrapped, with
# the documented example's own two clauses -- substr(d,5,7) answering and then
# raising for the same receiver -- and a makeString receiver as the control that
# the trap is not consulted when the first limb answers.
lang/required_string_nostring.rex
# The messages the protocol answers in its own right. ~string and ~objectName
# part on a string,
# ~request answers through the MAKE method and through the class-id match, and
# ~objectName= moves stringValue() without touching the conversion.
lang/required_string_face.rex
# What follows from where a builtin's argument conversion happens: position
# order whatever order the builtin reads in, SUBSTR's pad converted whether or
# not it is used, and a bad argument named by the object rather than by its
# conversion -- with the pad as the exception that fixes that last rule.
# The position the oracle fetches raw is its own program below.
lang/required_string_builtin_arguments.rex
# The NOSTRING condition with no directive in the file, so the SIGNAL ON is the
# only thing that can arm the protocol. required_string_nostring.rex covers the
# same condition and declares a makeString, which arms it at install and hides
# the trap's own arming site; this one declares nothing, and is the only corpus
# program that reddens when that site is disabled.
lang/required_string_nostring_no_directive.rex
# The one argument position in the whole builtin set that the oracle fetches
# raw: VALUE's new value, read with `optional_argument`, so the object is stored
# and the conversion happens later at whatever renders it. Both halves matter --
# the ordering shows the conversion is not at the call, and the class shows an
# object came back out of the variable -- and the NOSTRING rows reach the same
# position with the protocol armed by a trap and no directive at all.
lang/required_string_builtin_raw_argument.rex
# The dyadic-operator context, which is the operand on the *right*. Every
# operator family, and the two error messages that disagree about which operand
# they name.
lang/required_string_operator_argument.rex
# The section's "for all other methods" rule, re-derived: a makeString puts an
# object on the answering side of the 88.909 line
# message_send_argument_object_not_a_string.rex draws from the shape of the
# value instead.
lang/required_string_method_argument.rex
# A condition raised inside a makeString the protocol reached, which carries the
# REQUEST activation's own traceback frame -- one frame from a language context,
# two from a method argument. Both compared raw (see corpus.rs's
# RAW_STDERR_COMPARISON).
lang/required_string_make_string_raises.rex
lang/required_string_argument_make_string_raises.rex

# Phase 5a (2026-08-17 plan) Task 15: the method-declaring directives' option
# surface.
#
# The accessor pair a `::ATTRIBUTE` and a `::METHOD ... ATTRIBUTE` generate.
# The value program carries the round trip, the as-written variable spelling
# against the upcased message name, the pool entry EXPOSE reaches from both
# sides, and one declaration read back through a second receiver; the programs
# beside it are the refusals a program can read out of the pair, and they are
# separate programs because a program can only fail once.
lang/method_attribute_generated.rex
lang/method_attribute_generated_no_result.rex
lang/method_attribute_generated_getter_arguments.rex
lang/method_attribute_generated_setter_arguments.rex
lang/method_attribute_generated_setter_omitted.rex
# ABSTRACT, whose install is rc 0 and whose send is 93.965. The first line's
# output is what separates the two: it reaches stdout before the send fails.
lang/method_abstract_send.rex

# Phase 5a (2026-08-17 plan) Task 16: the GUARD instruction and REPLY inside a
# method.
#
# The guard's three spellings and both WHEN forms in one program, since each is
# a no-op with one activity and a no-op cannot fail on its own.
lang/method_guard_instruction.rex
# REPLY's own transcript, and the shape it is here for: EXIT STATUS 0 WITH A
# TRACEBACK, because 98.936 is raised in the body the reply left owed, after the
# main program has finished.
lang/method_reply.rex
# The legality checks either instruction can fail, one program each because a
# program can only fail once: GUARD and REPLY outside a method invocation, a
# second REPLY, and a bare REPLY read in an expression position. The bare one
# also carries the remainder running past the sender's own failure.
#
# method_reply.rex and method_reply_twice.rex are in corpus.rs's
# RAW_STDERR_COMPARISON: both report from the body a reply left owed, and the
# indent that report's clause line sits at is reached by no other program.
lang/method_guard_outside_method.rex
lang/method_reply_outside_method.rex
lang/method_reply_twice.rex
lang/method_reply_no_result.rex
# What a raise in an owed body does to the process: nothing. The main body's
# own EXIT chose 7 and the 98.936 beneath it did not change it.
lang/method_reply_exit_status.rex
# An owed body that sends a message whose method replies in its turn, so the
# queue is added to while it is being drained.
lang/method_reply_chain.rex
# GUARD's own value check: a WHEN expression that names an exposed variable
# but is not exactly `0` or `1` is 34.902, not IF's 34.1 or WHEN's 34.2.
lang/method_guard_when_not_logical.rex

# Phase 5a (2026-08-17 plan) Task 17: the environment search order, the
# Directory entry methods, and the .METHODS join.
#
# The two steps of the order a program with no ::CLASS can observe, which
# needed the entry-method mechanism before the shadowed name could be set at
# all; the mechanism itself, on the shipped .environment~local entry and on
# one a program stores; and the package tables, whose entries are what
# CoreClasses.orx reaches through .methods[name].
#
# .RESOURCES is not here and cannot be: a ::RESOURCE body is source lines that
# no clause span covers, and rexx-parse's every_corpus_program_tiles requires
# every byte of a corpus program to be tiled by a clause node. Its rows are
# dispatch.rs's the_package_tables_hold_what_their_directives_declare.
lang/environment_local_shadows_the_environment.rex
lang/environment_directory_entry_method.rex
lang/environment_methods_join.rex
lang/environment_routines_table.rex

# Phase 5a (2026-08-17 plan) Task 18: the install passes and class-object
# initialization.
#
# The INIT/ACTIVATE pair, which discriminates only together: both methods ask
# hasMethod of the same class object and only the moment differs. A constant
# expression naming a class declared later, which resolves because constants
# are a second pass over an already-installed list. The construction order every
# pass walks. What each form of a ::CONSTANT answers, the 97.4 an
# expression form gets while its own class is still being built, and the class
# a failing ACTIVATE is blamed against.
lang/class_init_activate_inherit_merge.rex
lang/class_constant_expression_later_class.rex
lang/class_init_activate_order.rex
lang/class_constant_values.rex
lang/class_constant_uninitialized.rex
lang/class_activate_failure_blames_the_last_installed_class.rex

# Task 18, fix round 1: the duplicate-member refusals, which had no
# counterpart here at all -- the oracle refuses the file at translation time
# and this crate ran it, answering the last member installed.
#
# One row per code, because a program can only fail once: ::METHOD's own,
# ::METHOD colliding with a ::CONSTANT that occupies both dictionaries,
# ::ATTRIBUTE's own reached through a setter key nothing in the file spells,
# ::CONSTANT's own, and the CLASS keyword with no ::CLASS above it. The last
# row is the negative control: every name in it is written twice and the
# oracle runs all of it, so a rule phrased over the directive's name rather
# than over the keys it claims fails there.
lang/class_duplicate_method.rex
lang/class_duplicate_constant_and_method.rex
lang/class_duplicate_attribute.rex
lang/class_duplicate_constant.rex
lang/class_member_class_keyword_needs_class.rex
lang/class_member_names_per_side.rex

# Task 18, fix round 3: the rest of the duplicate-directive family. ::CLASS
# was the last silent wrong answer in it, and ::RESOURCE the last gap.
#
# The refusal row is chosen to exclude three weaker rules at once: the pair is
# spelled once as a lower-case symbol and once as a quoted upper-case literal,
# the two are not adjacent, and a ::CLASS naming an unresolvable superclass
# follows them. The control row says the tables are per directive kind.
#
# ::RESOURCE has no row and cannot: every_corpus_program_tiles requires every
# byte of a corpus program to be covered by a clause node and a ::RESOURCE body
# is not clauses, measured directly. Its sole instrument is run/tests.rs's
# a_duplicate_resource_name_is_refused_and_a_distinct_one_is_not.
lang/class_duplicate_class.rex
lang/class_directive_names_are_their_own_table.rex

# Phase 5a (2026-08-17 plan) Task 19: what a ::CONSTANT installs, and what its
# expression runs as.
#
# The instance-side getter, read through ~method, with a class-only ::METHOD
# beside it in the same class so that the constant's answer says which
# dictionary it came out of. Then the expression's own activation: SELF and
# SUPER over a pair of classes that answer differently, and a PRIVATE class
# method the receiver is what reaches. Then the failure path through that
# activation, whose traceback carries a method clause the two-echo witnesses
# have no room for.
lang/class_constant_instance_method.rex
lang/class_constant_expression_self.rex
lang/class_constant_expression_method_failure.rex

# Phase 5a (2026-08-17 plan) Task 20: ::ANNOTATE's six targets and the readback.
#
# The six targets in one file, each read back through the handle a program has
# on what it annotated -- a class object, a Method object out of ~method, a
# Routine object out of .ROUTINES, and the Package object a class answers.
# Then the property that makes ~annotations more than a rendering: the table is
# the one the annotated thing owns, so an addition through it is visible to
# ~annotation afterwards. Then the refusal, whose target is declared below the
# ::ANNOTATE rather than absent, so it witnesses the resolution order.
lang/directive_annotate_targets.rex
lang/directive_annotate_table_is_live.rex
lang/directive_annotate_missing_target.rex

# Phase 5a (2026-08-17 plan) Task 21: the class mutators, the REXX_DEFINED
# lock, Setup.cpp's own removal and hiding, and the Package object.
#
# The lock has one row per mutator because the traceback's frame line names
# the message that was sent, and a check missing from one of the five is what
# five rows catch and one does not. Each is `.Array`, a class the image
# defines; the mutators succeed on a class the file declares, which is the
# next group.
lang/class_rexx_defined_define.rex
lang/class_rexx_defined_define_methods.rex
lang/class_rexx_defined_delete.rex
lang/class_rexx_defined_inherit.rex
lang/class_rexx_defined_uninherit.rex

# The mutators on a user class. The main row carries `~define`'s
# identity-preserving install against `~defineMethods`'s copy, the omitted
# argument's tombstone, `~delete` and `~uninherit`; the four beside it are the
# shapes that raise or that need their own file -- `.nil` in the method
# position, a delete of a name that is not there, `~inherit`'s position
# argument, and the three refusals.
lang/class_mutators_user_class.rex
lang/class_mutator_define_nil_removes.rex
lang/class_mutator_delete_absent_name.rex
lang/class_mutator_inherit_position.rex
lang/class_mutator_inherit_position_not_inherited.rex
lang/class_mutator_refusals.rex
lang/class_mutator_uninherit_not_inherited.rex
lang/class_mutator_define_methods_supplier.rex

# Setup.cpp's own removal and hiding, which `~method` reads apart: a hidden
# name answers `The NIL object` and a removed one raises. `.Array`'s rows are
# the decoy each removal is read against, and `.Queue~method("APPEND")` is
# what says the donation happened at all.
lang/class_native_hiding.rex
lang/class_native_removal_sort.rex
lang/class_native_removal_make_string.rex

# The Package object. `class_context_package.rex` reaches it through
# `RexxContext~package`, `class_package_classes.rex` reads and writes its class
# tables, and `class_package_addition_refused.rex` is the refusal the REXX
# package gives an addition. No row prints `~name` for the running package --
# it answers the program's own absolute path, and the corpus compares stdout
# byte for byte across checkouts.
#
# Beside them, the context object's own identity rather than the package's.
# `class_context_identity.rex` says it is one object per activation.
# `class_context_gc.rex` says it survives a forced collection while its
# activation is running or suspended. `class_context_reply.rex` says it
# survives one while the activation is parked by a REPLY. The last two exist
# because the sets were disjoint: no program held both `gc(` and `.context`,
# and none held both `reply` and `.context`, so neither rooting route had a
# differential row at all.
lang/class_context_package.rex
lang/class_context_identity.rex
lang/class_context_gc.rex
lang/class_context_reply.rex
lang/class_package_classes.rex
lang/class_package_addition_refused.rex
# Task 22: `::METHOD ... EXTERNAL 'LIBRARY REXX name'`. `_bind` is the shape
# `StreamClasses.orx:546`-`:549` uses -- a private class method read back
# through a `::CONSTANT` expression -- beside the entry point's other
# spellings, the default name and a case that differs from the exported one.
# `_missing` is the eager bind's failure, whose empty stdout is what a lazy
# bind would fill. `_arguments` is the entry point's own argument check, whose
# major line names no source line. `_duplicate_wins` and `_source_order` pin
# where the resolution sits in the install walk, from either side of it, with
# `_before_duplicate` the reverse of `_duplicate_wins` and the differential
# that makes the reverse order evidence for that placement.
# `_not_a_staged_gap` is what a bound `::METHOD EXTERNAL` does not preempt:
# an install-time failure standing above it. The last row is gate table D's
# `::METHOD EXTERNAL` probe, which stays in `gate-tables/directives/` because
# that table's row set still names it.
lang/directive_method_external_bind.rex
lang/directive_method_external_missing.rex
lang/directive_method_external_arguments.rex
lang/directive_method_external_duplicate_wins.rex
lang/directive_method_external_source_order.rex
lang/directive_method_external_before_duplicate.rex
lang/directive_method_external_not_a_staged_gap.rex
gate-tables/directives/method__external__subkeyword.rex
# Task 23: the message scope override send, `target~name:scope`. `_chain` is
# the answering half -- SUPER inside a body, an ancestor named outright, the
# receiver's own scope, a scope folded in by INHERIT, an override that itself
# chains, a native method, a scope merged from the metaclass, the `~~` form,
# and the UNKNOWN a scoped miss reaches. `_not_a_scope` is
# `validateScopeOverride`'s own refusal, with the two answering lines above it
# that make the refused scope the variable. The 88.914 that a scope which is
# not a class object gets is `message_send_scope_override.rex`, above.
lang/message_send_scope_override_chain.rex
lang/message_send_scope_override_not_a_scope.rex
# Task 23 attempt 2: the interpreter's own Rexx-written library, running at
# start. `_state` asks the post-bootstrap state four ways -- a class the
# prologue put in `.environment`, `~superClasses` on a native class it
# mutated, a method set `inheritInstanceMethods` donated (which no class-graph
# assertion can see), and `.TraceObject~option`, which a bootstrap that
# skipped the install pass leaves at `OPTION`. `_setup_methods_gone` is the
# other side: the two methods the image build puts on `.Class` are gone and
# the `REXX_DEFINED` lock is closed again, with the work they did still
# visible above the refusal. `do_over_string_table` is the iteration the
# prologue's own loop needs, every row of it order-independent because this
# crate's index order is not the oracle's. `string_upper` is the method
# `CoreClasses.orx:73` needs twice in one clause.
lang/library_bootstrap_state.rex
lang/library_bootstrap_setup_methods_gone.rex
lang/do_over_string_table.rex
lang/string_upper.rex
# Task 23 fix round 1: a condition raised inside the interpreter's own
# library. The oracle runs those methods out of an image with no source and
# says so; this crate has the text and has to answer the same bytes. `_nested`
# is the two-frame case, which is what says the frame is built per activation.
lang/library_method_traceback.rex
lang/library_method_traceback_nested.rex
# Task 23 fix round 3: the same `REXX_DEFINED` lock on the other half of what
# carries it. The `class_rexx_defined_*` rows above are each `.Array`, a class
# `Setup.cpp` builds, and none of them can see whether the classes the
# library's own `::CLASS` directives declare carry the flag. `_inherit` is the
# argumentless discriminator on a `CoreClasses.orx` class, `_define` a second
# mutator on a `StreamClasses.orx` one, and `_no_mutation` reads the class
# back after the refusal, which the other two cannot: both halt on it.
lang/class_rexx_defined_library_inherit.rex
lang/class_rexx_defined_library_define.rex
lang/class_rexx_defined_library_no_mutation.rex
# `::ATTRIBUTE ... EXTERNAL 'LIBRARY REXX name'` and the `::METHOD ...
# ATTRIBUTE EXTERNAL` spelling of it. `_bind` is the resolving half, one
# accessor per directive with a GET and a SET style in one class. What each
# other row pins is the name the 90.998 report carries: `_missing` is the
# prefix standing in front of the procedure rather than behind the method
# name, `_get_third_word` is the style that resolves its third word unchanged,
# `_set_default` is the spelling that makes an explicit third word the default
# one again, and `directive_method_attribute_external_missing` is the
# `::METHOD` keyword reaching the same code with no style to choose.
# `_arguments` is a bound setter's own argument check. The last row is gate
# table D's `::ATTRIBUTE EXTERNAL` probe, which stays in
# `gate-tables/directives/` because that table's row set still names it.
lang/directive_attribute_external_bind.rex
lang/directive_attribute_external_missing.rex
lang/directive_attribute_external_get_third_word.rex
lang/directive_attribute_external_set_default.rex
lang/directive_attribute_external_arguments.rex
lang/directive_method_attribute_external_missing.rex
gate-tables/directives/attribute__external__subkeyword.rex
# `Method~scope` on the routes to a method object beyond the directive pair
# `gate-tables/concepts/xscope.rex` asks about: the unattached
# `::METHOD` no class has taken, the two `~define` shapes, `~defineMethods`,
# the accessor and constant names, a class `Setup.cpp` builds and a donated
# name. It ends untrapped on the argument refusal, whose frame line names the
# scope the native method resolved at.
lang/method_scope.rex
