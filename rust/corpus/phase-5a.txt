# Phase 5a corpus subset -- the union with phase-4a.txt, phase-4b.txt and
# phase-4c.txt is what the harnesses read, not this file alone. Every entry
# in those three stays in force.
#
# One path per line, relative to this directory (rust/corpus/). Lines starting
# with # are comments; blank lines are ignored. Same format as the earlier
# phase files, and read by the same `read_subset`, which unions the files in
# first-seen order and drops a repeat.
#
# Task 1 built the harness plumbing this file needs (an unnormalised stderr
# comparison mode, a directive walker admitting `::CLASS`/`::METHOD` without
# panicking, and this file wired into every harness that reads a phase subset
# list) and added no corpus program of its own. Each later task appends its
# own witness here as its own construct starts agreeing with the oracle.

# Task 4: the directives that install. `::CLASS`, `::METHOD` and `::ATTRIBUTE`
# installing under it, and `::CONSTANT`'s two shapes -- a well-formed
# parenthesised expression (evaluated and discarded, since nothing can read
# it back before message dispatch exists) and the two ways it fails: the
# expression itself raising, and the structural "no preceding ::CLASS" check.
lang/directive_class_installs.rex
lang/directive_method_installs.rex
lang/directive_attribute_installs.rex
lang/directive_constant_expression_installs.rex
lang/directive_constant_expression_fails.rex
lang/directive_constant_expression_needs_class.rex
lang/directive_constant_expression_blames_the_last_class.rex

# Task 5: the message send, the scope override, and the dispatch seam. Sends
# to each value kind this crate can build, resolution from the receiver's own
# class and from an inherited scope, the three ways a primitive method's
# arguments can be wrong, the unknown-method condition, the scope-override
# refusal, the instruction form's own RESULT, and the message-assignment form.
lang/message_send_native.rex
lang/message_send_inherited_scope.rex
lang/message_send_unknown_method.rex
lang/message_send_unknown_method_on_nil.rex
lang/message_send_unknown_method_on_a_number.rex
lang/message_send_scope_override.rex
lang/message_send_too_many_arguments.rex
lang/message_send_missing_argument.rex
lang/message_send_argument_not_a_string.rex
lang/message_instruction.rex
lang/message_assignment_form.rex

# Task 6: the four directories, and the seam's second site. The four objects
# and the reflection names beside them, both of VALUE's one-argument routes,
# a class this file declares resolved by name from inside it, the package
# table shadowing the environment, the three names the parser resolves before
# either route can, and the package method table in both of its states.
lang/environment_symbols.rex
lang/environment_symbols_through_value.rex
lang/environment_package_class.rex
lang/environment_package_class_shadows_the_environment.rex
lang/environment_special_dot_variables_are_not_resolved.rex
lang/environment_methods_table_unattached.rex
lang/environment_methods_table_attached.rex

# Task 6 fix round 1 (ruling R12): what an operator, a condition and a builtin
# do with one of these objects as an operand -- the half the two
# implementations agree on. The half they do not is loud, which a corpus file
# cannot witness, and `eval.rs`'s `object_operand_tests` carries instead.
lang/environment_object_operands.rex

# Task 6 fix round 2: the DO header's numeric positions convert their value
# through a unary operator, so R12 reaches them too. This is the boundary
# either side of the three it refuses -- FOR, a bare repeat count, NUMERIC
# DIGITS and a builtin argument all read the value's text instead and go on
# agreeing.
lang/environment_object_in_a_loop_header.rex

# Task 6 fix round 4: RAISE substitutes its additional information, and the
# oracle array-converts it only under a SYNTAX condition. This is the side
# that does not convert, and every line of it was refused by the first
# version of that fix.
lang/environment_object_in_a_raise.rex

# Task 7: invoking a Rexx method body. A class method is the one kind a
# program can reach in this phase -- every other receiver whose behaviour
# could answer a ::METHOD is an instance, and nothing builds one until ~new
# lands -- so these witness a class object as a receiver, the activation a
# ::METHOD body runs in, `self` and `super`, RETURN with and without a value,
# EXIT, an untrapped condition inside a body, a `::ATTRIBUTE GET` whose
# accessor is written in Rexx, and the `>I>`/`<I<` pair a method activation
# adds around the callee's own clause echoes. The traced pair is compared raw
# (see corpus.rs's RAW_STDERR_COMPARISON), because the indents are the point.
lang/method_class_body.rex
lang/method_returns_no_value.rex
lang/method_no_result_is_an_error.rex
lang/method_exit_returns_to_the_sender.rex
lang/method_body_raises.rex
lang/method_class_side_lookup.rex
lang/method_attribute_body.rex
lang/method_trace_invocation.rex
lang/method_trace_nested.rex

# Task 7 fix round 1: the `::ATTRIBUTE SET` body nothing was running. Its
# resolved name carries an appended `=` that the getter's does not, and the
# >I>/<I< pair is the only place a program can read that name itself rather
# than infer it from which body ran, so this one joins corpus.rs's
# RAW_STDERR_COMPARISON.
lang/method_attribute_set_body.rex

# Task 7 fix round 1: the calling context a method activation answers to
# PARSE SOURCE, which was `COMMAND` here against the oracle's `METHOD` -- rc 0
# and empty stderr on both sides, so no refusal named an owner and nothing in
# the tree ran the program that would have shown it.
# corpus/lang/parse_sources.rex carries the contexts a method is not, and the
# pair is what stops a single fixed word passing both.
lang/method_parse_source.rex

# Task 7 fix round 2: what a primitive method does with an argument that has
# no string value. `.nil` was already refused; a class object and one of the
# interpreter's own objects were answered `0` at rc 0 where the oracle raises
# 88.909, and a stem answers as whatever its default holds. The rows that
# answer are in the same program, so a build refusing every unrecognised
# argument fails it too.
lang/message_send_argument_object_not_a_string.rex

# Task 8: EXPOSE and scope-keyed instance variables. A class object's own
# variable pool round-tripped across sends, with the uninitialised state, DROP
# and the two builtins that read a variable by name; a stem as an instance
# variable, tails and object identity included; the four ways an internal CALL
# meets an exposed name; the EXPOSE (list) form, whose selector is bound before
# its value is read; and EXPOSE where there is no method invocation to expose
# into.
lang/expose_class_variable.rex
lang/expose_stem.rex
lang/expose_internal_call.rex
lang/expose_indirect_list.rex
lang/expose_outside_a_method.rex

# Task 8: the coercion audit's own question, asked of the position EXPOSE
# adds. An instance variable is somewhere one of the interpreter's own objects
# can be put and taken out again, and the table's rule is whether this crate
# already produces the oracle's bytes there.
lang/expose_object_value.rex

# Task 8, ruling R26: `::CLASS ... SUBCLASS <class>`, which is what makes a
# second scope reach one object at all -- and is used by `CoreClasses.orx`
# itself. A parent's class method answering through the child, a target
# declared later in the same file, a chain of three, a package class winning
# over the environment entry of the same name, and the two ways a target
# fails: no name resolves to it, and the targets cannot be put in an order.
lang/class_subclass.rex
lang/class_subclass_not_found.rex
lang/class_subclass_cycle.rex

# Task 8's own acceptance criterion, which needs the three above to run: a
# subclass and its parent each exposing the same name, both sends addressed to
# the subclass's class object, holding different values at once.
lang/expose_two_scopes.rex

# Task 8 fix round 1: which ::CLASS a failing ::CONSTANT expression is blamed
# against once the file's classes no longer install in source order. The blame
# is the class installed last, which this program makes the first one in the
# file. That alone is also what reading source order backwards gives, so this
# program pins the rule only together with
# directive_constant_expression_blames_the_last_class.rex above, which reddens
# under exactly the backwards reading this one is green under. Measured, each
# positional rule built in turn: 105 of 106, one program differing each time.
lang/directive_constant_blames_the_last_installed_class.rex

# Phase 5a (2026-08-17 plan) Task 6: the native-method frame in a traceback,
# the operator half. An error raised inside a native method an *operator*
# invoked carries the same `*-* Compiled method "X" with scope "Y".` frame
# line the oracle emits, when the operator reaches that method through a
# stem's own forward to its default value rather than running directly on a
# `String`/`Number` value -- `b.~class` is `The Stem class` while the frame
# reads scope `String`, the class of the method that actually raises.
# Varies the operator across the programs below: infix `+`, infix `**` so
# the frame's own name is read rather than assumed, and prefix `-`, whose
# dispatch path is different from infix `-` and happens to collide with its
# frame text. Compared raw (see corpus.rs's RAW_STDERR_COMPARISON) so the
# comparison asserts the frame's own bytes -- leading whitespace and absent
# line number included -- rather than resting on what DEVIATION 0's
# normaliser happens to do to them; measured, od-verified, that normaliser
# is currently a no-op on every program below's stderr.
lang/operator_frame_stem_plus.rex
lang/operator_frame_stem_power.rex
lang/operator_frame_stem_prefix_minus.rex

# Fix round 1, finding 3: the same forwarded frame reached from a
# controlled `DO` header's numeric position instead of from an operator
# directly. `Interp::header_number` rounds each header position through a
# real unary `+` on the oracle, so a stem in any position carries the
# identical frame, one program per position -- the initial value, the `TO`
# bound and the `BY` increment.
lang/operator_frame_stem_do_initial.rex
lang/operator_frame_stem_do_to.rex
lang/operator_frame_stem_do_by.rex

# Fix round 2, finding 1: the frame belongs to the receiver of a forwarded
# operator whatever step of evaluating it raises, not only its own
# conversion -- `Interp::arith_general`, `Interp::apply_prefix` and
# `Interp::logical_values` (`eval.rs`) each blame once over their whole
# computation rather than only over the conversion `Interp::arith_left_operand`
# itself performs. Each program below reaches the frame from a different
# failing step past that conversion: arithmetic overflow past a valid
# conversion, the power exponent's own range check, a non-logical operand on
# `&` and on prefix `\`, and a DO header's own range check past a valid
# conversion.
lang/operator_frame_stem_divide_by_zero.rex
lang/operator_frame_stem_power_exponent_range.rex
lang/operator_frame_stem_logical_and.rex
lang/operator_frame_stem_prefix_not.rex
lang/operator_frame_stem_do_exponent_range.rex
