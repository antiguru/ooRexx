/* No readback: SCIENTIFIC is FORM's default, so form() answers the same
   whether the directive took effect or was parsed and dropped. This row's
   force is acceptance of the keyword, like every non-readback row here. */
say 'main'
::options form scientific
