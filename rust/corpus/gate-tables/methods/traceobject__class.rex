/* Table C method rows: TraceObject, class arm -- .TraceObject~hasMethod("M") for
   every method corpus/docs/class-methods.txt documents on this arm,
   one line per row and in the row set's own order. Derived by
   crates/rexx-exec/tests/gate_table_c.rs, which re-derives this file on
   every run and compares it in both directions. */
say 'class' .TraceObject~hasMethod("activate")
say 'class' .TraceObject~hasMethod("collector")
say 'class' .TraceObject~hasMethod("collector=")
say 'class' .TraceObject~hasMethod("counter")
say 'class' .TraceObject~hasMethod("makeStringImpl")
say 'class' .TraceObject~hasMethod("new")
say 'class' .TraceObject~hasMethod("notify")
say 'class' .TraceObject~hasMethod("notify=")
say 'class' .TraceObject~hasMethod("option")
say 'class' .TraceObject~hasMethod("option=")
say 'class' .TraceObject~hasMethod("setMakeString")
say 'class' .TraceObject~hasMethod("unsetMakeString")
