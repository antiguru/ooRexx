/* Table C method rows: RexxQueue, class arm -- .RexxQueue~hasMethod("M") for
   every method corpus/docs/class-methods.txt documents on this arm,
   one line per row and in the row set's own order. Derived by
   crates/rexx-exec/tests/gate_table_c.rs, which re-derives this file on
   every run and compares it in both directions. */
say 'class' .RexxQueue~hasMethod("new")
say 'class' .RexxQueue~hasMethod("create")
say 'class' .RexxQueue~hasMethod("delete")
say 'class' .RexxQueue~hasMethod("exists")
say 'class' .RexxQueue~hasMethod("open")
