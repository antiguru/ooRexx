/* Table C method rows: DateTime, class arm -- .DateTime~hasMethod("M") for
   every method corpus/docs/class-methods.txt documents on this arm,
   one line per row and in the row set's own order. Derived by
   crates/rexx-exec/tests/gate_table_c.rs, which re-derives this file on
   every run and compares it in both directions. */
say 'class' .DateTime~hasMethod("new")
say 'class' .DateTime~hasMethod("fromBaseDate")
say 'class' .DateTime~hasMethod("fromCivilTime")
say 'class' .DateTime~hasMethod("fromEuropeanDate")
say 'class' .DateTime~hasMethod("fromIsoDate")
say 'class' .DateTime~hasMethod("fromLongTime")
say 'class' .DateTime~hasMethod("fromNormalDate")
say 'class' .DateTime~hasMethod("fromNormalTime")
say 'class' .DateTime~hasMethod("fromOrderedDate")
say 'class' .DateTime~hasMethod("fromOrdinalDate")
say 'class' .DateTime~hasMethod("fromStandardDate")
say 'class' .DateTime~hasMethod("fromTicks")
say 'class' .DateTime~hasMethod("fromUsaDate")
say 'class' .DateTime~hasMethod("fromUtcIsoDate")
say 'class' .DateTime~hasMethod("fromWeekNumberDate")
say 'class' .DateTime~hasMethod("maxDate")
say 'class' .DateTime~hasMethod("minDate")
say 'class' .DateTime~hasMethod("today")
