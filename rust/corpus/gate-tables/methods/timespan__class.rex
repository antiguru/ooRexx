/* Table C method rows: TimeSpan, class arm -- .TimeSpan~hasMethod("M") for
   every method corpus/docs/class-methods.txt documents on this arm,
   one line per row and in the row set's own order. Derived by
   crates/rexx-exec/tests/gate_table_c.rs, which re-derives this file on
   every run and compares it in both directions. */
say 'class' .TimeSpan~hasMethod("new")
say 'class' .TimeSpan~hasMethod("fromCivilTime")
say 'class' .TimeSpan~hasMethod("fromDays")
say 'class' .TimeSpan~hasMethod("fromHours")
say 'class' .TimeSpan~hasMethod("fromLongTime")
say 'class' .TimeSpan~hasMethod("fromMicroseconds")
say 'class' .TimeSpan~hasMethod("fromMinutes")
say 'class' .TimeSpan~hasMethod("fromNormalTime")
say 'class' .TimeSpan~hasMethod("fromSeconds")
say 'class' .TimeSpan~hasMethod("fromStringFormat")
