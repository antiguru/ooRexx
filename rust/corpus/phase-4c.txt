# Phase 4c corpus subset -- the union with phase-4a.txt and phase-4b.txt is
# what the harnesses read, not this file alone. Every entry in those two stays
# in force.
#
# One path per line, relative to this directory (rust/corpus/). Lines starting
# with # are comments; blank lines are ignored. Same format as phase-4a.txt and
# phase-4b.txt, and read by the same `read_subset`, which unions the files in
# first-seen order and drops a repeat.
#
# WHY THIS FILE EXISTS AND WHY IT IS NOT AN EXTENSION OF phase-4b.txt.
# phase-4b.txt's own header excludes PARSE by definition ("no PARSE/ARG/PULL"),
# so a witness for a construct 4c implements cannot go there without making
# that header false. Criterion 1 (tests/coverage.rs) requires every in-scope
# variant to be witnessed by some program in the subset it reads, so the moment
# a variant moves into scope its witness has to land in a file that admits it --
# this one.
#
# WHAT THIS SUBSET ADMITS, one line per construct as it lands. Everything
# phase-4a.txt and phase-4b.txt admit, plus:
#
#   Builtin function calls, in the names tests/builtin_status.rs measures as
#   implemented. corpus/builtin-status.txt is the machine-derived table of
#   which those are; a name it does not call implemented cannot appear in a
#   program listed here.
#
#   PARSE (4c Task 7) -- all eight template trigger kinds, the comma fence,
#   the `.` placeholder, and every source. PARSE VERSION is implemented but no
#   program here prints it: every field it carries is the interpreter's own
#   build identity, the third a build DATE, so a differential witness over it
#   would be a claim about which binary the oracle was built from. It is pinned
#   as a measured constant in parse_template.rs instead.
#
#   The ARG and PULL instruction spellings, and the PARSE PULL and PARSE LINEIN
#   sources (4c Task 8). The queue a program fills itself is fair game; the
#   console is not, beyond reading it at end of input, because the harness gives
#   both interpreters an empty stdin and a program whose answers depended on the
#   console's contents could not be deterministic here. A command-line argument
#   is likewise out of reach: the harness passes none, so a program here can
#   only ever see the no-argument case. Both of those are witnessed against the
#   oracle in crates/rexx-exec/tests/input_oracle.rs, which drives the rexx-run
#   binary with argv and a fed stdin rather than calling run_program in process.
#
#   The ADDRESS instruction's environment-naming forms (4c Task 9): the
#   constant `ADDRESS env`, the computed `ADDRESS VALUE expr` and `ADDRESS
#   (expr)`, and the bare toggle. `ADDRESS env command` and any `WITH`
#   redirection are the command dispatch and stay out, with the command
#   clause. Of the C++'s five readers of the environment, the only two a Rexx
#   program can reach are the ADDRESS() builtin and issuing a command; this
#   subset admits the first since Task 10, so a program here CAN now print the
#   environment back. lang/address_env.rex's own header carries the
#   enumeration and the blocks that read it.
#
#   ADDRESS()'s answer for an environment no ADDRESS instruction has set is
#   the platform default, `sh` on this host. It is in the corpus for the same
#   reason PARSE SOURCE's `LINUX COMMAND` is and PARSE VERSION's build date is
#   not: it is a compile-time constant of the interpreter (SYSINITIALADDRESS
#   in platform/<os>/PlatformDefinitions.h) rather than a value that moves
#   under a rebuild. A run on a platform whose default differs would need the
#   expected byte changed, which is true of `LINUX COMMAND` already.
#
#   ::ROUTINE, and calls into one (4c Task 13). No other directive: the other
#   eight are Phase 5's, and the ones whose installation the oracle performs
#   before `main` fail loudly here -- phase-4-exclusions.txt's directive
#   section has the transcript per form. tests/coverage.rs's
#   assert_program_has_only_routine_directives admits ::ROUTINE and keeps the
#   panic for the rest, and its walker descends into a routine's body, so a
#   construct used only inside one still counts toward criterion 1.
#
# WHAT IT STILL EXCLUDES. Everything phase-4b.txt excludes except builtin
# function calls, PARSE, ADDRESS's environment forms and ::ROUTINE, which is
# to say: no message sends, no directive other than ::ROUTINE, no command
# clauses, no ADDRESS command or WITH form, no DO WITH, and no DO OVER on a
# stem. The authoritative,
# machine-checked list is
# tests/owners.rs's INSTRUCTION_TAGS/EXPR_TAGS: anything still carrying an
# `Owner::Phase` there fails loudly and cannot appear in any program listed in
# this file. Later 4c tasks move their own construct across that line and
# delete its name from the paragraph above in the same change.
#
# NO PROGRAM HERE MAY DEPEND ON EXTERNAL ROUTINE RESOLUTION. The oracle
# searches for a Rexx file named for a call target that matches no label, no
# builtin and no ::ROUTINE; this crate raises 43.1 instead (Phase 7,
# phase-4-exclusions.txt). A program relying on that search would pass or
# fail here according to what happens to be sitting in the directory the
# harness runs it from, which is the opposite of the determinism rule below.
# It is also the easiest probe error in the phase: the session scratchpad is
# ON that search path and holds hundreds of stale .rex files, so the same
# three-line program has been measured reporting 44.1 rc 212 from the
# scratchpad root and 43.1 rc 213 from a clean directory.
#
# A program listed here must also obey the corpus's one standing rule
# (README.md, "The one rule: determinism"): same bytes on every run, on every
# machine, with no clock, no PID, no filesystem and no locale in its output.
# ONE EXCEPTION, and it is not a program's own doing: the oracle puts the
# package's ABSOLUTE PATH in its own >I>/<I< trace lines, so a program that
# announces a ::ROUTINE entry has one in its stderr. That stays comparable
# because this harness gives both interpreters the same canonicalised path in
# the same run; a program that PRINTS a path itself does not, and is still
# forbidden. lang/routine_dispatch.rex's own entry below says which lines.
# PARSE SOURCE's third word is the program's own absolute path, so no program
# here prints it -- corpus/lang/source_arg.rex already projects it away the
# same way, and parse_sources.rex says so in its own header.

# New for 4c Task 7 -- the template movement rules, on stdout alone. Its own
# header states, per block, which wrong answer each would print. The pair it
# exists for is `-n` against `<n`: same movement, unrelated assignment, and an
# engine that treats them as one operation gets one of the two blocks right
# and the other wrong.
lang/parse_triggers.rex

# New for 4c Task 7 -- the sources, the case options, and the one trace fact
# no stdout witness can reach: a target's own value line is a CHOICE between
# `>=>` under TRACE I and `>>>` under TRACE R, not two independent gates, and
# the `.` placeholder's `>.>` appears under I only. It therefore runs the same
# two templates under both modes. Its traced sections deliberately contain no
# PARSE SOURCE, whose `>K>` line would carry the program's path.
lang/parse_sources.rex

# Existing (Phase 2/3), listed here now that PARSE runs: string, literal,
# positional and UPPER templates. Kept rather than folded into the two above
# because it is the one PARSE program in the corpus written before this engine
# existed, so it is an expectation this task had no hand in shaping.
lang/parse_template.rex

# New for 4c Task 8 -- PULL, PARSE PULL, PARSE LINEIN and the ARG instruction.
# lang/push_queue.rex can pin that PUSH/QUEUE evaluate and trace their
# expression but not which end of the queue a line landed on, and block A here
# reads that order back. Its own header states, per block, which wrong answer
# each would print. Its E block runs the same five constructs under TRACE R and
# then TRACE I, because a target's value line is a choice of prefix between the
# two modes and because a bare PULL's `>K>` line carries the line before the
# upcase while the `>>>` after it carries the line after.
lang/pull_queue.rex

# 4c Task 9 -- ADDRESS naming an environment, extended at Task 10. Blocks A,
# B and C pin which forms trace a value line and which do not, that the value
# line is `>>>` under TRACE I as well as TRACE R, and the 250-byte name limit
# either side of the boundary with the 29.1 trapped. Blocks D and E are the
# witness the first three were owed and could not write: the swap four toggles
# deep, and inheritance into a callee in both halves, both read back through
# ADDRESS(). E is the only place in the whole suite that can see a callee's
# inherited alternate -- resolve_and_run_call pops the callee unconditionally,
# so no in-crate test can read its state.
lang/address_env.rex

# New for 4c Task 10 -- the builtins that read interpreter state, other than
# ADDRESS. Its own header states, per block, which wrong answer each would
# print. Two pairs are the reason it exists: TRACE()'s five silent settings,
# which an engine storing only what tracing DOES cannot tell apart, and
# CONDITION's `I` against `S`, which are perfectly correlated until a handler
# re-arms its own trap and only then reveal that one is stored and the other
# is looked up live. Its D block reads CONDITION from inside a live handler,
# because outside one every option is the null string and a stub returning ''
# passes.
lang/state_builtins.rex

# New for 4c Task 10 fix round 1 -- one message, twice, once with the axes
# crossed and once without. A builtin's range message substitutes the integer
# its argument converted to, and an argument whose own rendering differs from
# that conversion is the only kind that can tell the two apart. Fatal and on
# stderr, so it has to be its own program's last clause; its header says why a
# trapped handler cannot stand in.
lang/builtin_argument_range.rex

# New for 4c Task 13 -- ::ROUTINE dispatch, and the one witness for >I>/<I<
# anywhere in the suite. Its own header states, per block, which wrong answer
# each would print. It is here rather than in tests/trace_oracle/ because
# those two lines name the package by its ABSOLUTE PATH: a committed
# .expected would be true on the machine that captured it and false on the
# next, where a live run gives both interpreters the same path. That absolute
# path on stderr is the one exception to the no-paths rule above, and it is
# the oracle's own doing rather than something the program prints.
lang/routine_dispatch.rex

# New for the pre-Phase-5 defect round -- POS's fourth argument, and the one
# position its search reaches past the window that argument names. The window
# does not simply bound where a match may fit, and its own header states, per
# block, which wrong answer each of the two positional readings would print.
# Its B block is the pair that rules both out: two haystacks differing only in
# a decoy byte that is not a match and cannot be one, under the same start and
# range, answering differently. Nothing else in the suite reaches this --
# measured, removing the overrun reddens one test in the workspace and it is
# the transcribed table in builtin/string.rs, which is why a witness the
# oracle re-measures on every run belongs here too.
lang/pos_window.rex

# New for the pre-Phase-5 defect round -- which entries admit a PROCEDURE.
# A ::ROUTINE is entered by a call and is not an INTERNAL one, so 17.1 is the
# oracle's answer there in both directions, and this program pins that against
# the two internal shapes that are legal. Its C and D blocks trap the refusal
# and then assign a fresh variable in the CALLER, which is the half a fatal
# program cannot reach: an engine that admits the instruction has pushed a
# second frame onto an activation that already owned one, and it is the
# caller's next unbound name that lands on a frame no longer at the top. Its E
# block leaves the same refusal untrapped for the exit status and the clause
# echo, so that block is fatal and last.
lang/procedure_entry_rule.rex

# Where a plain DO offers a queued condition its boundary. A CALL ON handler
# ending in `raise ... return` leaves a second condition pending, so the
# printed order and the printed SIGL read back directly which clauses the
# construct ends and when -- on stdout, with no TRACE, which is what puts this
# here rather than in the trace harness. A plain DO is two clauses, the header
# and END, and the program pairs each with the shape that answers differently:
# an empty body agrees under one requeue whichever way the header is placed and
# separates only under two, and a DO UNTIL runs the other way, owing its
# requeue to the clause AFTER the loop. Its adjacent successes are the loops
# whose header is already a clause of its own and the two neighbouring
# constructs, which this subject must not move.
lang/do_clause_boundaries.rex

# What a clause boundary owes when more than one condition is waiting. The
# oracle takes everything the clause left pending, in the order it was queued,
# and defers only what a handler queues while it is running. A single-slot
# queue answered the last condition and lost the rest, silently -- nothing in
# the suite could see it, because every earlier program leaves at most one
# pending at a time. The E block is the pairing that neither rule produces
# alone: a pass whose body clause owes both a handler left over from the
# previous pass's END and the one it queued itself, both at its own line.
# Its header says why a repeated condition name is not in here.
lang/condition_queue_drain.rex
