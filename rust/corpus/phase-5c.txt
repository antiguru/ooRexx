# Phase 5c corpus subset -- the union with phase-4a.txt, phase-4b.txt,
# phase-4c.txt, phase-5a.txt and phase-5b.txt is what the harnesses read, not
# this file alone. Every entry in those five stays in force.
#
# One path per line, relative to this directory (rust/corpus/). Lines starting
# with # are comments; blank lines are ignored. Same format as the earlier
# phase files, and read by the same `read_subset`, which unions the files in
# first-seen order and drops a repeat.
#
# Written by Phase 5d's Task 1, which is where 5c closes. While 5c's rows were
# still landing this file could not exist: gate_table_c.rs's
# `every_closed_phase_this_table_owns_rows_for_is_gated` makes a committed
# phase-<id>.txt oblige CLOSED_PHASES to name that phase, and 5c's method rows
# could not pay that until their owner became a property of the row's class.
# So 5c's witnesses waited in test binaries instead, and this file is where
# they land.

# The three interim witnesses, each moved here from the binary that ran it;
# those binaries are deleted -- variable_reference.rs, stem_object.rs and
# string_makearray.rs.
lang/variable_reference.rex
lang/stem_object.rex
lang/string_makearray.rex

# `::OPTIONS`, whose whole family 5c delivered. tests/directive_options.rs is
# NOT deleted and still runs these: it compares stderr raw, on both engines,
# and carries the LOSTDIGITS table, none of which the differential does.
# `directive_options_trace_reply` traces from two threads and is on corpus.rs's
# CONCURRENTLY_TRACED for Deviation 7; the rest are compared as a sequence.
lang/directive_options.rex
lang/directive_options_call_on_keeps_it.rex
lang/directive_options_digits_below_fuzz.rex
lang/directive_options_internal_call.rex
lang/directive_options_nostring_syntax.rex
lang/directive_options_novalue_syntax.rex
lang/directive_options_novalue_trap_wins.rex
lang/directive_options_numeric_inherit.rex
lang/directive_options_signal_off.rex
lang/directive_options_trace.rex
lang/directive_options_trace_method.rex
lang/directive_options_trace_reply.rex

# Phase 3 and Phase 4 programs that were never named by any subset file and so
# were never run against the oracle here -- the coverage gap
# `every_lang_program_is_run_or_named_unfiled` closes. They are filed under 5c
# because this is the file that exists, not because 5c wrote them; nothing ties
# a subset file's contents to its name. Each was compared against the oracle on
# all three descriptors and on both engines before being filed.
lang/call_procedure.rex
lang/condition_syntax.rex
lang/do_variants.rex
lang/gate_variants.rex
lang/keyword_as_variable.rex
lang/source_arg.rex
lang/string_builtins.rex
lang/whitespace_significant.rex

lang/mutablebuffer_state.rex
lang/mutablebuffer_instance.rex
lang/mutablebuffer_readers.rex
lang/mutablebuffer_mutators.rex
lang/mutablebuffer_caseless.rex
lang/mutablebuffer_conversion.rex

lang/string_search.rex
lang/string_search_refusals.rex
lang/string_counts.rex
lang/string_counts_refusals.rex
lang/string_match.rex
lang/string_match_refusals.rex
lang/string_extract.rex
lang/string_extract_refusals.rex
lang/string_words.rex
lang/string_words_refusals.rex
lang/string_edits.rex
lang/string_edits_refusals.rex
lang/string_rewrites.rex
lang/string_rewrites_refusals.rex
lang/string_rounding.rex
lang/string_rounding_refusals.rex
lang/string_base64.rex
lang/string_base64_refusals.rex
lang/object_hashcode.rex
lang/object_hashcode_refusals.rex
lang/string_operators.rex
lang/string_operators_refusals.rex
lang/string_choice.rex
lang/string_choice_refusals.rex
lang/string_pad.rex
lang/string_pad_refusals.rex
lang/string_strip.rex
lang/string_strip_refusals.rex
lang/string_compare.rex
lang/string_compare_refusals.rex
lang/string_convert.rex
lang/string_convert_refusals.rex
lang/string_bits.rex
lang/string_bits_refusals.rex
lang/string_datatype.rex
lang/string_datatype_refusals.rex
lang/string_numeric.rex
lang/string_numeric_refusals.rex
lang/string_extremes.rex
lang/string_extremes_refusals.rex
lang/string_caseless.rex
lang/string_caseless_refusals.rex
lang/array_enumeration.rex
lang/array_item_argument.rex
lang/array_index_argument.rex
lang/array_extra_argument.rex
lang/supplier_iteration.rex
lang/array_navigation.rex
lang/array_structure.rex
lang/array_structure_refusals.rex
lang/array_sorting.rex
lang/queue_operations.rex
lang/list_operations.rex
lang/collection_subclasses.rex
lang/collection_of.rex
lang/array_append_index.rex
lang/queue_bounds.rex
lang/collection_item_equality.rex
lang/queue_extent.rex
lang/collection_callback_mutates.rex
lang/nil_comparison.rex
lang/list_index_conversion.rex
lang/list_empty_entry.rex
lang/list_empty_and_section.rex
lang/supplier_bounds.rex
lang/collection_construction.rex
lang/sort_comparisons.rex
lang/array_dimensions_append.rex
lang/hash_table_store.rex
lang/set_operations.rex
lang/relation_multivalue.rex
lang/directory_string_keys.rex
lang/stem_collection.rex
lang/collection_copy.rex
lang/directory_set_method.rex
lang/method_new.rex
lang/stem_request_and_directory.rex
lang/do_over_request_array.rex
lang/arg_option_array.rex
lang/map_collection_of.rex
lang/pointer_buffer_new_refused.rex
lang/weak_reference_value.rex
lang/rexx_info_readers.rex

# Task 4: `Method`'s seven flags, `~source`, `~package` and its four setters
# over one directive of every kind that makes a method; and `Routine`'s
# `~call`, `~callWith`, `~'[]'`, `~source`, `~package` and `~new`, each of the
# three that run code sent twice to one object.
lang/method_introspection.rex
lang/routine_introspection.rex

# Task 5: `Class`'s five graph readers, `Object`'s `isInstanceOf`,
# `instanceMethod` and `instanceMethods`, the comparison operators a class
# answers as messages in both spellings, and `Class~enhanced`'s rendering.
lang/class_introspection.rex

# Task 6: `RexxContext`'s fifteen readers and `StackFrame`'s ten, the four
# frame kinds one stack can carry, the snapshot/live-handle split between the
# two classes, and the two `MapCollection~of` raises whose message text is
# `.context~name`.
lang/rexx_context.rex
lang/rexx_context_arity.rex
lang/rexx_context_edges.rex
lang/stack_frames.rex
lang/rexx_context_new.rex
lang/stack_frame_new.rex
lang/map_collection_of_index.rex
lang/map_collection_of_pair.rex

# Tasks 7 and 8: `Package`'s thirty-three rows -- its source and settings
# readers, its ten table readers, the six `find*` readers and its four writes,
# on both receivers. `package_tables_lib.cls` and `package_tables_dep.cls` are
# the two helpers, `.cls` so that no corpus scan reads them as programs of
# their own; each declares two public classes and two public routines, so that
# a body answering a one-entry table cannot pass.
# `package_writes` is on corpus.rs's HASH_ORDERED_STDOUT for Deviation 8:
# what it prints is a StringTable's own iteration order, which is a bucket
# walk on both sides.
lang/package_settings.rex
lang/package_tables.rex
lang/package_find.rex
lang/package_writes.rex
lang/package_rexx.rex
