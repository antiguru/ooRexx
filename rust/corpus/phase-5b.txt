# Phase 5b corpus subset -- the union with phase-4a.txt, phase-4b.txt,
# phase-4c.txt and phase-5a.txt is what the harnesses read, not this file
# alone. Every entry in those four stays in force.
#
# One path per line, relative to this directory (rust/corpus/). Lines starting
# with # are comments; blank lines are ignored. Same format as the earlier
# phase files, and read by the same `read_subset`, which unions the files in
# first-seen order and drops a repeat.
#
# Task 0 wired this file into every harness that reads a phase subset list
# (SUBSET_FILES in corpus.rs, coverage.rs, collect_stress.rs and ir_dual.rs;
# the unguarded literal in trace_oracle.rs; and EXPECTED_SUBSET_5B beside
# EXPECTED_SUBSET_5A in coverage.rs) and added no corpus program of its own.
# Each 5b task appends its own witness here as its own construct starts
# agreeing with the oracle, and updates EXPECTED_SUBSET_5B in the same commit.

# Task 1: `~new`, `INIT`, and the instance seam. The concept-row probes this
# task makes agree, and the witnesses of what those rows do not send.
# `instance_naming.rex` sends `~string`, `~defaultName`, `~objectName`,
# `~objectName=`, `~class` and `~isA` to an instance, which no gate-table row
# does -- `creo.rex` sends `~type`/`~balance`/`~rate` and `abscla.rex` sends to
# a class -- and `Class~defaultName` beside them, which nothing reached before
# this task. `instance_naming_overrides.rex` sends the same names to a class
# that overrides `defaultName`, `string` or `makeString`, which is what makes
# them sends rather than stored text, and `instance_naming_raises.rex` is that
# claim's stderr arm: a `defaultName` that raises reports `Compiled method`
# frames for `OBJECTNAME` and `STRING` that no stored rendering could make.
# `instance_self_reassigned.rex` is a method body assigning over `SELF` and
# then allocating, the shape the receiver's rooting turns on.
gate-tables/concepts/creo.rex
gate-tables/concepts/abscla.rex
lang/instance_naming.rex
lang/instance_naming_overrides.rex
lang/instance_naming_raises.rex
lang/instance_self_reassigned.rex

# Task 1 fix round: the positions a named instance IS the value of. `~objectName=`
# can give an instance a name that spells a number or a truth value, and the
# oracle converts it in every position below and refuses it as an operator's
# left operand, where this crate's own refusal is loud and no differential row
# can carry it. Nothing else in the corpus puts an instance in an operand
# position at all, which is how a build answering `o + 1` shipped.
lang/instance_named_operands.rex

# Task 5: `UNINIT`. `obdes.rex` is the gate row a class-side `UNINIT` makes
# agree. `uninit_instance_collected.rex` is the delivery driven by a
# collection and `uninit_instance_retained.rex` the one D69 owes: a
# class-scope variable roots the instance for ever, so no collection can ever
# reach it and only the termination sweep runs its finalizer.
# `uninit_class_inherited.rex` and `uninit_class_mixin.rex` are the two arms
# `obdes.rex` cannot see, where the class that fires declares no `UNINIT` of
# its own -- one reaching it through `SUBCLASS` and one through `INHERIT`.
# `uninit_class_inherit_runtime.rex` is the `INHERIT` arm again as a runtime
# send, which is a second registration path and also the only row whose
# entries are not all made at directive-install time.
# `uninit_class_sweep_order.rex` pins the sweep's order over class objects
# against declaration order, which it contradicts on every class.
# The last three are each a delivery *point* rather than an order:
# `uninit_class_uninherit.rex` is a registration that outlives its method and
# runs nothing, `uninit_allocating_finalizer.rex` is a finalizer that
# allocates, which bounds the termination sweep, and
# `uninit_nested_collection.rex` is a collection driven from inside a
# finalizer, which must run no finalizer of its own. That last one prints
# only whether the inner finalizer ran inline, never when it did run: the
# oracle answers that two ways across runs.
gate-tables/concepts/obdes.rex
lang/uninit_instance_collected.rex
lang/uninit_instance_retained.rex
lang/uninit_class_inherited.rex
lang/uninit_class_mixin.rex
lang/uninit_class_inherit_runtime.rex
lang/uninit_class_sweep_order.rex
lang/uninit_class_uninherit.rex
lang/uninit_allocating_finalizer.rex
lang/uninit_nested_collection.rex

# Task 2: the behaviour snapshot (D58). `objcla.rex` is the gate row an
# instance holding the behaviour it was built with makes agree.
# `class_behaviour_snapshot_inherit.rex` and its `uninherit` twin are the arm
# that goes the other way, where the mutator rebuilds the behaviour object the
# instance already holds and the change reaches it; no committed program
# combined either mutator with an instance before.
# `class_behaviour_snapshot_delete.rex` is the copying family's third member,
# whose own divergence -- an existing instance losing a deleted method -- no
# row named; it asks three readers of the same behaviour, the name, the send
# and the `UNINIT` question, because a build can move them apart.
# `class_behaviour_snapshot_subclass.rex` is where the two families agree:
# both rebuild every subclass's existing behaviour in place, so a subclass's
# instance sees either mutation on its parent.
gate-tables/concepts/objcla.rex
lang/class_behaviour_snapshot_inherit.rex
lang/class_behaviour_snapshot_uninherit.rex
lang/class_behaviour_snapshot_delete.rex
lang/class_behaviour_snapshot_subclass.rex
