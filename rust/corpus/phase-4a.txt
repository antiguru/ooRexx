# Phase 4a corpus subset -- see README.md's "Phase 4a subset" section.
#
# One path per line, relative to this directory (rust/corpus/). Every entry
# below was read and checked against what Phase 4a's executor implements: no
# builtin function calls, no CALL/RETURN/PROCEDURE/USE/SIGNAL/RAISE/
# INTERPRET, no PARSE/ARG/PULL/PUSH/QUEUE, no message sends, no ::
# directives, no command clauses, no DO WITH, and no DO OVER on a stem.
# Lines starting with # are comments; blank lines are ignored.
#
# Existing programs (Phase 2/3) that already qualify:
#
# num/digits_rounding.rex, num/exponential.rex and num/operators.rex are
# deliberately NOT listed here even though they were checked and read as
# qualifying: all three build ExprKind::List from a comma in a SAY (e.g.
# operators.rex:8, `say (-2) ** 3 , (-2) ** 2`), and List is not 4a's --
# the oracle prints two lines at rc 0, 4a fails loudly, so criterion 1's
# zero-divergence requirement cannot hold while they are in this subset.
# They stay in corpus/num/ for 4b or 4c, once List exists; this is a
# subset-membership exclusion, not a deletion (measured 2026-07-31: the
# variant-coverage gap list is unchanged with them excluded, so nothing
# about coverage was lost by dropping them).
lang/arith_digits.rex
lang/no_trailing_newline.rex
lang/select_when.rex
lang/stem_compound.rex
lang/trace_output.rex
num/comparison.rex
num/notation_thresholds.rex

# New for Task 14a -- executor control flow, stems, EXIT, TRACE, comparison:
lang/do_loop_forms.rex
lang/do_label.rex
lang/leave_nested_outer.rex
lang/iterate_from_select.rex
lang/if_else_chain.rex
lang/select_when_bodies.rex
lang/select_when_absorption.rex
lang/leave_iterate_variants.rex
lang/drop_stem_tail.rex
lang/stem_aliasing.rex
lang/exit_with_value.rex
lang/exit_no_value.rex
lang/number_identity.rex
lang/comparison_families.rex
lang/deep_nested_expr.rex
lang/trace_results.rex

# New for Task 14b -- closing criterion 1's variant-coverage gap (nineteen
# InstructionKind/ExprKind/LoopKind/PrefixOp/EndStyle/Trace/Operator variants
# unconstructed by any program above; see criterion-1-coverage-gap.md):
lang/prefix_dotvar_logical_over_label.rex
lang/comparison_operators_remaining.rex
lang/trace_numeric_request.rex

# New for Task 16 -- mutation-control witnesses for a real, previously
# uncaught defect class: a Controlled loop's control variable is the one
# construct in 4a whose first render can genuinely happen after a later
# NUMERIC DIGITS/FORM change, because bind_control does not eagerly render
# (unlike an assignment's traced value). See each file's own comment.
lang/mutation_digits_at_render.rex
lang/mutation_form_at_render.rex
lang/mutation_controlled_order.rex

# New for 4b Task 2, and 4a's by content rather than by date -- an authorised
# plan amendment. The program uses nothing outside this file's own header (25
# plain DO blocks around a failing clause, no CALL and no INTERPRET); it lands
# here because the rule it pins is one 4a should always have had. The 40-column
# saturation of the error report's clause echo was a divergence 4a shipped --
# the oracle printed 40 at depth 25 and the interpreter printed 50 -- and no
# program in this subset nested deeply enough to see it.
lang/deep_nesting_indent_cap.rex
