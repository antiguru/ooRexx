# Phase 7 — streams and platform, implementation plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development to
> implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Build the stream model, `.File`, the standard streams and monitors, commands and
`ADDRESS`, the platform layer, external routine resolution and the `Sys*` subset, so that a Rexx
program on this crate can read and write files, issue commands, call another program, and so that
the ooTest suite can start.

**Architecture:** The interpreter grows two pieces of state it never had -- a shadow environment and
a shadow current directory -- and one new object shape: a stream's native state riding in
`Body::Instance`'s `native` slot. Everything else is filling in entry points that already exist:
the 24 `file_*` and 24 `stream_*` rows of the `LIBRARY REXX` registry, the excluded stream
builtins, the ten `.local` names, and the last arm of the call resolver.

**Tech Stack:** Rust; `rexx-exec` (`dispatch/native.rs`, `run.rs`, `lib.rs`, `input.rs`,
`invocation.rs`, `require.rs`, `environment.rs`, `trace.rs`), `rexx-core` (`body.rs`), `rexx-lib`
(the embedded `StreamClasses.orx`), plus two new dependencies (`rustix`, `filetime`).

**Spec:** `docs/superpowers/specs/2026-09-11-phase-7-streams-platform.md`

**The surveys are part of the spec.** Six measured reports live in
`.superpowers/sdd/2026-09-11-phase-7/survey/` (`A-streams.md`, `B-file.md`, `C-monitors.md`,
`D-commands-env.md`, `E-external-resolution.md`, `F-rexxutil.md`) and are copied to
`docs/superpowers/records/2026-09-12-phase-7/` at the close. Each task below cites the section of
the survey that holds the measured oracle transcripts it must reproduce. **A task's brief carries
those citations rather than a re-transcription of the bytes**: the report is the measurement, and
restating a measured transcript in a brief is how it gets corrupted.

## Global Constraints

* **No `unsafe`.** The workspace lint is `deny`; a per-site exception is Moritz's alone to grant and
  has not been granted for this phase. Where a syscall is needed, use the safe wrapper the spec
  names (`rustix`) or a `/proc` read.
* **No process-global state is mutated** (spec A2): no `std::env::set_var` / `remove_var`, no
  `std::env::set_current_dir`, and no write to file descriptor 0, 1 or 2 from interpreter code. The
  shadow environment and shadow directory are the only ones Phase 7 code reads after `run_program`
  starts. Task 2 lands the test that asserts the crate's sources name none of those functions.
* **No new dependency** beyond the two the spec names.
* **Correctness is byte-identical stdout, stderr and exit status against the C++ oracle**, read on
  **three separate descriptors, never `2>&1`**. Oracle runs use the standard wrapper
  `( ulimit -v 1048576; LD_LIBRARY_PATH=/home/moritz/dev/repos/ooRexx/build/lib /home/moritz/dev/repos/ooRexx/build/bin/rexx FILE )`
  from a fresh empty directory.
* **Probe hazards.** Read `rust/corpus/oracle-crashes.txt` before an unusual probe and never run its
  entries -- entries 10, 11 and 12 are this phase's own. Never
  `select; when 1 = 0 then; when 2 = 2 then nop; end`. Never `NUMERIC DIGITS` above 1000. Never
  `.Package~new` on a file inside the repository. A symbol `x` or `b` directly followed by a quote
  is a hex or binary literal. Put `timeout` on both sides of any probe that loops or reads input.
* **File-system and command probes and witnesses stay inside their own directory.** Commands are
  limited to `echo`, `printf`, `true`, `false`, `sh -c 'exit N'`, `cat`, `ls`, `pwd`, `env`.
* **Four gates, all green at the closing commit:** `cargo fmt --all --check`;
  `cargo clippy -j 4 --workspace --all-targets -- -D warnings`;
  `cargo test -j 4 --release --workspace --no-fail-fast -- --test-threads=8`;
  `REXX_CORPUS_GATE=1 cargo test -j 4 --workspace --no-fail-fast -- --test-threads=8`. Record each
  exit status unpiped and the count of `ok` result lines. **Commit before a long gate run and leave
  the tree frozen until it finishes.** Wait on a running gate in ~1-hour stretches, not 10-minute
  polls.
* **Comments are minimal** (`rust/CLAUDE.md`, Moritz 2026-08-27): a one-sentence overview,
  parameters/returns/panics, and properties the implementation does not show. No narrative, no
  history, no counts of in-repo sets. Measurements and reasoning go in the spec, the survey or a
  record.
* **Every extent claim is derived and its command committed.** No "all", "none", "every" or a count
  in prose without the enumeration that produced it.
* **Write each negative control's prediction before running it**, and mark each part confirmed,
  falsified, or unobservable.
* **A method that exists and does nothing is not implemented.** Build the body or leave the name
  refusing loudly; never a shell that answers nothing.
* **A refusal names the phase that owes the work**: `Phase 8` for native libraries, `Phase 10` for
  RXAPI, the external queues and the RexxUtil remainder. After this phase, no refusal names
  `Phase 7`.
* Never `git add -A`; stage explicit paths. Never amend; never force-push; never
  `git reset --hard`; never `git checkout -- <path>` on an edited file; never bare `git stash` /
  `git stash pop`. Commit messages are written to a file and passed with `-F`.
* Scratch files live in the session scratchpad, never in the repository.

---

## Task order

The phase has one hard prerequisite chain and three independent arms:

```
1 (loud owners)  →  2 (shadows)  →  3 (platform readers)
                          ↓
                          ├── 4 → 5 → 6 → 7 → 8 → 9 → 10 → 11 → 12   the stream arm
                          ├── 13                                      .File
                          ├── 14 → 15 → 16                            commands
                          ├── 17                                      external routines
                          └── 18                                      Sys*
19 (interactive debug) needs 11; 20 (security manager) needs 9, 14 and 17;
21 (rexx-run flush) needs 10; 22 (close-out) needs everything.
```

---

### Task 1: Every unimplemented name refuses loudly, naming the right phase

Three defects with one shape: a name this crate does not implement answers something a program
cannot tell from its own mistake, or blames the wrong phase.

**Files:**
- Modify: `rust/crates/rexx-exec/src/lib.rs` (`Loud::unresolved_call`, around `:273`;
  `Loud::value_selector`)
- Modify: `rust/crates/rexx-inventory/src/lib.rs` (the excluded-builtin table gains an owner per
  row)
- Modify: `rust/crates/rexx-exec/src/run.rs` (the resolver arm at `:3549`)
- Modify: `rust/crates/rexx-exec/src/environment.rs` (`STDQUE`'s owner)
- Create: `rust/crates/rexx-exec/src/internal_routines.rs` (the internal-package table)
- Modify: `rust/crates/rexx-exec/tests/owners.rs` (the assertion)

**Interfaces:**
- Produces: `internal_routines::lookup(name) -> Option<&InternalRoutine>` with a `package`
  (`REXX` or `REXXUTIL`), an `owner` phase string and an optional body; Tasks 3, 13 and 18 fill
  bodies in.
- Consumes: nothing.

- [ ] **Step 1: Give every excluded builtin its owner in the inventory**, from
      `docs/superpowers/plans/phase-4-exclusions.txt`'s own rows: the seven stream builtins plus
      `QUALIFY`, `USERID`, `SETLOCAL`, `ENDLOCAL` are `Phase 7`; `RXQUEUE`, `RXFUNCADD`,
      `RXFUNCDROP`, `RXFUNCQUERY` are `Phase 10`. `Loud::unresolved_call` reads the owner from
      there instead of its hardcoded `"4c"`.
- [ ] **Step 2: Assert it in `tests/owners.rs`** -- every excluded builtin has an owner, and it is
      one of the phase strings the split table already knows. The negative control: change one row
      to `4c` and watch the test fail.
- [ ] **Step 3: Build the internal-routine table.** Rows for `DIRECTORY`, `FILESPEC` and `BEEP`
      (package `REXX`) and for every name RexxUtil registers on Linux (package `REXXUTIL`), derived
      from `interpreter/runtime/RexxUtilCommon.cpp`'s table plus its platform `#include` -- commit
      the command that derives it. Every row starts bodiless with owner `Phase 7` (the nine of
      Task 18) or `Phase 10` (the rest).
- [ ] **Step 4: Consult it in the resolver**, at `run.rs:3549` between the library-bootstrap lookup
      and `routine_not_found`, so an unimplemented row refuses loudly instead of raising 43.1.
      Survey F §4 and survey D §4.3 are the readings this step implements.
- [ ] **Step 5: `.STDQUE` says Phase 10**, not Phase 7 (`environment.rs`'s `ORACLE_LOCAL` owners).
- [ ] **Step 6: Witnesses.** Crate-only tests: each of `DIRECTORY`, `SysFileExists` and one Phase 10
      `Sys*` name refuses loudly with its owner; `call zorkolo` still raises 43.1 rc 213 (the
      adjacent case that pins the change to *known* names).
- [ ] **Step 7: Commit.**

---

### Task 2: The interpreter owns its environment and its current directory

Everything downstream resolves paths and spawns children through these two fields. They exist
because the harnesses run interpreters on threads in one process (spec A2), and because
`std::env::set_var` is `unsafe` in this edition.

**Files:**
- Modify: `rust/crates/rexx-exec/src/lib.rs` (`Interp` gains `env` and `cwd`; `resolve_search` reads
  them, `:2965`)
- Modify: `rust/crates/rexx-exec/src/invocation.rs` (`with_environment`, `with_directory`,
  `with_c_args`)
- Modify: `rust/crates/rexx-exec/src/require.rs` (a `SearchContext` instead of process reads)
- Create: `rust/crates/rexx-exec/src/paths.rs` (`qualify(cwd, name)`, the port of
  `SysFileSystem::normalizePathName`, and the shadow-directory accessor)
- Create: `rust/crates/rexx-exec/tests/no_process_state.rs`

**Interfaces:**
- Produces: `Interp::env_get(name)`, `Interp::env_set(name, value)`, `Interp::cwd_text()`,
  `Interp::set_cwd(path)`, and `paths::normalize(path, cwd) -> String`. Nothing else may join a
  relative path itself.
  **Corrected 2026-09-13, at Task 14.** This block named `shadow_env`, `set_shadow_env`, `cwd`,
  `resolve_path` and `paths::qualify`, and not one of those exists -- Task 2 built the names above
  instead. `resolve_path`, which this block made mandatory for every later task, was never written
  at all; `paths::normalize` is what the consumers call, and `paths.rs`'s own module doc already
  lists a command's working directory among them.
- Consumes: nothing.

- [ ] **Step 1: Write the failing test** -- `paths::qualify` against a table of the cases survey B
      §3 measured (relative, absolute, trailing separator, `..` collapsed, `..` at the root, empty,
      duplicate separators, a component that merely starts with `..`), as a `datadriven` file.
- [ ] **Step 2: Port `normalizePathName`** (`interpreter/platform/unix/SysFileSystem.cpp:687`)
      byte for byte -- lexical only. `fs::canonicalize` and `Path::components` are both wrong here
      and the test says why.
- [ ] **Step 3: Add the two fields**, initialised from `std::env::vars_os()` and
      `std::env::current_dir()` at `Interp::new` unless the `Invocation` supplies them.
- [ ] **Step 4: Move `resolve_search` onto them** through `SearchContext`, keeping its behaviour
      byte-identical (the `::REQUIRES` corpus programs are the regression witness).
- [ ] **Step 5: The no-process-state test** -- scan `crates/*/src/**.rs` for `set_var`,
      `remove_var` and `set_current_dir` and fail on a hit outside a documented allowance. Negative
      control: add one and watch it go red.
- [ ] **Step 6: Commit.**

---

### Task 3: The platform readers — `VALUE`'s selectors, `DIRECTORY`, `QUALIFY`, `USERID`, `SETLOCAL`/`ENDLOCAL`

**Files:**
- Modify: `rust/crates/rexx-exec/src/builtin/datatype.rs` (`VALUE`'s third argument)
- Modify: `rust/crates/rexx-exec/src/builtin/state.rs` or a new `builtin/platform.rs` (`QUALIFY`,
  `USERID`, `SETLOCAL`, `ENDLOCAL`)
- Modify: `rust/crates/rexx-exec/src/internal_routines.rs` (`DIRECTORY`'s body)
- Create: `rust/corpus/lang/value_environment.rex`, `value_environment_selector.rex`,
  `directory_shadow.rex`, `qualify_forms.rex`, `platform_arities.rex`, `setlocal_endlocal.rex`
- Modify: `rust/corpus/phase-7.txt`

**Interfaces:**
- Consumes: Task 2's shadows and `paths::qualify`.
- Produces: the `SETLOCAL` snapshot stack on the top-level activation, which Task 17's external
  routines restore at their return.

- [ ] **Step 1: `VALUE(name, new, 'ENVIRONMENT')`** on the shadow: the old value is answered, a
      value truncates at the first NUL, `.nil` removes, a name that is empty or holds `=` is
      silently not stored, the selector is caseless, an unknown selector is 40.914, a non-string
      value is 88.909. The `''` selector reads and writes `.environment` (Phase 5's, and cheap).
      Survey D §2.7 has every case.
- [ ] **Step 2: `DIRECTORY`** as an internal-package routine (Task 1's table): qualify the argument,
      require a directory, store the canonicalised path, answer it; a failure answers `""` and
      changes nothing; no argument answers the stored path. Arity errors are 88.922. Survey D §2.8.
- [ ] **Step 3: `QUALIFY`** = `paths::qualify` against the shadow directory; `USERID` from
      `/proc/self/status` plus `/etc/passwd`, falling back to the shadow `USER`/`LOGNAME`.
- [ ] **Step 4: `SETLOCAL`/`ENDLOCAL`** push and pop `(directory, environment)` on the top-level
      activation; a restore re-puts the saved names and removes nothing added since; an empty stack
      answers 0. **Do not run the oracle on an `ENDLOCAL` past the stack or on a second restore** --
      `oracle-crashes.txt` entry 10, and this crate's answers there are the spec's licensed
      divergences.
- [ ] **Step 5: Witnesses** as named above, each also carrying its adjacent case (an unset name
      reads `''` and not its own name; `directory()` twice is stable; the first unpaired
      `endlocal()` before any `setlocal()` is 0).
- [ ] **Step 6: Commit.**

---

### Task 4: A `Stream` object with state, and the queries that need no I/O

**Files:**
- Modify: `rust/crates/rexx-core/src/body.rs` (`native: Option<Box<NativeState>>`, the `Buffer` and
  `Stream` arms, `StreamState`)
- Modify: `rust/crates/rexx-exec/src/value.rs` (`buffer`/`buffer_mut` gain the arm; `stream`/
  `stream_mut` join them)
- Modify: `rust/crates/rexx-exec/src/dispatch.rs` (the `MutableBuffer` constructor wraps its state)
- Create: `rust/crates/rexx-exec/src/stream.rs` (the state, the kinds, `description`)
- Modify: `rust/crates/rexx-exec/src/dispatch/native.rs` (`stream_init`, `std_set`, `handle_set`
  loud, `stream_state`, `stream_description`, `qualify`, `query_exists`, `query_size`,
  `query_time`, `query_streamtype`, `query_handle`, `stream_close`, `stream_flush`, `stream_uninit`)
- Create: `rust/corpus/lang/stream_closed_queries.rex`, `stream_uninit.rex`

**Interfaces:**
- Produces: `StreamState`, `Interp::stream_mut(ObjRef)`, and the rule that every entry point finding
  `native == None` raises 48.1.
- Consumes: Task 2's `resolve_path`.

- [ ] **Step 1** Widen the `native` slot and keep `MutableBuffer` green (its rows in
      `corpus/method-bodies.txt` are the regression witness).
- [ ] **Step 2** `stream_init` stores the name as given; `~string` answers it; `qualify` answers the
      qualified path without opening. `.Stream~new(.nil)` is 93.938 and a missing argument 93.901.
- [ ] **Step 3** The closed-stream queries: `state` `UNKNOWN`, `description` `UNKNOWN:`,
      `query exists` the qualified path or `""` (a directory answers `""`), `query size` from
      `stat`, `query datetime`/`timestamp` from the mtime (`chrono` for the local rendering),
      `query handle`/`position` `""`, `query streamtype` `UNKNOWN`, `close` on an unopened stream
      `""`, `flush` `READY:`.
- [ ] **Step 4** `stream_uninit` closes and drops the state; every entry then raises 48.1. **Do not
      run the oracle on a post-`uninit` reader** -- `oracle-crashes.txt` entry 12.
- [ ] **Step 5** `handle_set` refuses loudly (reaching an open descriptor needs `unsafe`), and
      `HANDLE:` names are never opened.
- [ ] **Step 6: Witnesses** from survey A §2.1's closed-query block and §2's `p10c` rows; adjacent:
      `~makestring` is 97.1, `~state('x')` is 88.922.
- [ ] **Step 7: Commit.**

---

### Task 5: `OPEN` and `CLOSE`, and the command tokenizer

**Files:**
- Modify: `rust/crates/rexx-exec/src/dispatch/stream.rs` (the open modes and the option table).
  Task 4 put the entry points here rather than at `src/stream.rs`, which this task's text named
  and which does not exist; the option table goes beside the bodies that read it.
- Modify: `rust/crates/rexx-exec/src/dispatch/native.rs` (`stream_open`, `stream_close`)
- Create: `rust/corpus/lang/stream_open_modes.rex`, `stream_open_errors.rex`

**Interfaces:**
- Consumes: Task 4.
- Produces: the open state every later stream task reads.

- [ ] **Step 1** Port `StreamCommandParser`'s tokenizer: runs of non-blanks, with `=`, `+`, `-`,
      `<` as single-character tokens that also end the run before them; first table entry whose
      leading bytes match caselessly, then the per-option minimum length. Survey A §3.8 and the
      abbreviation table in §2.2 are the specification; a `datadriven` table is the test.
- [ ] **Step 2** The modes: READ, WRITE, BOTH (default), APPEND (default), REPLACE, SHARED,
      SHAREREAD, SHAREWRITE, NOBUFFER, BINARY, RECLENGTH n. Conflicting, repeated, or
      RECLENGTH-without-BINARY is a bare 93. A `write`/`both` open creates the file; `read` on a
      missing file is `ERROR:2` and leaves the stream `UNKNOWN`.
- [ ] **Step 3** A second `open` on an open stream closes and reopens.
- **Hazard:** never send `~copy` to a `Stream` against the oracle -- it aborts at termination,
  rc 134, after the program has printed everything (`corpus/oracle-crashes.txt` entry 13). The
  state block stays `Clone` so this crate's `COPY` answers an independent unopened stream; that is
  a licensed divergence, not a shape to match.
- [ ] **Step 4: Witnesses** from survey A §2.2, minus the `HANDLE:` rows; adjacent: every 93 row.
- [ ] **Step 5: Commit.**

---

### Task 6: Characters and lines — reading, writing, positions, and the `LINES` cache

**Files:**
- Modify: `rust/crates/rexx-exec/src/dispatch/stream.rs`
- Modify: `rust/crates/rexx-exec/src/dispatch/native.rs` (`stream_charin`, `stream_charout`,
  `stream_linein`, `stream_lineout`, `stream_chars`, `stream_lines`)
- Create: `rust/corpus/lang/stream_chars_lines.rex`, `stream_line_ends.rex`,
  `stream_write_positions.rex`

**Interfaces:**
- Consumes: Tasks 4 and 5.
- Produces: the read-ahead buffer and the position rules Tasks 8 and 9 build on.

- [ ] **Step 1** Reading: a 4 KiB read-ahead with one unget byte; `gets` drops a `\n` and the `\r`
      immediately before it and nothing else; a final unterminated line is a line; a short read is
      EOF.
- [ ] **Step 2** Writing: write-through, no local buffer. `LINEOUT` writes `\n`; a write past the
      end leaves the OS to zero-fill; a file ending in `0x1A` has it overwritten by the next
      `LINEOUT`.
- [ ] **Step 3** The four positions and the two line-character positions, `resetLinePositions` on
      every `charin`/`charout`, and the read/write switch re-seeking the descriptor.
- [ ] **Step 4** EOF: `LINEIN` answers `""` with `NOTREADY:EOF`; `CHARIN` answers `""` with
      `ERROR:0`. Both are measured, and they differ.
- [ ] **Step 5** Port the `stream_line_size` cache **including its off-by-one** (survey A §2.6,
      §5.7): the second `LINES('C')` after a `CHARIN` answers one short on the oracle, and a port
      that recounts diverges. Say so at the site in one sentence.
- [ ] **Step 6: Witnesses** from survey A §2.4 and §2.5; adjacent: `lineout(f,'')` leaves a
      one-byte file where `charout(f,'')` leaves an empty one; `lines()` on a missing file is 0 with
      `ERROR:2` and creates nothing.
- [ ] **Step 7: Commit.**

---

### Task 7: `NOTREADY`

**Files:**
- Modify: `rust/crates/rexx-exec/src/run.rs` (raising a named non-syntax condition)
- Modify: `rust/crates/rexx-exec/src/dispatch/stream.rs` (raise points)
- Create: `rust/corpus/lang/stream_notready_trap.rex`, `stream_notready_signal.rex`

**Interfaces:**
- Produces: the raise path Task 11's input routes and Task 15's `ERROR`/`FAILURE` both reuse.
- Consumes: Task 6.

- [ ] **Step 1** Raise with the stream name **as given** as `description`. Measured 2026-09-12, a
      `SIGNAL ON NOTREADY` handler at EOF reads `C=NOTREADY D=n.txt I=SIGNAL S=OFF sigl=<line>`,
      and every one of those letters is already answered here.
      **`additional` and `result` are NOT stored, and no condition directory is built.** The
      original text had `PendingTrap` gain both, rooted. They are observable only through
      `CONDITION('A')`/`('O')`, which refuse loudly in this crate (`builtin/state.rs:316`) and are
      a committed `diverges` row in `refusal-sites.tsv`. Two fields nothing can read are a hollow
      shell by the phase's own constraint, and they would need `Interp::object_roots`
      (`pending_traps`) and `Activation::object_roots` (`condition`) extended -- both ignore those
      fields today, with no justification comment, because neither holds an `ObjRef`. The task that
      implements condition objects adds the fields and both root walks together.
- [ ] **Step 2** `SIGNAL ON` unwinds the clause; `CALL ON` queues and delivers at the clause
      boundary; untrapped changes nothing but the state. **Never queue the same condition twice in
      one clause against the oracle** -- `oracle-crashes.txt` entry 5.
- [ ] **Step 3** `::OPTIONS NOTREADY SYNTAX` through the existing `escalated` gate.
- [ ] **Step 4: Witnesses** from survey A §2.8 and survey C §2.6; adjacent: `LINES`/`CHARS` at EOF
      raise nothing, **and the same two on a name nothing resolves to raise once each** -- measured
      2026-09-12, a `CALL ON NOTREADY` handler fires for `lines` and again for `chars`, both with
      description `nosuch.txt`, and the state ends `ERROR:2`. The split is by cause, not by entry
      point: every one of the six that can implicitly open (`chars`, `lines`, `charin`, `charout`,
      `linein`, `lineout`) raises when the open fails, and only the reads raise at end of file.
- [ ] **Step 5: Commit.**

---

### Task 8: `SEEK`, `POSITION` and `QUERY POSITION`

**Files:**
- Modify: `rust/crates/rexx-exec/src/dispatch/stream.rs`, `dispatch/native.rs` (`stream_position`,
  `stream_query_position`)
- Create: `rust/corpus/lang/stream_seek.rex`, `stream_seek_line.rex`, `stream_seek_errors.rex`

**Interfaces:** consumes Tasks 5 and 6.

- [x] **Step 1** The `=`, `<`, `+`, `-` prefixes; no range check anywhere (a `<99` on a 29-byte file
      answers `-69`); a READ seek clears the count cache; a seek resets the state to `READY`.
- [x] **Step 2** `READ`/`WRITE`/`CHAR`/`LINE` and the "neither given" rule: both positions move when
      the stream is open BOTH. LINE positioning scans and clamps to the last line plus one.
- [x] **Step 3** `QUERY POSITION` defaults to WRITE only for a write-only stream, `SYS` answers the
      OS offset, and `SYS` is a QUERY-only token that `SEEK` rejects.
- [x] **Step 4** A transient stream is 93.958 -- after the options are parsed, not before.
- [x] **Step 5: Witnesses** from survey A §2.3; adjacent: `seek write` with no offset
      is 93.903 (one substitution, the word `SEEK`, not two). A third witness
      `stream_seek_errors.rex` carries the mutual-exclusion refusals, which render as a bare
      `Error 93` with no subcode line. Do **not** pin `QUERY POSITION SYS` after a line
      recompute: it reports wherever the oracle's own `countLines` buffering left the
      descriptor, not a behaviour this crate can reproduce.
- [x] **Step 5b** The carried Task 6 defect: `lineout(data, line)` writes at the line *number*
      as though it were a character position, corrupting data. Route it through the same
      line-to-character walk `SEEK ... LINE` uses. Measured: the bytes land at the line's
      character offset and overwrite in place, while the write pointer ends at end-of-file
      plus the bytes written -- the two disagree, and that is upstream's own inconsistency
      (`StreamInfo::lineout` runs `writeSetup()` before `setLineWritePosition`). The survey's
      figure of 19 is wrong; it is 22 on a 17-byte file. Needs its own witness.
- [x] **Step 6: Commit.**

---

### Task 9: The stream table and the stream builtins

**Files:**
- Modify: `rust/crates/rexx-exec/src/run.rs` (the builtins become sends), `activation.rs` (the
  table), `builtin.rs` (the stream names leave the excluded set)
- Modify: `rust/crates/rexx-inventory/src/lib.rs` (the excluded set shrinks)
- Modify: `docs/superpowers/plans/phase-4-exclusions.txt` -- delete the seven stream rows and
  record them as delivered, the way QUALIFY/USERID/SETLOCAL/ENDLOCAL already are. **This is
  the file the probe-set derivation reads**, so it must move in the same commit: while the
  names are still listed the harness demands no probe for them, and the moment they leave
  it demands all seven.
- Modify: `rust/corpus/builtin-probes.txt` -- **the seven stream names have no rows there at
  all**, because that file carries one probe per name that is *not* a whole exclusion. The
  status table is derived by running a probe through both interpreters, so seven probe
  programs must be written before `builtin-status.txt` can move off `excluded`. Each is a
  complete single-line Rexx program, tab-separated from the name, and must create whatever
  file it reads. A row that runs but disagrees derives as `divergent`, which the harness
  refuses without a `KNOWN GAP: <NAME>` row in the exclusions file.
  The membership is derived from the exclusions file and asserted both ways, so the seven
  rows become **required** the moment the names leave it -- a missing row is as red as a
  spurious one. Two rules bind each program: it must elicit a *value* the builtin had to
  compute (an error-message probe is satisfied by an implementation that computes
  nothing), and it must not nest one builtin inside another (a draft using `c2x(...)` made
  unrelated builtins report the wrong name). For the readers that means the probe has to
  create its own file without leaning on a second stream builtin -- resolve that before
  writing the rows, not at gate time.
- Modify: `rust/corpus/builtin-status.txt` -- all seven stream names are pinned `excluded`
  there and the file is derived, not hand-written: `tests/builtin_status.rs` runs each
  name's `builtin-probes.txt` program through both interpreters and asserts the table in
  both directions. Implementing them flips seven rows; refresh by running
  `cargo test --offline -p rexx-exec --test builtin_status` and copying the measured table
  the failure prints. `tests/method_bodies.rs` reads the same file.
- Modify: `rust/corpus/refusal-sites.tsv` -- the two new 40.x constructors (40.27, 40.904)
  shift every definition line below them in `error.rs`, as Task 8's two did; re-derive from
  the `refusal_sites` failure output rather than editing rows by hand.
- Modify: `rust/corpus/bif-exempt.txt` -- **44 of its 49 rows are blocked on Phase 7** and the
  stream builtins are what unblocks them, so rows will start passing; that file is policed in
  both directions, so refresh it with `REXX_BIF_EXEMPT_REFRESH=1 cargo test --release
  -p rexx-exec --test bif_assertions` and commit the diff. `tests/owners.rs` needs **no** edit:
  its loop runs over `EXCLUDED`, so it shrinks with the rows, and `owner_of("LENGTH") == None`
  is already the assertion that an implemented builtin owes nobody.
- Create: `rust/corpus/lang/stream_builtins.rex`, `stream_table_sharing.rex`,
  `stream_close_at_exit.rex`

**Interfaces:**
- Produces: `Interp::resolve_stream(name, added) -> ObjRef`, which Task 10 routes to the standard
  streams and Task 20 hooks the security manager into.
- Consumes: Tasks 4-8.

- [x] **Step 1** `resolve_stream`: an omitted or empty name and `STDIN`/`STDOUT`/`STDERR` (with or
      without a colon, caselessly) route to the standard streams; anything else is qualified and
      looked up in the table by the qualified name, else `NEW` on `Stream`.
- [x] **Step 2** The table lives on a program or method activation and is borrowed by reference by
      internal calls, `PROCEDURE`, `::ROUTINE` and `INTERPRET`; the builtins that the C++ passes
      `&added` insert, `STATE`, `DESCRIPTION` and non-OPEN/CLOSE/SEEK commands do not; `LINEOUT(name)`
      alone, a `CLOSE`, and a failed `OPEN` remove.
- [x] **Step 3** Each builtin sends the message named after it with exactly the arguments given.
      `LINES()` defaults to `Normal` where the method defaults to `Count`.
- [ ] **Step 4** On a program or method activation's exit, send `CLOSE` to every entry.
      **Left undone deliberately: no observable was found.** This crate writes through
      with no local buffer, so the flush the sweep exists to cause has already happened.
      Three probes were byte-identical on both sides -- STATE/CHARS on an open unclosed
      stream, a second program reading an unclosed file, and `p09b`'s rc-214 error path.
      Building it now would add machinery no witness can tell from its absence. The
      likely places an observable turns up are Task 11's monitors and a subclass with
      its own `uninit`.
- [x] **Step 5** `STREAM(name, op, command)`: `S`/`D`/`C`, first letter only, 40.27 on an empty
      name, 40.904 on a bad operation.
- [x] **Step 6: Witnesses** from survey A §2 `p12`, `p12b`, `p09b`, `p09c` and §2.11; adjacent: a
      method call does not advance the caller's stream position where an internal routine does.
- [x] **Step 7: Commit.**

---

### Task 10: `.STDIN`, `.STDOUT`, `.STDERR`, and the rest of `.local`

**Files:**
- Modify: `rust/crates/rexx-exec/src/environment.rs` (build the ten entries instead of refusing)
- Modify: `rust/crates/rexx-exec/src/dispatch/stream.rs` (the `Std` kind), `input.rs` (byte reads,
  `has_data`)
- Modify: `rust/crates/rexx-exec/src/invocation.rs` (`.SYSCARGS` from the argv words)
- Create: `rust/corpus/lang/local_std_objects.rex`, `std_stream_writes.rex`

**Interfaces:**
- Produces: the six standard objects Task 11 routes through, and `Input::read_bytes`/`has_data`.
- Consumes: Tasks 4 and 9.

- [x] **Step 1** `.STDIN`/`.STDOUT`/`.STDERR` are `Stream` instances marked by
      `StreamState::standard` -- **survey A 5.1's `kind: Kind` with a `Std` variant was not
      what got built**; the implemented state is `open: Option<OpenFile>` plus
      `mode: OpenMode` plus `standard: Option<StandardStream>`, and `standard` has exactly
      two readers today (`std_set` writes it, `open` short-circuits on it). This task is
      where it starts meaning something. Writes append to `Interp::write_out` (the same
      `out` buffer `SAY` uses, which is what makes survey C 2.3's byte order hold) and to
      the `trace` sink for `.STDERR`; reads take from `ProgramInput`.
      **A program-built `.Stream~new('STDOUT')` does *not* share that buffer**, and this step's
      earlier text saying it does is wrong. Measured, same program, `SAY 'AAAA'` then a built
      stream's two `LINEOUT`s interleaved with more `SAY`s: to a pipe the descriptor carries
      `AAAA BBBB CCCC BUILT-1 BUILT-2`, so the built stream's writes are buffered and flushed at
      exit rather than interleaved; to a regular file it carries `BBBB CCCC BUILT-1 BUILT-2`, with
      `AAAA` overwritten, because the built stream writes from its own position at offset 0. No
      file named `STDOUT` is created either way -- it holds the real descriptor.
      **Ruling: every `standard`-marked stream routes to the sink anyway, and the ordering
      difference for a program-built one is recorded rather than reproduced.** The alternative --
      testing for the bootstrap objects specifically, which `StreamState::standard` cannot do
      since `!std_set` sets it from the *name* -- sends a built stream down `ensure_open`, which
      creates a real file named `STDOUT` in the working directory. Stray files in a program's own
      directory are a worse failure than interleaving, and corpus programs run in shared-shape
      directories under A6. Cost if wrong: a program writing through a built standard-named
      stream sees its bytes interleaved with `SAY` rather than flushed at exit, and on a
      descriptor redirected to a regular file it keeps output the oracle overwrites.
- [x] **Step 2** The five monitors are built as `LocalServer~initInstance` builds them, each
      stacking the next, with the oracle's object names; `.STDQUE` stays unbuilt naming Phase 10.
- [x] **Step 3** `.SYSCARGS` is an `Array` of the separate command-line words. **It is never
      absent**, which this step used to claim: measured, `rexx p.rex` with no arguments still puts
      it in `.local` (`hasentry` answers `1`) as an `Array` of `0` items, and `1 "2 3" 4` makes it
      an `Array` of `1`, `2 3`, `4`. `.local~allindexes` lists it either way. **`Invocation` cannot supply them today**: it stores one joined
      `argument: Option<Vec<u8>>` and `join_command_line` collapses the words into it, so
      the separate words are destroyed at construction. Survey C 2.2 measures
      `1 "2 3" 4` as `Array 3` with items `1`, `2 3`, `4`, which that string cannot be
      split back into. `Invocation` has to retain the word list alongside the joined
      argument, and `rexx-run`'s own entry point has to pass it.
- [x] **Step 4** Positioning a standard stream is 93.958; closing one is `READY:` and later writes
      continue.
      **`query streamtype` and 93.958 both turn on whether the standard descriptor is transient,
      and that fact belongs to the embedding rather than to the host process.** Measured, the
      oracle reports what its descriptor *is*: the same program answers `PERSISTENT` with fd 1 on a
      regular file and `TRANSIENT` on a pipe or `/dev/null`, and the same positioned `CHAROUT`
      answers `0` in the first case and raises 93.958 in the second.
      **Ruling: carry it on `Invocation`, not by reading `/proc/self/fd/<n>` from the
      interpreter.** `Invocation::with_standard_transient([bool; 3])` -- input, output, error --
      reaches `Interp` through `into_parts` like `input` and `directory` do, and defaults to all
      transient, which is what a terminal is and what this step was already willing to answer
      everywhere. `rexx-run` fills it from `/proc/self/fd`, the one place that reading is
      legitimate because that process owns the descriptors.
      **Why the direct read had to go.** It reproduced the oracle under `rexx-run` and made the
      *same program* answer differently under `cargo test`: the interpreter never writes to fd 1,
      it appends to `Interp.out` and the embedder decides what becomes of it, so the host's own
      descriptors say nothing about the embedding's. Measured, it also produced a **stable**
      disagreement between `bif_assertions.rs` and a byte-identical copy of it -- one classifying
      `CHAROUT::test_stdout_start#1` as `RAISE-MISMATCH`, the other as `Pass`, three runs each.
      That disagreement was never explained; it disappeared with the process-global read, which is
      consistent with a cause in it that was not isolated. Do not reintroduce the direct read to
      chase it. With the field in place: `186 of 186` raise rows pass and both `CHAROUT` rows leave
      `bif-exempt.txt`.
      **`query handle` is `0`, `1`, `2` by `StandardStream`**, stable under redirection; today
      `query_handle` ignores its state and always answers the null string.
      **A read sent to `.STDOUT` rewinds the shared descriptor, and this crate does not.**
      Measured: a program that says two thirty-byte lines, sends `.stdout~charin`, then says a
      third leaves 62 bytes on the oracle with the third line overwriting the first, against 94
      bytes here with the transcript intact. `~chars` and `~lines` do not do it; only the reads
      (`~charin`, `~linein`) do. **Ruling: record it, do not reproduce it.** Matching would mean
      giving a standard stream a real descriptor position -- the `OpenFile` coupling the Step 1
      design avoids precisely so that no reader or writer changes shape -- and the behaviour it
      buys is a program destroying its own output. Cost if wrong: a program that reads from an
      output stream and then writes diverges, silently and in its favour. Revisit only if a real
      program is found doing it.
      **Closing one loses the bytes, and the object does not say so.** `~close` answers `READY:` and
      leaves `UNKNOWN`; a later `lineout` answers `0` and restores `READY`/`READY:`. But the
      descriptor is genuinely closed, so those bytes never appear -- measured, a program that closes
      `.stdout` and then writes prints nothing further and still exits 0. A probe reporting on
      stdout therefore cannot see its own subject; report into a file opened before the close.
- [x] **Step 5: Witnesses** survey C §2.2 (identities, names) and §2.3 (byte order); adjacent:
      `.local~stdout = .stderr` moves `.stdout~lineout` but not `SAY`.
      **The `.local~allindexes` half cannot be witnessed yet, and neither can `==` between two
      arrays.** Measured: `ALLINDEXES`, `HASENTRY`, `ENTRY` and `ITEMS` on a `Directory` all refuse
      as Phase 5, and `==` applied to an `Array` refuses as Phase 5 too -- that last one is
      recorded in no exclusions file and no `unfiled.txt` row, and is noted here because a witness
      author will reach for it. `==` between *instances* works, which is how the stream and monitor
      identities are asserted; `.SYSCARGS`'s identity is shown by mutation visibility instead.
- [ ] **Step 6: Commit.**

---

### Task 11: Every route goes through its monitor, and `SAY` keeps its fast path

**Files:**
- Modify: `rust/crates/rexx-exec/src/run.rs` (`say_evaluated`, `pull_line`, `linein_line`),
  `trace.rs` (the trace and report writers), `environment.rs` (a generation counter on `.local`)
- Create: `rust/corpus/lang/output_redirection.rex`, `input_redirection.rex`,
  `trace_redirection.rex`
- Modify: `rust/crates/rexx-exec/tests/` (a `rexxcps` instruction-count check, see Step 4)

**Interfaces:**
- Consumes: Task 10.
- Produces: `Interp::output_route()` and its trace and input siblings, which Task 19's debug pause
  and Task 15's `+++` line both use.

- [ ] **Step 1** `SAY` sends `SAY` with one string to `.local`'s `OUTPUT`; `LINEOUT()`/`CHAROUT()`
      with the name omitted send with exactly the arguments given and answer the reply;
      `LINEOUT('STDERR', ...)` goes to `.ERROR`. A missing entry or `.nil` writes to stdout
      directly; any other object gets the message. Survey C §2.1 is the message table.
- [ ] **Step 2** Every trace line and every line of the error report is one `LINEOUT` of a
      `TraceObject` to `.TRACEOUTPUT`.
- [ ] **Step 3** Reads send `LINEIN`/`CHARIN` with no arguments to `.INPUT`, `LINES` sends
      `LINES('NORMAL')`, and the queue is consulted first; end of input raises `NOTREADY` (Task 7),
      `PULL` included.
- [ ] **Step 4** The fast path: re-derive the route per `SAY` -- the `.local` slot behind a
      generation counter, the bootstrap monitor by identity, its destination queue's head against
      the bootstrap `.STDOUT` -- and write directly only when all of it holds.
      **`rexxcps` and `emptyloop` cannot witness this, which survey C §7 left open and this task
      measured.** `emptyloop` is `do i = 1 to 25000000; nop; end` with one `SAY` after the loop;
      `rexxcps`'s in-loop `say`s are `if ... then say 'FailedN'` guards that a passing run never
      takes -- measured, a real run prints five banner lines, no `Failed`, at 10,190,112 clauses per
      second. So neither axis executes a `SAY` inside its timed section, and both would read ~0%
      whether or not the fast path exists: the 1% floor would be satisfiable and worthless.
      **Ruling: build Steps 1-3 without the fast path, measure, and let the measurement decide.**
      Add a `sayloop` axis that puts `SAY` in a loop with output to `/dev/null`, take the same
      interleaved instruction counts on it and on `rexxcps` (which stays, per the standing rule that
      every benchmark includes it), and build the counter only if that axis shows a regression worth
      it. A generation counter guarding writers that mostly refuse -- `Directory~setentry` and
      `~remove` are Phase 5 -- is machinery no witness could distinguish from its absence.
- [ ] **Step 5: Witnesses** survey C §2.4 and §2.5, minus the shapes that crash the oracle
      (`oracle-crashes.txt` entry 11) -- for those the crate's own answer is asserted as a
      self-witness and recorded as licensed; survey §6's T2 recommends 97.1 on `.nil`, as for p03e,
      for `LINEOUT(,)` with no `OUTPUT` entry.
      **Several of §2.5's witnesses are blocked on Phase 5 and cannot be written yet.** Measured
      one construct at a time: `~destination` (push and pop), `~current`, `.local~output =`,
      `.monitor~new(...)~lineout` and `.output~say` all run in this crate; `.local~setentry`,
      `.local~remove` and `.local~entry` refuse as Phase 5. So p03g, p03g2, p03e, p03f and p14 wait
      on those, and survey C §7 already lists `Directory~remove` as a blocker hit repeatedly across
      that survey. Write what is reachable and name the rest here rather than in a report.
- [ ] **Step 6: Commit.**

---

### Task 12: `arrayIn`, `arrayOut`, `makeArray`, `supplier`, `say`

**Files:** `dispatch/native.rs` (`stream_arrayin`), `rust/corpus/lang/stream_array.rex`

- [ ] **Step 1** `stream_arrayin` fills the array in place and lets the EOF raise reach Rexx, which
      the orx `signal on notready` catches -- the Rexx halves already exist and must not be
      rewritten.
- [ ] **Step 2** `StreamSupplier` over the remaining lines, `next` past the end 93.937, and the
      close-if-it-was-`UNKNOWN` rule.
- [ ] **Step 3: Witnesses** survey A §2 `p10` lines 13-24; adjacent: `~arrayin('q')` is 93.915.
- [ ] **Step 4: Commit.**

---

### Task 13: `.File`

**Files:** `dispatch/native.rs` (the 24 `file_*` rows), a new `rust/crates/rexx-exec/src/files.rs`,
`rust/corpus/lang/file_*.rex`, `rust/corpus/docs/class-set.txt`

**Interfaces:** consumes Task 2 (`resolve_path`, `paths::qualify`); independent of the stream arm.

- [ ] **Step 1** `file_qualify` first -- it blocks every instance method, because `init` qualifies
      eagerly.
- [ ] **Step 2** The predicates: `exists`, `isFile`, `isDirectory` from `stat` (following symlinks,
      so a dangling link is false everywhere); `canRead`/`canWrite` through `rustix::fs::access`
      *and* an existence check; `isHidden` is `exists && the qualified path contains "/."`;
      `length` answers `0` for a missing file.
- [ ] **Step 3** `list`/`listFiles` in `read_dir` order, never sorted, never `.`/`..`; `.nil` when
      the receiver is not a directory.
- [ ] **Step 4** Timestamps: getters from `Metadata` re-encoded as the oracle's local-wall-clock
      basetime, `.nil` when missing; setters through `filetime`, preserving the other stamp.
      Check `.DateTime~fullDate` answers before relying on it (survey B §7 flags it).
- [ ] **Step 5** Mutation: `makeDir`, `makeDirs`, `delete` (`access(W_OK)` on the file first, then
      `unlink`; a non-empty directory answers `.false`), `setReadOnly`/`setWritable` (all three
      write bits, and they answer **nothing**), `renameTo` (refuse a same-path and an existing
      target before `rename(2)`).
- [ ] **Step 6** The class methods: `isCaseSensitive`, `listRoots`, `temporaryPath`, `searchPath`.
- [ ] **Step 7** `class-set.txt`'s `File` row gains the construction `.File~new('.')`.
- [ ] **Step 8: Witnesses** survey B §2; adjacent: `say ro~setReadOnly` is 91.999 while the side
      effect still happens, and `.File~new` with no argument is 93.901.
- [ ] **Step 9: Commit.**

---

### Task 14: Commands

**Files:** `rust/crates/rexx-exec/src/run.rs` (the command instruction and `ADDRESS`'s command
arm), a new `rust/crates/rexx-exec/src/command.rs`, `rust/corpus/lang/command_*.rex`

**Interfaces:**
- Produces: `Interp::run_command(environment, command, io) -> CommandOutcome`, which Tasks 16, 19
  and 20 extend.
- Consumes: Task 2.

- [ ] **Step 1** The handler table: `""`, `COMMAND`, `SYSTEM`, `SH` run `/bin/sh -c`; `KSH`, `CSH`,
      `BSH`, `BASH`, `TCSH`, `ZSH` run `/bin/<lowercased> -c`; `PATH` splits the string itself
      (blanks and tabs, double-quoted runs, 400 arguments maximum); anything else is rc 30 and
      `FAILURE`. Names are stored as written; only the lookup upcases.
- [ ] **Step 2** Spawning: `env_clear().envs(shadow).current_dir(shadow)`; stdout and stderr piped
      and drained concurrently, then appended to `Interp.out`/`Interp.trace`; stdin from
      `ProgramInput`. A signal is rc `-signal`; 127 is `FAILURE`; any other non-zero is `ERROR`.
- [ ] **Step 3** `cd`, `set`, `unset` and `export NAME=value` are handled in the interpreter against
      the shadows, and only when the command carries no unquoted `< > | & ;`.
- [ ] **Step 4** `RC` is assigned before any trace; `.RS` is the activation-settings field that
      internal callees inherit and methods and external programs do not.
- [ ] **Step 5: Witnesses** survey D §2.1, §2.2, §2.11 and §2.12, avoiding host-dependent bytes:
      no `ksh`/`zsh`/`csh`, no shell "not found" text (use `sh -c 'exit 127'`), no `userid()`.
      Adjacent: `say 'a'; 'echo b'; say 'c'` prints `a b c` in that order.
- [ ] **Step 6: Commit.**

---

### Task 15: `ERROR`, `FAILURE`, `::OPTIONS ... SYNTAX`, and the `+++` line

**Files:** `run.rs`, `options.rs`, `trace.rs`, `rust/corpus/lang/command_traps.rex`,
`command_options_*_syntax.rex`, `command_trace_<letter>.rex`

- [ ] **Step 1** Build the condition directory the oracle builds (survey D §2.3's measured index
      set), queue it through the trap machinery, rename an untrapped `FAILURE` to `ERROR` and raise
      again.
- [ ] **Step 2** `::OPTIONS ERROR|FAILURE SYNTAX` is checked where the raise happens **and** after
      the conversion; an armed trap disables it.
- [ ] **Step 3** The `+++ "RC(n)"` line is the `>>>` emitter at the same indent, only when the
      clause was traced and the rc is numeric and non-zero; the clause and `>>>` come before the
      child's own output under `C`/`A`/`R`/`I` and after it under `N`/`E`/`F`.
      `trace_oracle.rs`'s `PREFIX_COVERAGE` row for `+++` moves to witnessed.
- [ ] **Step 4: Witnesses** survey D §2.4 and §2.5; adjacent: a zero rc under `trace c` prints the
      clause and `>>>` and no `+++`.
- [ ] **Step 5: Commit.**

---

### Task 16: `ADDRESS ... WITH`

**Files:** `run.rs`, `command.rs`, `activation.rs` (the permanent configuration table),
`rust/corpus/lang/address_with_*.rex`

- [ ] **Step 1** A permanent per-name configuration in the activation settings, inherited by
      callees and not copied back; a per-command context built by evaluating sources and targets at
      issue time.
- [ ] **Step 2** Stem in and out with the REPLACE/APPEND and `stem.0` rules; `USING` a string, a
      stem, an array or an ordered collection; the output line-splitting rules (`\r\n` is one
      terminator, a trailing partial line is a line, NUL is data).
- [ ] **Step 3** `STREAM name` in and out after the stream arm lands; a `RexxQueue` target refuses
      loudly naming Phase 10.
- [ ] **Step 4** The error numbers: 98.924, 98.996, 98.997, 98.922, 98.998, 26.904, 98.999, 98.920,
      98.923.
- [ ] **Step 5: Witnesses** survey D §2.6; adjacent: `with output normal` overrides a permanent
      stem configuration.
- [ ] **Step 6: Commit.**

---

### Task 17: External routines and package loading

**Files:** `run.rs` (the resolver's last arm), `require.rs`, `lib.rs` (`load_requires`,
`new_file_executable`, `package_from_source`), `dispatch/package.rs`,
`rust/crates/rexx-exec/tests/corpus.rs` (the sidecar), `rust/corpus/lang/external_*.rex`

**Interfaces:** consumes what Task 2 actually built -- `Interp::resolve_search`, which reads
`PATH` and `REXX_PATH` through `Interp::shadow_var` and resolves against `Interp.cwd`. There is no
`SearchContext` type: survey E Q4 recommended one and Task 2 delivered the capability without it,
so a step wanting the search to be pure and unit-testable has to extract it first.

- [ ] **Step 1** The three measured infidelities in `require.rs`: `.hid` carries no extension; a
      path entry that exists but is not a regular file abandons that spelling and extension
      (candidates become groups); ASCII lowercase, not Unicode.
- [ ] **Step 2** An external file call reuses the `::ROUTINE` call path with three differences: the
      caller's `ADDRESS` is inherited, the call type is `FUNCTION`/`SUBROUTINE`, and
      `merge_required` runs after a normal return. The file is re-parsed on every call and never
      enters the requires cache.
- [ ] **Step 3** A callee that does not parse raises the callee's own error with the two-frame
      traceback, for the call route and for `::REQUIRES` alike.
- [ ] **Step 4** `Package~new(name)` searches the global context; `newFile` and
      `loadPackage(name, array)` gain the parent-package chain, which lifts both refusals.
- [ ] **Step 5** The harness sidecar: `<name>.env` (`PATH=`, `REXX_PATH=`, `CWD=`) and a
      `<name>.d/` fixture directory, fed to the oracle through `Oracle::run_in` and to the crate
      through the `Invocation`. The rule at `phase-4-exclusions.txt:342` is lifted for `phase-7.txt`
      programs.
- [ ] **Step 6: Witnesses** survey E §2.1-§2.4 as its §6 lists them; adjacent: a near-miss file
      present and the call still 43.1.
- [ ] **Step 7: Commit.**

---

### Task 18: The nine `Sys*` functions

**Files:** `internal_routines.rs` (bodies), `files.rs` (shared with `.File`),
`rust/corpus/lang/sys_*.rex`

- [ ] **Step 1** `SysFileExists`, `SysIsFile`, `SysFileDelete` (the `access(W_OK)` pre-check and its
      constant 13), `SysMkDir` (mode through `DirBuilderExt`), `SysRmDir` -- every path through the
      shadow directory.
- [ ] **Step 2** `SysFileTree`: the options grammar, the stem output format and the result order,
      with the glob matcher hand-ported for `*`, `?`, `[abc]`, `[!abc]`, `[a-z]`.
- [ ] **Step 3** `SysSleep` (bound 2147483, and it blocks only its own thread), `SysVersion` and
      `SysLinVer` through `rustix::system::uname`.
- [ ] **Step 4: Witnesses** survey F §2.2; adjacent: a filename exercising `[a-z]` and one
      exercising `[!...]`, and `SysFileDelete` on a missing file answering 13.
- [ ] **Step 5: Commit.**

---

### Task 19: Interactive `TRACE ?`

**Files:** `trace.rs`, `run.rs`, `rust/corpus/lang/trace_debug_*.rex` with `.stdin` sidecars

- [ ] **Step 1** The per-activation state: debug, pause, skip and suppressed, bypass,
      prompt-issued, source-traced; inherited by internal calls and restored on return.
- [ ] **Step 2** `mode_from_setting` stops dropping `?`; `TRACE ?` toggles and keeps the letter; a
      bare `TRACE` ends debug and sets Normal; `TRACE()` answers `?X`; TRACE instructions are
      ignored in debug and the builtin is not; numeric `TRACE` outside a pause stays 24.901.
- [ ] **Step 3** The pause: the banner once, the prompt once per activation, `""` continues, `=`
      re-executes, anything else runs as an INTERPRET fragment with no tracing, no `RC`, no `.RS`,
      whose SYNTAX error prints the two `+++ Interactive trace.` lines and is swallowed; a fragment
      that changes the flow or sets a trace setting ends the pause.
- [ ] **Step 4** `RXTRACE=ON` arms `?R` on each program's top-level activation, before its own
      `trace off` can run.
- [ ] **Step 5: Witnesses** survey C §2.8's transcripts. **Never `::OPTIONS TRACE ?x` against the
      oracle** -- `oracle-crashes.txt` entry 9.
- [ ] **Step 6: Commit.**

---

### Task 20: The security manager's Phase 7 hooks

**Files:** `dispatch/executable.rs`, `dispatch/package.rs`, `dispatch.rs`, `environment.rs`,
`command.rs`, `run.rs`, `rust/corpus/lang/security_manager_*.rex`

- [ ] **Step 1** Store the manager per executable and reach it through
      `getEffectiveSecurityManager`'s equivalent, inherited by what a `::REQUIRES` loads.
- [ ] **Step 2** One `check(message, entries) -> Option<Directory>`: the send must reach `UNKNOWN`,
      a missing result is 91.999 and a non-logical one 34.903.
- [ ] **Step 3** The hooks: `COMMAND` before the handler, `STREAM` in `resolve_stream` with the
      qualified name, `LOCAL`/`ENVIRONMENT` in `env_seam::admit`, `METHOD` in
      `check_protected_method`, `CALL` and `REQUIRES` at their resolution sites.
      `setSecurityManager` stops refusing.
- [ ] **Step 4: Witnesses** survey D §2.10; the target is that
      `ootest/ooRexx/base/security.manager/SecurityManager.testGroup` runs.
- [ ] **Step 5: Commit.**

---

### Task 21: `rexx-run` shows a prompt before it reads

**Files:** `rust/crates/rexx-exec/src/bin/rexx-run.rs`, `input.rs`, a `rexx-run`-level test

- [ ] **Step 1** The binary installs a pair of sinks that drain `Interp.out` and `Interp.trace` to
      the real descriptors when a read is about to block on the process's stdin; the harness never
      installs them, so no differential changes.
- [ ] **Step 2** The test spawns `rexx-run` with pipes and asserts the prompt arrives before stdin
      is written. No corpus witness can see this.
- [ ] **Step 3: Commit.**

---

### Task 22: Close-out

- [ ] **Step 1** Delete every Phase 7 row from `docs/superpowers/plans/phase-4-exclusions.txt` as
      delivered, and re-home the rest by the spec's rulings (native libraries to Phase 8, the queues
      and RexxUtil's remainder to Phase 10).
- [ ] **Step 2** Re-derive `rust/corpus/method-bodies.txt` and the gate tables; the `File`,
      `Stream` and `StreamSupplier` rows must read `answers`.
- [ ] **Step 3** `gate_tables::CLOSED_PHASES` names `7`. `corpus/keyword-exempt.txt`'s
      `ADDRESS.testGroup` rows are already gone -- Task 16 removed them, and the Phase 7 bullet
      describing them with it, because the cases they exempted now pass; that file's own test
      polices the set in both directions, so check it rather than this sentence.
- [ ] **Step 4** Assert that no refusal in the crate names Phase 7.
- [ ] **Step 5** Run `ootest/testOORexx.rex` against this crate and record how far it gets --
      reported, not gated.
- [ ] **Step 6** Write `docs/superpowers/plans/phase-7-gate.md`: the criteria, the instruments, what
      they cannot see, and the four gate commands with their exit statuses and `ok` counts. Linux
      measured; the other four platforms recorded as CANNOT ASSESS.
- [ ] **Step 7** Copy `.superpowers/sdd/2026-09-12-phase-7/` to
      `docs/superpowers/records/2026-09-12-phase-7/`, minus the review packages.
- [ ] **Step 8: Commit.**
