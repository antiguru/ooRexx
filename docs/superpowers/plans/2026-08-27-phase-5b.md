# Phase 5b — an instance exists

**Goal.** Build the mechanisms `docs/superpowers/specs/2026-08-27-phase-5b-instances.md` assigns to
5b, make every gate-table row whose owning phase is `5b` agree with the oracle, land
`corpus/phase-5b.txt`, and flip `5b` into `CLOSED_PHASES`.

**Where this comes from.** That spec, at `01e024157`, after one adversarial review whose six
high-severity findings were verified independently and folded in. Decisions D57-D69 bind. Where a
task below and the spec disagree, the spec wins and the task is wrong.

**Base.** `01e024157`. Corpus 264 of 264; all five gates green at `c7345f7a9`, whose only descendants
are one documentation commit and the spec's two, and `git diff --stat c7345f7a9..HEAD` touches no file
under `rust/`.

**Delivery order is not free, and it is not "Task 1 then everything".**

* **Task 0 comes first.** Four tasks below say "`corpus/phase-5b.txt` carries ..." and the file does
  not exist; creating it turns four guarded tests red until it is wired everywhere. Whichever task
  landed first would have inherited that.
* **Task 1 unblocks Tasks 2, 3, 5's instance arm, 7 and 9.** It does **not** unblock everything:
  `methodsbyclass.rex` refuses at `Array~new`, not `Object~new`, so Task 8 needs no instance; Task 6's
  witness program runs to completion on the crate today; and Task 4's `FORWARD` half is measurable
  with a class method. Those three may land before Task 1.
* **Task 6 follows Task 5.** Task 6 commits an asserted divergence whose crate side is "the class's
  `UNINIT` never runs". Task 5's third delivery changes that to "runs in the termination sweep" -- the
  divergence moves rather than disappearing -- so a DEVIATION written first pins the wrong bytes and
  Task 5 reddens it. That is the condition Task 6 says must redden, arriving from inside the plan.
* **Tasks 2's and 3's second controls are re-run at Task 10.** Each asserts a mutation that no
  existing gate row can see, which is only readable once the rows that might have seen it are green.
  At the point those tasks finish, `usesem`, `obdes`, `methodsbyclass` and the two `DELEGATE` rows may
  all still be red. Record them at the task as "reddens the new witness; every row that agreed before
  the mutation still agrees after it", and re-run them at Task 10 against the full green table.

Task 9 is an audit that wants the rest landed. Task 10 is the flip.

## Global constraints

`docs/superpowers/records/2026-08-17-phase-5a/` carries the constraints this project runs under and
they bind unchanged. The ones this plan leans on hardest:

* **Differential correctness.** stdout, stderr and exit status byte for byte against the oracle, on
  `REXX_ENGINE=ir` **and** `REXX_ENGINE=tree-walker`. Three descriptors read separately, never
  `2>&1`. Probes from a fresh empty directory with absolute paths. The crate bootstraps on every run,
  so bound crate probes with `timeout -s KILL 20`.
* **A silent wrong answer is the worst defect here** and no gate sees one. **Every task below turns a
  loud refusal into an answer**, which is exactly when one is introduced. Probe past the row.
* **The five gate commands** at the end of every task, `--no-fail-fast`, each status read unpiped,
  and **the command that printed a figure quoted beside the figure**.
* **Never run a program listed in `rust/corpus/oracle-crashes.txt`**, and never construct one of
  those shapes.
* Comments: ASCII only, no em-dashes, no historical framing, and no comment states the size of a set.
  `rust/CLAUDE.md`'s comment rule of 2026-08-27 governs: one sentence of overview, the parameters,
  returns and panics, and properties not clear from the implementation. Prefer no comment to a
  paragraph, and assert a load-bearing property in a test instead of describing it.
* Never `git checkout -- <path>` on a file you have edited; `cp` to the scratchpad and restore from
  the copy. Never `rm` with a star glob. Never a bare `git stash`. Commit with `-F`, name paths
  explicitly, never amend.
* **Prefer deleting to rewriting.** Every false sentence the previous two plans shipped arrived as an
  added justification whose argument was right.
* **One amendment to the inherited constraints**: 5a's say a task that adds a corpus program adds it
  to `rust/corpus/phase-5a.txt`. For this phase that file is `rust/corpus/phase-5b.txt` (D64).
* **A task that makes a gate-table row agree adds that row's probe path to `corpus/phase-5b.txt` in
  the same commit.** Gate-table probes are not corpus programs and live outside every subset file
  (`gate_table_d.rs:30`-`:34`), and a phase file means "agrees with the oracle" -- so a probe moves
  there in the task that makes its row agree, which is also what puts it under the differential and
  the collect-on-every-allocation runs. `phase-5a.txt` carries five such entries with the reason
  written beside each.

**"Agrees" and "reddens" need an invocation this plan must give, because the five gate commands do not
provide one.** A gate-table verdict mismatch is an exit status only under `REXX_CORPUS_GATE` **and**
only for a row whose owning phase is closing or already closed (`gate_tables/mod.rs`'s
`verdict_is_gated`). `CLOSED_PHASES` holds `"5a"` until Task 10, so **every 5b row can be red and all
five gates exit zero.** Every "agrees" and "reddens" below is therefore read under

```
REXX_PHASE_GATE=5b REXX_CORPUS_GATE=1 cargo test --release -p rexx-exec \
    --test gate_table_c --test gate_table_d --no-fail-fast
```

quoted beside the figure like any other command. The 5a plan named its equivalent and this one had
omitted it.

**Three rules this phase adds, each because the spec's review found the defect it prevents.**

1. **Treat every "what is already there" paragraph below as a claim to re-measure, not a premise.**
   Four of them were wrong on the previous plan, all in the direction of making the task look
   smaller. Correct the plan file where you find it wrong -- not your brief and not your report.
2. **A witness must be run against a control that makes it fail.** Gate table D's two `DELEGATE` rows
   are green **today** over a mechanism that is not implemented, measured. Two more would be green
   over a *wrong* implementation rather than a missing one: `usesem.rex`, which defines `EXTRA` with
   no class-level `EXTRA` to lose to, and a `k~at` draft of the replacement `::ATTRIBUTE` probe. The
   two failure modes are different and both are in scope. Each task below names its control;
   **record the control as run**, with its transcript, or the acceptance is not met.
3. **No check may depend on an ordering the oracle does not reproduce** (D61, D68). Two instance
   `UNINIT`s at termination, and `~completed` sampled before `~result`, are both racy on the oracle,
   measured. If a probe needs two finalizers, force each with `drop` then `call gc 'force'`.

**Measured at `538f35310` by the controller before this plan was written**, and reproduced during the
spec's review at `be1cf430b`: every 5b row of gate table C diverges and every one but `obdes` is
loud; `obdes` is `diverge-stdout` with empty stderr on both sides; both 5b rows of gate table D
`agree` over a mechanism that is not implemented.

---

## Task 0: `corpus/phase-5b.txt` and its wiring

**Goal.** The file four later tasks write into exists and is read everywhere a phase subset file is
read.

**Why it is a task.** 5a had the same one and said so in the file's own header: its Task 1 "built the
harness plumbing this file needs ... and this file wired into every harness that reads a phase subset
list, and added no corpus program of its own." 5b's plan had no such task, so whichever of Tasks 2,
3, 5 or 7 landed first would have inherited four red tests and one silent narrowing.

**Build.** `corpus/phase-5b.txt` in `phase-5a.txt`'s shape and format, read by the same `read_subset`,
plus its wiring into every reader:

* `SUBSET_FILES` in `crates/rexx-exec/tests/corpus.rs`, `coverage.rs`, `collect_stress.rs` and
  `ir_dual.rs`. **These four are guarded** against a directory listing of `corpus/phase-*.txt`, so
  each goes red on the commit that creates the file and green when its literal is updated.
* **The unguarded literal in `crates/rexx-exec/tests/trace_oracle.rs`**, whose own doc comment says
  it is "the one call site those four do not cover, and a phase subset file added and forgotten
  *here* would silently keep this check measuring the union as it stood before."
* **`EXPECTED_SUBSET_5B` in `coverage.rs`**, beside `EXPECTED_SUBSET_5A`, with the test that pins the
  file's entries against it. Nothing forces this one to exist either.

Two of the six are silent, which is why they are named individually rather than as "the harnesses".

**Done when** the file exists, all six sites read it, and **the control is recorded as run**: removing
`phase-5b.txt` from any one of the four guarded literals reddens that binary, and removing it from the
`trace_oracle.rs` literal reddens nothing -- which is the property that makes the last one worth
naming.

---

## Task 1: `~new`, `INIT`, and the instance seam

**Goal.** `corpus/gate-tables/concepts/creo.rex` and `abscla.rex` agree.

**Measured.** Both are rc 120 today, refused at `method "NEW" of class "Object" is not implemented
(Phase 5)`. Oracle for `creo.rex` is rc 0, `type a savings account` / `balance 1000.00` /
`rate 6.25`; for `abscla.rex` it is rc 158, stdout `declared AB Class`, stderr the `98.989 Class AB
is ABSTRACT and cannot be directly created.` transcript with a `Compiled method "NEW" with scope
"Object".` frame.

**What is already there, probed separately.** `Body::Instance(ScopePools)` exists and is what a class
object's own variables already use; `receiver_kind` (`dispatch.rs:1123`) refuses it with
`Err("an instance of a user class")`, which is the seam. `("Object", "INIT", Arity::Fixed(0),
native_no_op)` is already in the native table (`dispatch.rs:375`).
`~objectName`/`~objectName=`/`~string`/`~class`/`~isA` all answer for a class receiver and need only a
second receiver kind. **`~defaultName` answers for no receiver at all** -- `DEFAULTNAME` appears
nowhere in `crates/rexx-exec/src/` outside doc comments, and `.K~defaultName` is rc 120 on both
engines today against the oracle's `The K class` -- so it is built here from zero.
**Re-measure all of this.**

**Build.** `RexxClass::completeNewObject` (`ClassClass.cpp:1882`) in its four steps, in order:
`checkAbstract`, set the behaviour from the class's instance behaviour, register `UNINIT` if the
class or a parent defines it, send `INIT` with `~new`'s arguments. The registry already carries the
`UNINIT` flags (`has_uninit`, `parent_has_uninit`) that step three reads; Task 5 owns what happens
with them afterwards.

Also in this task, because they are the same receiver kind and each is a silent-wrong-answer risk on
its own: an instance's `~string`, `~defaultName`, `~objectName` and `~objectName=`, its `~class` and
`~isA`. Measured, `.Object~new~string` is `an Object` and an instance of `K` is `a K`; setting
`~objectName` changes `~string` and `~objectName` and leaves `~defaultName` answering `a K`.

**5b owes only the constructions its own rows make** (D57, and the spec's native-`~new` split):
`Object` and user classes here, `StringTable` in Task 3, `Array` in Task 8, and **the Message object
`~start` answers** in Task 7 -- *not* `.Message~new`, which refuses `93.901` on the oracle and is 5c's.
Twenty-three of the fifty-nine `*__instance.rex` classes refuse a bare `~new` on the oracle and are
5c's; do not build them and do not treat their rows as this phase's business. `Message` is one of the
twenty-three, which is why the two obligations have to be said apart.

**The rooting hazard is this task's, and it needs a targeted witness rather than the subset run.**
`Interp::pool_owner`'s doc comment (`run.rs:3077`) records that a running send's receiver is rooted
only by the `SELF` slot, which a method body may assign over, and names "the task that creates
instances (`~new`)" as the one that can settle it. This task makes it reachable, and it is a
use-after-free rather than a wrong answer.

**Task 1 measured that doc comment false and it is corrected in the tree.** `Interp::message_term`
takes `push_temp(receiver)` before it resolves anything and both engines enter **a program's message
term** through that one function, so a send a program writes has its receiver rooted by the sending
clause's temporaries frame however the body treats `SELF`. That is narrower than "both engines enter
a method body through that one function", which is false: `Interp::send_message` has callers outside
`message_term` -- `required_string_dispatch`, `send_make_string`, `native_new`, `class_factory`,
`native_string`, `native_object_name`, `native_request` and `lib.rs`'s own -- and four of them were
added by Task 1. The window that is real is one of exactly those: the `INIT` send `~new` makes
itself, where the receiver is the new object and `message_term`'s temporary holds the class.
`native_new` takes its own `push_temp` over it, and removing that one reddens the witness.

**The ordinary-send half of that claim is argued, not witnessed, and the fix round measured it.**
Removing `message_term`'s `push_temp(receiver)` (`dispatch.rs`) in a scratch copy leaves
`collect_stress` at 8 passed / 0 failed -- the harshest collector setting this project has -- so
nothing in the tree reddens when that root goes, and the `go`-side `SELF` clobber in
`instance_self_reassigned.rex` contributes nothing to the rooting question. The line stays: absence
of a witness is not evidence it does nothing. What is witnessed is `native_new`'s own `push_temp`,
which `a_method_that_assigns_over_self_keeps_its_exposed_variables` reddens on.

**Two properties the witness program needs, both found by running it.** The instance must not be
held in a caller variable at the moment the mutation is tested -- with `o = .K~new` the caller's slot
roots it and no rooting mutation reddens anything -- so the committed program clobbers `SELF` inside
`INIT` as well. And the exposed value must be wider than `rexx_core::INLINE_BYTES`, or it rides in
the handle, is never a heap object, and the row stays green under every mutation including
`Body::trace` losing the pools.

**The subset-wide stress run cannot see it**, two ways: `collect_stress.rs`'s subset test walks
`SUBSET_FILES`, which contains no gate-table probe, so neither of this task's rows is in it; and no
ordinary program assigns over `SELF`. (Once the task's own witness is in `corpus/phase-5b.txt` the
subset run does read it, so the targeted case and the subset row redden together.) The construct is
legal and reachable -- measured, oracle rc 0, `a kept 192 clobbered`:

```rexx
o = .K~new
say 'a' o~go
::CLASS K
::METHOD init
  expose v
  v = 'kept'
::METHOD go
  expose v
  self = 'clobbered'
  t = ''
  do i = 1 to 100
    t = t || i
  end
  return v length(t) self
```

`collect_stress.rs` already holds targeted cases for exactly this class of hazard; this is one more
beside them.

**Done when**

* both rows agree on both engines under the phase-gate command;
* a program that assigns over `SELF` inside a method body and then allocates enough to force a
  collection agrees on three descriptors, is in `corpus/phase-5b.txt`, and passes under
  `run_program_collect_every_alloc` as a targeted case beside `collect_stress.rs`'s existing ones;
* **`corpus/phase-5b.txt` carries the instance-naming witness.** No gate row sends `~string`,
  `~defaultName`, `~objectName`, `~objectName=`, `~class` or `~isA` to an *instance* -- `creo.rex`
  sends `~type`/`~balance`/`~rate`, `abscla.rex` sends to a class, and Object's instance method row is
  `hasMethod` calls and is 5c's -- so six methods this task calls a silent-wrong-answer risk would
  otherwise have no witness at all. Oracle for the instance receiver, rc 0: `an Object`, then `a K`
  for `~string`/`~defaultName`/`~objectName`, then after `~objectName = 'zed'` both `~string` and
  `~objectName` answer `zed` while `~defaultName` still answers `a K`;
* and **two controls are recorded as run**: dropping `checkAbstract` from the `~new` path makes
  `abscla` construct (the row's own committed control), and not sending `INIT` after the object is
  built reddens `creo`.

---

## Task 2: the behaviour snapshot (D58)

**Goal.** `corpus/gate-tables/concepts/objcla.rex` agrees, and both arms of D58 have witnesses.

**Measured.** `objcla.rex` was rc 120 when this plan was written; oracle rc 0, `before 0` /
`after 0` / `fresh 1`.

**Task 1 has since changed the row's shape, and not in this task's favour.** `Body::Instance` carries
its class and dispatch resolves against that class's instance behaviour live, which is exactly the
"live walk" the paragraph below names -- so `objcla` now reads `diverge-stdout`, rc 0 on both sides,
empty stderr on both sides, `after 1` against the oracle's `after 0`. The row was a loud refusal and
is now a silent wrong answer, which is this phase's worst defect class, and closing it is this task's
whole subject. What the shape has to become is an instance holding the behaviour it was built with:
`rexx_classes::ClassGraph` already models the copy on `~define`, `~defineMethods` and `~delete` and
the in-place rebuild on `~inherit`/`~uninherit`, and already exposes `instance_behaviour_handle` with
a `lookup_at`/`has_method_at`/`resolve_super_scope_at` family over a `BehaviourHandle` -- what it does
not do is let `rexx-core` name that handle, since `Body` cannot depend on `rexx-classes`.

**Build.** The rule is D58 and the spec carries its evidence: `~define`, `~defineMethods` and
`~delete` replace the class's instance behaviour with a copy so existing instances keep the old one
(`ClassClass.cpp:860`-`:862`, `:531`-`:533`, `:962`-`:964`, each with the C++'s own comment saying
so); `~inherit` and `~uninherit` rebuild the existing behaviour in place (`:1361`, `:1413`, both into
`updateSubClasses` at `:1036`), so existing instances see the change.

**Make it a property of where the behaviour lives, not a rule restated at each mutator.** A live walk
of the class graph on every send gets `~inherit` right and `~define` wrong; a copy on both families
gets `~define` right and `~inherit` wrong. Both are rc 0 with empty stderr.

**Done when** `objcla` agrees on both engines; `corpus/phase-5b.txt` carries the `~inherit` and
`~uninherit` witnesses; and **two controls are recorded as run**.

The spec prints the `~inherit` program and states the `~uninherit` arm in prose without one, so the
`~uninherit` witness is here: `k = .Object~subclass('K')`, `k~inherit(.Mx)`, `o = k~new`, then
`k~uninherit(.Mx)` gives `a 1` / `b mixin-ran` / `c 0` / `e trapped 97` at oracle rc 0, with `.Mx` a
`MIXINCLASS Object` carrying `mxm`. Neither arm exists in the corpus today: of the committed `.rex`
files matching `~inherit` or `~uninherit`, none also uses `~new`.

The controls: `objcla`'s own committed control (rebuild the lookup from the class on every send)
reddens `objcla`; and **copying the behaviour on `~inherit` too reddens the new `~inherit` witness
while every gate row that agreed before the mutation still agrees after it**. The second is the point
of the task -- it is the mutation no existing row can see -- and it is phrased against the rows that
were green rather than against "every gate row", because at this point in the plan several 5b rows are
still red for reasons that have nothing to do with the mutation. **Re-run it at Task 10** against the
full green table and record it there too.

---

## Task 3: per-object methods, their scopes, and the restricted-private check

**Goal.** `corpus/gate-tables/concepts/usesem.rex` agrees.

**Measured.** rc 120 today; oracle rc 0, `setmethod one-off` / `not-shared 0` / `enhanced enhanced` /
`still-not-shared 0`. The probe needs `.StringTable~new`, **`StringTable`'s `[]=`** and
`Class~enhanced` as well as `setMethod`. `[]=` exists for **no** receiver kind today -- the native
table has `[]`, `AT`, `PUT` and `UNKNOWN` for `StringTable` and no setter -- and the syntax parses, so
this is a build item rather than a parser one: `a = (7,8,9)` then `a[2] = 5` is rc 120 at
`method "[]=" of class "Array" is not implemented` after printing the `[]` read.

**Build.** `setMethod`, `unsetMethod`, `enhanced`, and the object's own scope. `compile_method_source`
already turns a source string into a method object for `~define` and is what the string and
string-array forms need here.

**`compile_method_source` keeps no body, though, and that is the largest single piece of this task.**
Its own doc comment says so -- "The compiled body is validated and not retained" -- so nothing in the tree can *run* a
method compiled from source text, and `usesem`'s `setMethod("EXTRA", "return 'one-off'")` has to run.
Two things follow. A compiled body needs a `(program, directive)` pair, which means filing the parse
as a program of its own. And **a compiled method reports under its own name, not the program's
path**: measured, oracle rc 214, a one-off whose body is `return 1/0` reports `Error 42 running MM
line 1:`, and `parse source` inside one answers `LINUX METHOD MM`. The name is the one the caller
wrote, which `setMethod` upcases and `~define` does not.

**The scope argument is D67 and is the part most easily got silently wrong.** `FLOAT`, the default,
is **one pool per object** shared by all that object's `FLOAT` one-offs and separate from the class's;
`OBJECT` shares the class's. One `FLOAT` method cannot distinguish "one pool per object" from "one
pool per method" -- the spec measures the difference with two.

**The restricted-private check is D66 and has two refusals, not one.** From a program context the
ordinary private check refuses first at dispatch: `97.2 ... cannot accept private message "SETMETHOD"
from this context.` at rc 159, with no method frame on the traceback. `checkRestrictedMethod`
(`ObjectClass.cpp:697`) refuses from a class method of a class the object is not an instance of:
`98.991 Method SETMETHOD may only be invoked from a method of the same object or one of its
classes.` at rc 158, **with** a `Compiled method "SETMETHOD" with scope "Object".` frame. The frame is
the discriminator. `send` and `sendWith` are not in the trio and answer from a program context.

**The per-object first step of the search order is this task's too**, and `usesem.rex` cannot see it:
it defines `EXTRA` only in the object's scope, so an implementation searching the class first and the
object second prints byte-identical output. The spec's discriminating program shadows a class method
of the same name and carries `unsetMethod` with it.

**`Class~enhanced`'s methods are a level of their own, not `setMethod`'s.** The oracle merges the
enhancing table into a dummy subclass's behaviour and then rebuilds that behaviour's dictionary
(`ClassClass.cpp:1454`-`:1462`), so nothing tracks them as instance methods of the object, and
`MethodDictionary::removeInstanceMethod` removes from the dictionary only what the object's own
`instanceMethods` table held (`MethodDictionary.cpp:363`-`:371`). So `unsetMethod` over an enhancing
name **reveals** it where the same call over a `setMethod` entry removes it, and an enhancing name
nothing set is untouched -- measured, oracle rc 0 for both, and rc 0 with different stdout for an
implementation that keeps one level for the two.

**A one-off `UNINIT` is this task's too**, and it is the silent wrong answer this task most easily
ships: nothing registers the object, so the finalizer never runs, at rc 0 with empty stderr on both
sides. `defineInstanceMethod` and `deleteInstanceMethod` each call `checkUninit`
(`ObjectClass.cpp:2314`, `:2338`), so attaching `UNINIT` to one object registers it for finalization
and hiding the name cancels a class's -- measured, oracle rc 0 for both directions, from a forced
collection and at termination alike.

**Done when** `usesem` agrees on both engines; `corpus/phase-5b.txt` carries the precedence program,
the two `FLOAT` methods sharing a pool with a second instance seeing an uninitialised name, and both
restricted-private refusals; and **two controls are recorded as run**: `usesem`'s own committed
control (put a `setMethod` definition in the class's dictionary so it reaches the class's other
instances) reddens `usesem`, and searching the class before the object reddens
the new precedence program while every gate row that agreed before the mutation still agrees after it.
That second control is re-run at Task 10 against the full green table, for the reason Task 2's is.

---

## Task 4: `FORWARD` and `DELEGATE` (D62)

**Goal.** Gate table D's two 5b rows agree over probes that exercise the mechanism.

**Measured.** Both rows `agree` today and neither sends the delegated message: each probe is
`say 'main'` plus directives, so both sides print `main` at rc 0 whether or not `DELEGATE` exists.

**Build.** `FORWARD` as an instruction, then `DELEGATE`. Measured, a plain `forward class (super)`
returns from the forwarding method rather than continuing it.

**`dire.xml`'s stated equivalence is not observably exact, and building `DELEGATE` as it would ship a
differential mismatch.** Measured by Task 4 over one failing inner method, oracle, both shapes rc 214
`Error 42.3`: `::method m delegate d` reports the inner clause and then the sending clause, while
`::method m` / `expose d` / `forward to (d)` reports the inner clause, **its own `forward to (d)`
clause**, and then the sending clause. The C++ builds a `DelegateCode`
(`parser/DirectiveParser.cpp:2442`, `execution/CPPCode.cpp:605`), a primitive that pushes no Rexx
activation, so it leaves no frame. Build `DELEGATE` as a directive-implemented method beside the
generated accessor pair -- the shape `read_attribute` and `write_attribute` already have, which is
"no activation and no frame" for the same reason -- and for `::ATTRIBUTE` install the pair `name` and
`name=`, both delegating.

**`instrc.xml` `keyForward` names six options: `TO`, `CLASS`, `MESSAGE`, `ARGUMENTS`, `ARRAY`,
`CONTINUE`.** `class` and `to` are measured; the other four are not, and the spec records that it did
not read `keyForward` in full. **Measure them before building, and say which arms this phase owes** --
both replacement probes exercise `to()` alone, so four options would otherwise be built with no
acceptance criterion anywhere.

**`FORWARD` as an instruction needs no instance and can land before Task 1**, which is also why it
gets its own corpus witness rather than riding on the `DELEGATE` rows. Measured, oracle rc 0
`a base-cm` against the crate's rc 120 `FORWARD is not implemented (Phase 5)` on both engines:

```rexx
say 'a' .K~cm
::CLASS Base
::METHOD cm CLASS
  return 'base-cm'
::CLASS K SUBCLASS Base
::METHOD cm CLASS
  forward class (super)
```

**Replace both probes, under two constraints the spec measured.** `gate_table_d.rs`'s
`expected_oracle_lines` bounds a non-refusing row's oracle stdout at exactly one line, so a two-line
probe reports `unanswered` and can never reach `agree`. And the `::ATTRIBUTE` probe must send the
setter as well as the getter, reading back **through the delegate object** -- the spec prints both
one-line probes.

**Done when** both rows agree on both engines with the replaced probes; `corpus/phase-5b.txt` carries
a `FORWARD`-as-an-instruction witness, which D64's list asks for and this task had not named; the four
unmeasured options have an answer recorded either way; and **the control is recorded as run**:
replacing `::attribute at delegate d` with a plain `::attribute at` in the setter probe
makes it print `main AT` instead of `main via-set`. That control is not optional bookkeeping. A draft
of the same probe read `k~at` instead of `k~peek~at` and passed with `DELEGATE` unimplemented; the
control is what separated them.

**The `::METHOD` replacement's own value is load-bearing and the spec's choice was not.** With
`d = 'abcdef'` that probe prints `main 6` -- and so does a build that reads the **directive's own
name** instead of the `DELEGATE` symbol, because an uninitialised variable renders as its own
spelling and `'length'` is six bytes too. Measured: `'length'~length`, `'LENGTH'~length` and
`'abcdef'~length` are all `6`. The committed probe assigns `'abcdefgh'` and prints `main 8`, which no
wrong-variable reading produces. **The spec carried the `'abcdef'` spelling in D62's quoted probe
and the controller corrected it at `1aa54204b`**; both of its sites now use `'abcdefgh'`.

**The `::ATTRIBUTE` probe gained a second send in the Task 4 fix round, and the spec's quoted copy of
it has not.** Reading back only through `k~peek~at` leaves the delegating **getter** unsent: measured
in a `git archive` extract, a build whose `::ATTRIBUTE ... DELEGATE` registers a plain getter and a
delegating setter prints `main via-set` for that probe and leaves the corpus at `315 of 315` and
table D's 5b rows at `0 not yet agree`. With `say 'main' k~at k~peek~at` the same build prints
`main AT via-set` -- the added send alone is what moves -- and the row reads `1 not yet agree` at
exit 101. The committed probe now carries both sends; the spec's quoted program at
`docs/superpowers/specs/2026-08-27-phase-5b-instances.md:834`-`:836` still carries one, **and
aligning it is the controller's**, as the `'abcdef'` correction above was. The spec's own paragraph
about why the probe reads through `peek` stays true and describes the second send, not the first.

---

## Task 5: `UNINIT` (D60, D61, D69), and characterising the sweep order

**Goal.** `corpus/gate-tables/concepts/obdes.rex` agrees, and instance `UNINIT` is delivered.

**Measured, as this task found it.** `obdes.rex` was a silent row: rc 0 and empty stderr on both
sides, oracle stdout `main` / `uninit ran` against a crate stdout of `main`
(`.superpowers/sdd/2026-08-27-phase-5b/task-5-report.md:456`, `:614`). The oracle's half does not
move; the crate's is what this task changes.

**What is already there.** `Heap::collect` returns `CollectStats::pending_uninit`, the unreachable
objects whose flag is set, resurrected rather than swept; `Heap::set_uninit` is the only writer of
`true` and `Heap::clear_uninit_all` is how a caller reports the finalizer ran. `Interp::collect`
(`lib.rs:6446`) carries a `debug_assert!` that the list is empty whose comment names itself as the
site that owes delivery. `ClassGraph::check_uninit` and `parent_has_uninit` are 5a's half of the
propagation -- **but they answer about a class's *instances*, which is deliveries 1 and 2 only.**
Delivery 3 has no predicate at all: the class-object side, where a `::METHOD uninit CLASS` lands,
needs the table the oracle's `requiresUninit` fills. That table is this task's to build, and
`obdes.rex` -- this task's only gate row -- is exactly that side. **Re-measure all of this.**

**Build three deliveries, not one.**

1. **From a collection**, for an instance: replace `Interp::collect`'s assertion with the delivery,
   and `clear_uninit_all` after the finalizer runs. Reproducible shape, five runs of five: `drop` then
   `call gc 'force'`, one instance -- **with allocating clauses between `~new` and the `drop`**, and
   that precondition is not optional. The oracle holds recently allocated objects out of a driven
   collection (`Memory::SaveStackSize`, and `GC('force')` is `collectAndUninit(false)`, which keeps
   that stack), so written without them the witness cannot fire on the oracle at all: measured at
   Task 5 on a program whose padding clauses are bare string assignments, eight give `a c uninit`
   and nine give `a uninit c`. **The threshold is in allocations, not in clauses**, so it does not
   transfer to a program whose clauses allocate more: `corpus/lang/uninit_instance_collected.rex`
   fires with two clauses in that position, and its own comment carries the transcript that says
   which of the two carries it.
2. **At termination, over every live object carrying the flag** -- D69. This is not the same as (1)
   and cannot be built out of it: under D59 a class-scope `EXPOSE` roots an instance for ever, so it
   is never unreachable and a collection-driven delivery never runs its `UNINIT` at all. The oracle
   fires those at termination, measured, and the spec prints the program.
3. **At termination, for class objects** -- D60. Never from a collection, because D59 means a class
   object is never collected.

**Characterise the order, which the spec deliberately does not.** D60 records that the oracle's class
`UNINIT` order at termination is reproducible and is **not** creation order: a three-class program
fires a class declared second after one declared third, three runs of three, and a mixed program
fires runtime-built classes before directive-installed ones. Two witnesses are not a rule. Find the
rule, or establish that it cannot be characterised cheaply and say so; either outcome is the task's
output, and D61 stays in force over class objects until the first one lands.

**ANSWERED at Task 5, from the oracle's source and confirmed by running.** The sweep
(`MemoryObject::lastChanceUninit`, `memory/RexxMemory.cpp:324`) walks `uninitTable`, an
`IdentityTable` at `HashCollection::DefaultTableSize` -- 17 buckets. `HashContents::iterateNext`
(`classes/support/HashContents.cpp:489`) takes buckets in index order and the overflow chain within
one, which `HashContents::put` appends to. The bucket is `getHashValue() % bucketSize`
(`HashContents.hpp:204`), and **`RexxClass::getHashValue` (`classes/ClassClass.cpp:209`) is the hash
of the class's id STRING** -- `h = 31*h + byte` over the bytes (`classes/StringClass.hpp:328`).

So: **`strhash(id) % 17` ascending, ties by the order the classes entered the table.** Nothing about
creation order enters it except as the tie-break.

**Where an entry is made is a separate question, and getting it wrong is silent.** The oracle enters
a class from `RexxClass::checkUninit` (`classes/ClassClass.cpp:1211`), and its callers are the class
factory tail and **`RexxClass::updateSubClasses` (`:1052`)**, which `inherit` (`:1360`) and
`uninherit` (`:1413`) both end in and which recurses into every subclass. The directive
`::CLASS K INHERIT M` is the same path, because `ClassDirective::install` sends `INHERIT` to the
class object (`instructions/ClassDirective.cpp:230`). A first implementation of this task called
`check_uninit` only where a class is *declared*, and four `UNINIT` deliveries went missing at rc 0
with empty stderr -- two class-side and two instance-side, one pair per inherit path. The crate's
`ClassGraph::update_sub_classes` is the single place that closes all four.

**A registered finalizer can still be cancelled, and not by removal.** `RexxObject::uninit`
(`classes/ObjectClass.cpp:2579`) re-checks `hasMethod(UNINIT)` at delivery, and `checkUninit` never
clears anything, so after `.QQ~uninherit(.MX)` the oracle reaches `QQ` in the sweep and runs
nothing: measured, three runs of three, rc 0 with empty stderr, `main / inherited / uninherited /
u MX`. An instance built between an `~inherit` and its `~uninherit` is the same shape and runs no
finalizer either. Confirmed by running, three runs each, rc 0 and
empty stderr: four classes declared `C B A D` fire `D A B C`; twenty declared `A`..`T` fire
`D E F G H I J K L M N O P Q A R B S C T`; three ids colliding in one bucket fire in declaration
order and reverse when the declarations reverse; and a runtime-built class placed at bucket 16 fires
**last**, which falsifies the runtime-before-directive reading above -- that observation was hash
order.

**The same reading is why instances are NOT reproducible**, which is what D61 was protecting:
an instance's `getHashValue` is the inherited `identityHash()`, its address
(`classes/ObjectClass.hpp:335`, `:340`). Measured: one class-side `UNINIT` on a class at bucket 16
beside one live instance answered `instance` before the class 19 times in 20 and after it once.

**So D61 lapses by its own terms over class-against-class order and stays in force over anything
involving an instance.** `corpus/lang/uninit_class_sweep_order.rex` is the committed witness of the
rule; `ClassRegistry::take_uninit_classes_in_sweep_order` is where it is implemented. It predicts the
oracle only while the table has not expanded (`HashCollection::expandContents` doubles and rehashes);
twenty entries do not reach that, measured.

**5a's Task 7 handed three class-`UNINIT` programs to this phase and no task had named them**
(`docs/superpowers/records/2026-08-17-phase-5a/task-7-report.md:333`-`:348`, "All three programs
travel to 5b together"). `obdes.rex` has one class declaring its **own** `UNINIT` and cannot see
either of the other two arms. The oracle's answer for each, rc 0 with empty stderr:

```
::CLASS P / ::METHOD uninit CLASS / ::CLASS K SUBCLASS P
   oracle:  main / uninit on K / uninit on P     ten runs of ten

::CLASS M MIXINCLASS Object / ::METHOD uninit CLASS / ::CLASS K INHERIT M
   oracle:  main / uninit on K / uninit on M     three runs of three
```

(The mixin arm as 5a handed it over gave `K` a `::METHOD uninit CLASS` of its own. That version
agrees on both sides whether or not `INHERIT` propagates anything, so it cannot witness its own
subject; the committed row is the version above, and deleting its `INHERIT` moves the oracle's
output to `main / uninit on M`.)

The first is the inherited arm: `K` declares no `UNINIT` and fires one anyway, **before** the parent
that declares it. That also constrains the order question above -- `P` is declared first and fires
second. Both must agree. **Under D61 neither can be a corpus row until the order question is
answered**, so if it is not, record them as still open with their transcripts rather than dropping
them. (`K` is bucket 7 and `P` is 12, `M` is 9: both arms are the rule above, and both are corpus
rows -- `corpus/lang/uninit_class_inherited.rex` and `corpus/lang/uninit_class_mixin.rex`.)

**Done when** `obdes` agrees on both engines; the two inherited arms above agree or are recorded open
with their transcripts; `corpus/phase-5b.txt` carries a one-instance collection
delivery and a one-instance termination delivery; the order question has an answer recorded either
way; `gate_table_c.rs`'s `obdes` control text is corrected from "stop running `UNINIT` before the
object's storage is reclaimed" to "skip the termination sweep", since under D59 a class object's
storage is never reclaimed; and **the control is recorded as run**: skipping the termination sweep
reddens `obdes`, silently, at rc 0 with empty stderr on both sides.

---

## Task 6: the licensed divergences (D59a)

**Goal.** D59's licence has witnesses the harness runs.

**This task follows Task 5.** Its divergence is "the class's `UNINIT` runs in the termination sweep
rather than at a driven collection", and the crate does not do that until Task 5's delivery 3 lands.
Written first, the DEVIATION pins "never runs" and Task 5 then makes it diverge differently -- which
is the condition this task says must redden, arriving from inside the plan.

**Build, in two halves, because the file the shape comes from does not supply the second.** The row
goes in `docs/superpowers/plans/phase-4-exclusions.txt`'s DEVIATIONS section, in the
IMPLEMENTED / SCOPE / WHY shape the parent spec's D41 already cites for `~identityHash` -- **or in a
Phase 5 successor file named by this task**, since that file's set is read by a Phase 4 builtin gate
and putting a Phase 5 row in it is a decision, not a default. Say which and why.

**The runnable half has to be invented, because that file is prose nobody executes** -- DEVIATION 4
carries sixteen transcripts and nothing re-runs any of them. Build it in the shape of `ir_dual.rs`'s
`KNOWN_DIVERGENCES` and its `the_known_engine_divergences_still_diverge_exactly_as_recorded`: the
program, the oracle's expected stdout, the crate's expected stdout, and an assertion that the two
differ. That test's own doc states the property this task needs -- red if either side's answer moves
in either direction, "including a fix, which is what should delete the row rather than update it".

The licence covers four consequences and this task owns the one 5b makes reachable: a class's
`UNINIT` running in the termination sweep rather than at a driven collection. The witness runs both
interpreters and **asserts the expected divergence**, so it reddens if the crate ever starts matching
or starts diverging differently.

The other three are recorded with the phase that owes them: `~subclasses` keeping a dropped class and
a `WeakReference` to one still answering it are 5c's, because 5c lands those methods; the OOM
asymmetry is stated in D59a with its measurement (100,000 `~subclass` calls, oracle rc 0
`done 100001`, crate rc 137 OOM-killed at the 1G cap) and needs no runnable witness, because a
witness that OOM-kills a gate run is worse than the prose.

**Done when** the DEVIATION exists in a named file, the harness runs its witness, and **the control is
recorded as run**: delivering the class's `UNINIT` from the driven collection instead of the
termination sweep reddens the DEVIATION's assertion. (That is the applicable mutation. "Make the crate
match the oracle" is not one anybody can apply without undoing D59.)

**A fifth divergence, found by Task 5, and this task has to decide it rather than absorb it.** It is
about **instances**, not class objects, so D59a's licence -- which is stated over "a class object was
collected" -- does not reach it, and D59a's own rule is that a further observer needs its own
decision.

**What it is: when a driven collection reaches a just-created instance.** The oracle pushes every
newly allocated object onto a `PushThroughStack` of `Memory::SaveStackSize`
(`memory/RexxMemory.cpp:1053`, `memory/Memory.hpp:107`, value 10), and `GC('force')` is
`collectAndUninit(false)` -- the `false` keeps that stack. So the oracle's forced collection cannot
reach an object still in it. This crate has no such hold, so its forced collection does.

Measured at Task 5, three runs at each N, rc 0 and empty stderr throughout, on `o = .K~new` followed
by N padding clauses each allocating one string, then `drop o`, `call gc 'force'`, `say 'c'`:

```
N       oracle                crate (ir and tree-walker)
0..8    a c uninit            a uninit c
9..     a uninit c            a uninit c
```

`nop` in place of a padding clause does not move the threshold and two `GC('force')` calls in a row
do not either. It is silent -- rc 0, empty stderr, stdout differing in the *position* of one line.

**Why Task 5 did not close it.** Closing it is a ring of recently allocated handles rooted from
`Interp::alloc_with`: the hottest path in the interpreter, a change to collection *reachability* for
every object rather than to `UNINIT`, and a `collect_stress` interaction. That is a decision about
this crate's collector.

**Two ways out and this task picks one.** Either license it, with a runnable witness in the same
shape as the class-object one above and the note that no corpus program is on the diverging side --
`corpus/lang/uninit_instance_collected.rex` deliberately puts allocating clauses between `~new` and
`DROP`, and its own comment says why -- or build the hold and pay the sitting. **Do not leave it
unstated**: a silent wrong answer with no row anywhere near it is the defect class this phase is
most exposed to, and it now has a measurement.

**Task 6 licensed it**, as DEVIATION 6 in `docs/superpowers/plans/phase-4-exclusions.txt`, with the
witness `rust/crates/rexx-exec/tests/licensed_divergences.rs` runs and its control recorded as run.
**Ruled by Moritz, 2026-09-01**: *"anything that depends on gc ordering is inherently unpredictable,
and we shouldn't aim for equal behavior. it just needs to be one of the many correct orderings."*
The reason the row gives is the documented contract rather than an incapacity --
`oodocs/rexxref/en-US/provide.xml`'s `obdes` section and `oodocs/rexxpg/en-US/classes.xml:447` both
say only that Rexx runs an UNINIT *before reclaiming the object's storage*, and neither says when
reclamation happens or in what order across objects, so both timings are correct orderings of the
same program. The licence is over **ordering only**, and each row's SCOPE states the boundary in
full: the finalizer must still run, after the object is unreachable and no later than termination,
with no exit-status or stderr difference and no stdout difference beyond the position of the
finalizer's own output. The same framing is applied to the class-object row, which this task had
first written as a shortfall against the oracle.

Below that, as a supporting fact rather than the reason: this crate's arena allocation count, read
through `rexx_exec::run_program_collect_every_alloc`, does not move for either padding shape -- the
family
reads 4 at each `N` measured (0, 1, 2, 3, 4, 8, 9, 10), the concatenating one reads 4 at each of
`N` = 1, 2, 3, 4, and fifty concatenating clauses with no instance at all read 0 -- where the
oracle's window advances one slot per plain padding clause and three per concatenating one. So a
ten-deep hold would not reproduce the threshold. Measured under that hold rather than predicted:
`REXX_CORPUS_GATE=1 cargo test --release -p rexx-exec --test corpus` goes from `315 of 315 matching`
to `4 of 315 corpus programs disagree`, `corpus/lang/uninit_instance_collected.rex` among them, and
the padding family answers `start / after-gc / uninit ran` at `N` = 8, 9 and 10 alike -- agreeing at
8 and diverging at 9 and 10, which is the other half of the range. The row is not D59a's: that licence is
stated over a *class object* being collected, and this is an instance, so it is a decision of its own
and lives only in the deviations register.

---

## Task 7: `~copy`, `~run`, `~send`/`~sendWith`, `~start`/`~startWith`

**Goal.** Each has a witness in `corpus/phase-5b.txt` and agrees.

**Measured, oracle, all rc 0 unless noted.** `~copy` is shallow, copies the object's own scope and
its exposed variables, and `(o == c)` is `0`. `o~send('M', 3)` and `o~sendWith('M', (4,))`
answer from a program context. `o~run(...)` is `97.2` at rc 159 from a program context -- Task 3's
check, reached again here. `o~start('M', 5)` answers a `Message`; `m~result` is the value and
`m~completed` after it is `1`.

**Two of those measurements do not discriminate what the task builds, and a witness written from them
alone is met by a wrong implementation.**

* **`~copy`.** The copy's initial values and its identity read the same whether the pools were copied
  or shared. The witness must **write through the copy and read the receiver back**: after
  `c~set('changed')` the oracle answers `o~get` `orig` and `c~get` `changed`, rc 0. Without those two
  lines the named control cannot redden anything.
  **The identity line cannot be spelled `(o == c)`.** An instance as an operator's *left* operand is
  a loud refusal this phase keeps on purpose -- `eval.rs`'s
  `a_named_instance_is_never_an_operators_left_operand` asserts it and
  `corpus/lang/instance_named_operands.rex` is the half the oracle answers -- so `(o == c)` is rc 120
  here, measured on both engines. `(o~identityHash == c~identityHash)` is the same claim through a
  position the phase does allow: oracle rc 0, `0` for the copy and `1` for the receiver against
  itself, and both engines agree.
* **`~run`.** The only measured behaviour above is the refusal, which fires at dispatch before any
  body runs -- so a witness of it agrees as soon as `RUN` is a restricted-private name with no body
  behind it. The allowing arm must be witnessed too: from a method,
  `self~run('use arg x; return "ran" x', 'I', 5)` answers `ran 5`, oracle rc 0. Note the D67
  interaction, measured: the run method's `EXPOSE` reaches the object's **float** pool, not the
  class's, so `self~run('expose v; return v*10')` raises 41.1 at rc 215 where the class's `v` is 7.

**`~run`'s option surface is the largest in this task and nobody has enumerated it.**
`fundclasses.xml` `mthObjectRun`: a Method object, a source string, or an Array of source strings;
times `Individual`, `Array`, or neither; first letter only. Enumerate it before building and say which
arms this phase owes.

**Build.** The four paths, plus enough of `Message` for `~result`, `~completed` and `~hasError`.
`send` and `sendWith` also accept an array whose first item is the name and whose second is a class to
start the method search from -- the dynamic form of the `~m:scope` override 5a built statically. That
is documented in `mthObjectSend`/`mthObjectSendWith` and was **not** measured while writing the spec;
measure it before building it.

**D68 bounds what may be asserted.** `~start`'s interleaving with the program that started it is not
reproducible (twenty runs, two orders), and **`~completed` sampled before `~result` is part of that
interleaving** -- forty runs split thirty to ten. A corpus program may assert `~result`'s value and
`~completed` after it, and nothing else about timing.

**Done when** each of the four has a witness that agrees on both engines; the `~start` witness asserts
`~hasError` after `~result` and the `send` witness carries the array form's starting-class override,
both of which are mechanism-set items this task's acceptance had not covered -- measured, oracle rc 0,
`o~send('M')` answers the subclass's method and `o~send(('M', .Base))` the base's; and **the
control is recorded as run**: making `~copy` share the receiver's scope pools instead of copying them
reddens the `~copy` witness at rc 0, which it can only do with the write-through lines above.

**The starting-class override is written as an array literal and not as `.Array~of`.** The behaviour
is the same either way -- both spellings are oracle rc 0, `plain sub` / `array base` -- but
`.Array~of` is Task 9's and is rc 120 here, so a witness written with it would be red for a reason
that is not this task's. Measured on both engines at BASE: `a = ('M','x') ; say a~items ; say a[1]`
answers `2` / `M` byte for byte with the oracle on `ir` and on `tree-walker`, while
`.Array~of('M','x')~items` is `rexx-exec: method "OF" of class "Array" is not implemented (Phase 5)`
at rc 120 on both.

What `~start` does with a method that **raises** is not asserted. Measured after the plan was
written, so that the reason for not asserting it is on the record rather than the absence of a
measurement: `o~start('M')` on a method whose body is `1/0`, then `m~result`, is oracle **rc 214**
with the same 42.3 report written to stderr **twice** -- the message's own and the one `~result`
raises again in the sender. **The transcript is not reproducible**: four runs of that one program
gave two orderings, three of them interleaving the two reports into one block whose `running ...
line` lines both name the `m~result` clause, and one giving two complete reports naming the method's
own clause and then `m~result`'s. That is D68's interleaving, so the shape stays out of the corpus.

---

## Task 8: multidimensional `Array`, and `methodsbyclass` (D63)

**Goal.** `corpus/gate-tables/concepts/methodsbyclass.rex` agrees, with the equivalence arm added.

**Measured.** rc 120 today; oracle rc 0, `element 0` / `dimensions 2` / `methods 1 1` /
`environment Array Directory StringTable`.

**Build.** `.Array~new(2, 3)`, `~dimension`, and `[]`/`[]=` with several indexes. `Body::Array` is a
flat `Vec<Option<ObjRef>>` today, so this is a representation change and not a method addition.

**Add the arm the section states and the probe omits.** `provide.xml` `methodsbyclass` says
`matrix[2, 3] = 0` and `matrix~"[]="(0, 2, 3)` are both valid and equivalent; the committed probe
writes `matrix[2, 3] = 0` once, reads `matrix[2, 3]` once, and never uses the message form. The row's
own committed control already names that half.

**The equivalence arm alone does not make the row see the indexing, and this is the task's real
acceptance problem.** The probe writes one cell and reads the same cell, so **any injective index
mapping passes** -- row-major, column-major, transposed, off by a constant -- because the write and
the read miss together. Adding the message form writes and reads that same cell again: an arity check,
not a mapping check. The committed control catches only the collapse to one subscript. So D63 would
close over an acceptance that cannot see the indexing.

**A second, asymmetric cell does not discriminate either, and this paragraph used to say it did.**
The sentence it replaces read "`m[1,2]` and `m[2,1]` are distinguishable only under the right
mapping", over the program below, and that is false for the reason the paragraph above it already
gives: a transposed mapping transposes the write and the read alike, so the two cells swap places
together and every read still answers what was written to it. **Measured during Task 8** with the
crate's own `multi_dimension_position` accumulating the offset in reverse -- row-major where the
oracle is column-major -- this program is byte-identical to the oracle on stdout, stderr and exit
status, on both engines:

```rexx
m = .array~new(2, 3)
m[1, 2] = 'a12'
m[2, 1] = 'a21'
m~"[]="('b23', 2, 3)
say 'x' m[1, 2] m[2, 1] m[2, 3]      -- x a12 a21 b23
say 'y' m~dimension m~dimension(1) m~dimension(2)   -- y 2 2 3
say 'z' m~size m~items               -- z 6 3
```

**What discriminates is a reader of the slots in their own order**, because nothing built out of a
write and its matching read can see a relabelling both of them share. `~toString('l', ' ')` is one,
and the shipped probe carries it: measured, oracle rc 0, a 2 by 3 array with `m[i, j]` set to
`i || j` at every cell renders `11 21 12 22 13 23`, so the **first** subscript moves fastest
(`ArrayClass::validateMultiDimensionIndex`, `classes/ArrayClass.cpp:1408`-`:1410`). Under the
transposed build above the same probe renders `a12 a21 z23` where the oracle renders
`a21 a12 z23`, and its `element` line is identical on both -- which is the same measurement from the
other side.

The asymmetric cell stays in the probe regardless: it is what witnesses the *operator* spelling of
the setter, since the cell the equivalence arm overwrites is read back from the message form alone.
`~dimension(1)`/`~dimension(2)` stay too, pinning the shape where a bare `~dimension` answering `2`
would not.

**`oracle_lines` moves with the probe.** `gate_table_c.rs`'s `methodsbyclass` row declares a line
count, checked as `OracleShape::Exactly`, and a structural failure is red in **every** mode -- so an
added output line that leaves the literal alone is red for a reason that has nothing to do with this
task.

**Done when** the row agrees on both engines with the equivalence arm, the asymmetric second cell
**and a reader of the slots in their own order** in the probe, `oracle_lines` matches, and **the
control is recorded as run**: the row's committed control (route `matrix[2, 3] = 0` to a single-index
`[]=`) reddens it, and **transposing the index mapping also reddens it** -- which it cannot do today.

---

## Task 9: the instance-side reading of 5a's limits

**Goal.** Every limit 5a pinned with a class as the receiver is re-measured with an instance.

**Where this comes from.** The parent spec calls it Task 7's recorded debt and 5b's mechanism set
carries it as a row. It is an audit, not a build: 5a could only send messages to class objects, so
every refusal text, error number and traceback line it pinned was measured on one receiver kind and
assumed for the other. A limit measured on one and assumed for the other is a claim, not a
measurement.

**Method.** Walk 5a's landed error and refusal sites, and for each, send the same message to an
instance. The differences to expect are in the receiver's rendering (`an Object`, `a K`) and in the
`Compiled method "X" with scope "Y".` frame. Anything that diverges is either a defect this task
fixes or a row `corpus/phase-5b.txt` gains.

**Divergences already found that belong on this task's list.** Each was measured on both engines
with three descriptors read separately; the list is appended to as later tasks find more, so it is
not a closed set and no task may read it as one.

**A method installed by a runtime `~define` cannot be sent.** Found by Task 2 and measured by the
controller at `f558ea501`, both engines, three descriptors read separately:
`.K~define('X', "return 'from-source'")` then `.K~new~x` is oracle rc 0 `from-source`, and this crate
is rc 120 with `rexx-exec: method "X" of class "K" is not implemented (Phase 5)`. The same holds for
a `Method` object argument. The method is *reflected* correctly; it is the send that fails. Loud
rather than silent, so it is smaller than the trace row above.

This is on this task's list rather than Task 3's: Task 3 owns the per-object machinery
(`setMethod`, `unsetMethod`, `enhanced`), while this is a class-side `~define` whose effect was
simply unmeasurable in 5a, when a class was the only receiver a message could reach. It became
reachable when Task 1 landed `~new`, which is also what falsified `a4ee9284f`'s commit-message claim
that "No send this phase can make reaches an instance-side entry" -- true when written, false since
Task 1, and an instance of this project's recurring boundary-prose failure.

Its sibling, **`~defineMethods` after construction**, is measured (oracle rc 0, `a 0` / `b 1`) and
**cannot get a corpus witness until `.Directory~new` exists**, so it is listed here and not built
here.

**`TRACE`'s rendering of an instance.** `RexxActivation::traceValue` renders through
`stringValue()`, which is an `OBJECTNAME` send, so a class overriding `defaultName`, `objectName` or
`string` changes what a value line prints. Measured, `trace i` over `o = .K~new` with `::METHOD
defaultName` returning `overridden`: the oracle prints `>M>   "NEW" => "overridden"` and this crate
prints `>M>   "NEW" => "a K"`, rc 0 on both sides with the same stdout -- a silent wrong answer that
no gate row is near. Task 1 made the three *message* paths send (`Object~objectName`,
`Object~string` and the required-string protocol's last limb) and left this one, because
`Interp::string_value_text` is infallible and `Interp::intermediate_text` and
`Interp::result_text` answer `Option<Vec<u8>>`: a send can raise, so closing it means making the
trace rendering path fallible from the emit sites down. That is the shape of the fix; the cost was
not measured.

**`USE STRICT ARG` inside a `::METHOD` body.** `USE STRICT ARG` inside a
`::METHOD` body reports the routine error where the oracle reports the method one: measured at HEAD
with a class receiver alone -- `::METHOD m CLASS` carrying `use strict arg v`, sent as `.K~m` -- the
oracle is rc 163, `Error 93 ... Incorrect call to method.` / `Error 93.901:  Not enough arguments for
method; 1 expected.`, and this crate is rc 216, `Error 40 ... Incorrect call to routine.` /
`Error 40.3:  Not enough arguments in invocation of M; minimum expected is 1.` The traceback frames
above the error agree; only the error itself does not. It is a 5a defect reachable without any of
5b's code, and Task 1 widened its reach: with `~new` landed, a bare `~new` on `Alarm`,
`CaselessColumnComparator`, `ColumnComparator`, `File`, `InvertingComparator`, `Stream`, `Ticker`
and `TimeSpan` reaches a prologue class's `::METHOD init` and reports the same wrong pair where it
used to refuse loudly. The too-many-arguments direction was not measured.

**This crate's own error-message substitutions**, which is the same infallible-rendering line the
`TRACE` one is on. Measured, `o = .DN~new` with `::METHOD defaultName` returning `overridden`, then
`o~zzz`: the oracle is rc 159 `Error 97.1:  Object "overridden" does not understand message "ZZZ".`
and this crate is rc 159 `Error 97.1:  Object "a DN" does not understand message "ZZZ".` The same
program without the override agrees byte for byte. Loud rather than silent, so it is smaller than
the trace one, and the same fix reaches both.

**A `::METHOD string` override and a truth test.** The oracle takes the `IF`/`WHEN`/`WHILE`
condition through the required-string protocol, so an override decides it; this crate renders
through `Interp::to_text`, which does not send. Measured, `o = .K~new` with `::METHOD string`
returning `'1'`, then `if o then say 'yes'; else say 'no'`: the oracle is rc 0 `yes` and this crate
is rc 222 `Error 34.1: ... found "a K".` The same shape with `~objectName=` giving a numeric name
agrees on both sides, which is what says the gap is the send and not the truth test. Loud in this
direction and it cannot be silent -- the derived rendering is an article and a class id, which is
never `0` or `1`.

**An `objectName` or `string` override that answers an object.** Measured, `::METHOD objectName
return self` then `say o~objectName`: the oracle is rc 251 `Error 5 ... System resources
exhausted.`, this crate rc 0 `a K`; and the same with `return .MS~new` where `MS` has a
`makeString` (oracle rc 251, crate rc 0 `a MS`). `native_string` sends `OBJECTNAME` and renders the
answer with the infallible `Interp::string_value_text` instead of re-entering the protocol, so it
stops after one hop where the oracle recurses until the stack goes. **Recorded, not to be built**:
matching an oracle stack overflow is not a target, and this row is here so the audit does not read
the divergence as an unowned defect.

**A non-continuing `FORWARD` that resolves back to the method it is in.** Found and ruled before
Task 4 built `FORWARD`; the oracle's half is `corpus/oracle-crashes.txt`'s own entry and was not
re-run. The oracle **SIGSEGVs**, rc 139, both descriptors empty, because its non-continuing path
stops the activation before it sends -- `settings.setForwarded(true)`, `stopExecution(RETURNED)`,
then `messageSend` (`execution/RexxActivation.cpp:1365`-`:1391`) -- so the recursion never grows the
depth its own resource guard counts and the C++ stack goes instead. This crate sends with the
forwarding activation still on the stack, so `Interp::enter_method_body`'s `MAX_ACTIVATION_DEPTH`
guard counts it: measured on the crate alone, both engines, `o = .K~new ; say o~m` over
`::METHOD m` whose body is `forward` is **rc 245** with empty stdout and a traceback ending
`Error 11.1:  Insufficient control stack space; cannot continue execution.` That is the same clean
answer the oracle itself gives the *bounded* shape, `forward continue`, and the same one ordinary
unbounded Rexx recursion gets on both sides. **Recorded, not to be built**: matching an oracle stack
overflow is not a target, and this row is here so the audit does not read the divergence as an
unowned defect. It is also the reason no corpus program contains a bare `FORWARD`.

**That rc 245 holds only where no `REPLY` comes first, and the Task 4 fix round measured the other
half.** On the crate alone, both engines, `timeout -s KILL 60`, the oracle never handed any of them:
a mutual pair (`a` forwards to `b`, `b` forwards to `a`) and an explicit `forward to (self)
message('M')` are both rc 245 in under 0.08 s, and **the same two shapes with a `reply` in front of
the `FORWARD` do not terminate** -- rc 137 at the timeout, stdout and stderr both empty, `VmRSS` flat
at 17,760 kB across 24 s of sampling. A `gdb` stack of the hung interpreter thread is 24 frames and
does not grow: `Interp::run_deferred_replies` -> `Interp::resume_reply` -> `Interp::run_activation`
-> `Interp::exec_forward` -> `Interp::send_message` -> `Interp::enter_method_body`. So the resumed
body forwards, the forward re-enters the method, that invocation replies and queues another deferred
body, and the queue refills as fast as it drains -- no activation depth grows and
`MAX_ACTIVATION_DEPTH` never fires. Measured identical against a `c9c730ba7` extract, so it is not
the fix round's. **No bound was invented**: every member of this family is a shape the safety rule
forbids handing to the oracle, so a cap on the deferred-reply chain would be behaviour with nothing
to validate it, and a cap wide enough to be safe for ordinary `REPLY` programs is a number chosen
rather than measured. Whether to take one is the controller's and Moritz's, and until it is taken
**this crate's answer to the self-forward shape is rc 245 without a preceding `REPLY` and
non-termination with one**.

**A translation error inside `INTERPRET` loses the offending clause's own traceback line.** Found at
Task 4 while probing past its rows and **pre-existing**, established two ways rather than assumed:
`interpret "guard on"` inside a method has the identical one-line difference with no `FORWARD` in it,
and the pre-Task-4 pinned binary `bench-baselines/pinned/rexx-run-f558ea501` produces the same
two-line traceback for the `FORWARD` program. Measured, both engines, `interpret "forward
message('OTHER')"` inside a `::METHOD`: both sides rc 157 with the same `Error 99.923: INTERPRET data
must not contain FORWARD.`, and the oracle's traceback carries three clause lines --
`forward message('OTHER')`, then `interpret "..."`, then the sending clause -- where this crate
carries the last two. Loud, not silent. It belongs to whoever owns `INTERPRET`'s parse-failure
reporting rather than to `FORWARD`.

**`.Array~of` is not implemented and `FORWARD ARGUMENTS` is a way to reach it.** Measured at Task 4,
both engines, three descriptors: `forward message('OTHER') arguments (.Array~of(7))` inside a method
is rc 120 `rexx-exec: method "OF" of class "Array" is not implemented (Phase 5)` against the oracle's
rc 0. Loud, not silent, and it belongs to whoever owns the `Array` class surface rather than to
`FORWARD` -- the same option over an array literal and over a plain value agrees byte for byte.

**Five more on `FORWARD`'s and `DELEGATE`'s own paths, all measured by the Task 4 fix round**, both
engines, three descriptors read separately, from a fresh empty directory. The first is silent and the
rest are loud.

* **A stem's tails reach `FORWARD ARGUMENTS` in the wrong order.** The count agrees and the items do
  not: `s.0=3` through `s.3='r'` then `arguments (s.)` is oracle `a seen 4 [0] [3] [2] [1]` against
  this crate's `a seen 4 [0] [1] [2] [3]`, rc 0 and empty stderr on both sides. The oracle's order is
  a walk of the balanced tree `CompoundVariableTable` builds -- `first()` takes the deepest left
  leaf and `next()` comes up from below (`classes/support/CompoundVariableTable.cpp:343`-`:425`) --
  so it depends on the order the tails were assigned in. This crate's `Body::Stem` keeps its tails in
  a `NameMap`, which is a `HashMap` and holds no insertion order at all, so reproducing it is a
  change to the representation of the hottest data structure in the interpreter and owes a
  performance sitting. Deterministic on the oracle, measured five runs. The crate orders by the
  oracle's own comparison, length then bytes, which agrees for a stem with no tails and for one with
  a single tail.
* **A `.StringTable` or `.Directory` as `FORWARD ARGUMENTS`.** Oracle rc 0 `a seen 1` for a
  one-entry `.StringTable` against rc 120 `one of the interpreter's own objects as FORWARD ARGUMENTS
  is not implemented (Phase 5)`. `HashCollection::makeArray` is `allIndexes`, whose order is the
  oracle's hash order, so this one carries D61's question as well as the conversion.
* **An instance whose class defines `makeArray`.** `requestArray` sends `REQUEST('ARRAY')`, which
  looks `MAKEARRAY` up in the behaviour and sends it (`classes/ObjectClass.cpp:1920`-`:1940`).
  Oracle rc 0 `a seen 2 [x] [y]` for a class whose `makeArray` answers a two-item array, against
  rc 120 `an instance of a user class as FORWARD ARGUMENTS is not implemented (Phase 5)`. An
  instance whose class defines **no** `makeArray` now agrees: 98.946 at rc 158 on both sides.
* **A `.List` and a `.Queue` as `FORWARD ARGUMENTS`**, each blocked behind a class surface this
  phase has not built: a `.List` or `.Queue` is oracle rc 0 `a seen 2 [p] [q]` against rc 120 on
  `~new`. Re-measured at Task 9 on both engines, unchanged.
* **A multi-dimensional array as `FORWARD ARGUMENTS`, and this row said the wrong thing.** It read
  "`arguments (.array~new(2,2))` is oracle rc 158 98.946 against rc 120 on `~new`", which was true
  when written and stopped being true when Task 8 landed `.Array~new(2,3)`: `~new` now answers, and
  the send goes through. Re-measured at Task 9, both engines, three descriptors: oracle rc 158
  `Error 98.946:  FORWARD arguments must be a single-dimensional array of values.` against **rc 0
  and `a seen 0`** here. So the row is no longer a loud refusal with an oracle answer beside it, it
  is a **silent wrong answer**, which is the defect class this phase's constraints call the worst
  and no gate sees. It has three siblings on the same mechanism, all found by Task 9's walk and all
  silent: `o~sendWith('M', .array~new(2,2))` is oracle rc 168 `Error 88.913:  Argument message
  arguments must be a single-dimensional array.` against rc 0 `ran 0`, the same with a class
  receiver likewise; `o~startWith('M', .array~new(2,2))` is oracle rc 158 `Error 98.913:  Unable to
  convert object "an Array" to a single-dimensional array value.` against rc 0; and `~run`'s `'A'`
  style takes the same argument array. The C++ is one inline function for all four --
  `arrayArgument` rejects `array->isMultiDimensional()` after `requestArray`
  (`runtime/MethodArguments.hpp:675`-`:717`) -- and this crate's `Interp::array_slots_of` has no
  dimensionality check at all, so every caller of it that stands for an `arrayArgument` site
  flattens instead of raising. **Owner: whoever owns the `Array` argument-conversion surface**; it
  is one check in one helper and it is not Task 9's, which closed the allocation defect beside it
  and would have had to change `FORWARD`'s, `~sendWith`'s, `~startWith`'s and `~run`'s error
  numbers in one go to close this.
* **A `DELEGATE` whose variable is a stem or a single compound tail.** `::method m delegate a.b` with
  nothing assigned is oracle rc 159 `Object "A.B" does not understand message "M".` against rc 120,
  and `delegate a.` is the same with `A.`. The refusal now carries its own wording -- `a DELEGATE to
  the variable "A.B" is not implemented` -- where it used to borrow the generated accessor's, which
  named a site that is not this one. It needs the same scope-pool storage `Loud::compound_expose`
  refuses, reached from a third direction.

**Found by Tasks 7 and 8, after this section was written, and re-measured by the controller before
being written down here.** All four are loud refusals at rc 120 with an oracle answer, so each is a
row this task either closes or gives a named owner. Measured on the oracle under the standard
wrapper from a fresh empty directory and on this crate's `ir` engine, three descriptors read
separately:

| program | oracle | this crate |
|---|---|---|
| `.Array~subclass('K')~new(2,3)~size` | rc 0, `6` | rc 120, `~new on a subclass of Array is not implemented (Phase 5)` |
| `m = .array~new(2,3) ; say m~dimensions` | rc 0, `2` and `3` on two lines | rc 120, `method "DIMENSIONS" of class "Array"` |
| `s = 'abc' ; say s~copy` | rc 0, `abc` | rc 120, `method "COPY" of class "Object"` |
| `.Message~new(.Object~new, 'STRING')~class~id` | rc 0, `Message` | rc 120, `method "NEW" of class "Message"` |

`~dimensions` answering on two lines is the plural form returning an array, which `SAY` renders
through `makeArray`'s newline join -- not two `SAY`s, and worth stating because a reader checking the
row will otherwise read it as a transcription error.

Task 8 owns the first two by subject and declined both with its reasons in its report: a `Body::Array`
carries no behaviour handle, which is what a subclass instance needs. Task 7 owns the second two and
declined `Object~copy` for a string, an array, `.nil` and any `Body::Native` because a copied short
string lives in the handle rather than the arena, so what its `~identityHash` should answer is a
question the arm would have to settle first.

**`condition('A')` is not implemented, and it is the reason an error's substituted argument positions
cannot be compared differentially.** Found by Task 8, which had to carry six substituted texts in two
unit tests because a corpus program can compare an error's *number* but not its substitutions. This
is the one item on this list whose absence changes what kind of witness later tasks can write, rather
than what a program answers, so it is worth an owner on those grounds rather than on its own size.

**Also declined by Task 7 with reasons in its report**, listed so the audit does not read them as
unowned: `~run` given a `Class~method` object (such an object carries no route to a body a send can
enter); `arrayArgument`'s `requestArray` conversion for `~sendWith`, `~startWith` and `~run`'s `A`
option, which is the crate's standing position for that helper and is why the
`sendWith(.nil,.nil)`/`startWith(.nil,.nil)` pair could not be a corpus row; and `~start`'s
scheduling, which D68 gives to Phase 6.

**Found by Task 9's own walk, each measured on both engines with three descriptors read separately
from a fresh empty directory.** The multi-dimensional `FORWARD ARGUMENTS` row above is the fourth and
is written there rather than here because it is a correction to a row that already existed.

* **An array size the allocator cannot satisfy aborted this crate.** `.array~new(999999999999999)`
  was `memory allocation of 15999999999999984 bytes failed` at rc 134 with no traceback, against the
  oracle's `Error 5 running <file> line 1:  System resources exhausted.` at rc 251 -- which a
  `SIGNAL ON SYNTAX` traps, so a program that answers on the oracle died here. Four paths reached it:
  `~new` with one extent and with several, a write past the end, and a write past a dimension.
  **Closed by Task 9** with `Vec::try_reserve_exact`, which is the mechanism
  `Raised::system_resources`'s own doc already names as the crate's way of asking the allocator
  rather than a threshold, and which `builtin.rs`'s result buffers already use.
  `corpus/lang/array_allocation_refused.rex` is the witness, and the control is sharp: with the guard
  reverted the corpus harness itself dies, `SIGABRT` at rc 101, because it runs the program in
  process.
* **`Package~classes` is not implemented and `.Array~package` reaches it.** `.Array~package~classes~items`
  is oracle rc 0 `67` against rc 120 `method "CLASSES" of class "Package"`. Loud, not silent.
  **Owner: whoever owns the `Package` class surface.**
* **`setMethod` given a source that is not a string, an array or a method object refuses where the
  oracle raises.** `self~setMethod('Z', .nil, 'BOGUS')` inside a method is oracle rc 163 `Error
  93.974:  The method argument must be a string, array, or method object.` against rc 120
  `a method source that is neither a string nor an array is not implemented (Phase 5)`. Loud, not
  silent, and the arm is the one `Loud::method_from_source` takes at `dispatch.rs:4213`. It is not
  closed here because the same arm is what a `Method` object would reach once `.Method~new` exists,
  and answering 93.974 for it would be a wrong answer then. **Owner: whoever lands `.Method~new`.**
* **`setMethod` sent to a class object from one of its own class methods.** The program is

  ```rexx
  say .K~cm
  say .K~zz
  ::class K
  ::method cm class
    self~setMethod('ZZ', "return 'from-set'")
    return 'done set'
  ```

  oracle rc 0 printing `done set` then `from-set`, against rc 120 `a receiver with no scope of its
  own is not implemented (Phase 5)`. **Sent from the main body instead it is 98.991 on both sides**,
  which is the access check rather than this gap, so the sender being the receiver is what the row
  turns on. `RexxObject::checkRestrictedMethod` passes because the
  sender is the receiver, and this crate keeps a per-object dictionary in `Body::Instance` and has
  nowhere to put one on a class object. Loud, not silent, and it is `Loud::object_method`'s own
  disclosed gap reached from the one direction that gets past the access checks. **Owner: whoever
  owns per-object methods.**
* **`Package~publicClasses`, and it is the REXX package that diverges, not the method.** The
  program is

  ```rexx
  say .Array~package~publicClasses~class~id
  ```

  oracle rc 0 `StringTable` against rc 120 `the REXX package's class table is not implemented
  (Phase 5)`. **The neighbour that bounds it, and a reader will try it first:**
  `say .context~package~publicClasses~class~id` is `StringTable` on both sides with empty stderr, so
  `PUBLICCLASSES` itself is implemented and reaching it on the *running program's* package agrees.
  `native_package_public_classes` splits on `which_package`, and only `Package::Rexx` refuses
  (`dispatch.rs:5057`). `.Array~package~name` is `REXX` on both sides, so getting the REXX package
  object is not the gap either. A third spelling measures neither: `~publicClasses~items` is rc 120
  on the program package too, because `StringTable~items` is a separate gap.
  **Owner: whoever owns the `Package` class surface**, together with `Package~classes` above.
* **A quoted compound `::ATTRIBUTE`'s generated accessor reads the same on an instance as on a
  class.** `o~"A.B"` over `::attribute "A.B"` is oracle rc 0 printing `A.B` against rc 120
  `a generated accessor for the attribute "A.B" is not implemented`.
  `Loud::accessor_variable`'s own doc already measured the class-side half and carries no owner
  string by design: the scope-pool storage it needs is what `Loud::compound_expose` and
  `Loud::delegate_variable` also need, and nothing has been scheduled to build it. What Task 9 adds
  is that `~new` landing did not widen the refusal -- the instance side is the same answer, so the
  three refusals stay one gap reached from three directions rather than becoming four.

**Confirmed unchanged by Task 9's re-measurement**, so no later reader has to re-run them: the
`~define`-then-send row, the `defaultName` override in a 97.1's substitution, the `::METHOD string`
override under a truth test, `USE STRICT ARG` in a `::METHOD` body (**and on the instance side too**,
which was the unmeasured half -- `.K~new~m` over `use strict arg v` is oracle rc 163 93.901 against
rc 216 40.3, the same wrong pair the class receiver gets), `.Array~of` through `FORWARD ARGUMENTS`,
`~dimensions`, `'abc'~copy`, `.Message~new`, `.Array~subclass('K')~new`, the stem tail order, a
`.StringTable` as `FORWARD ARGUMENTS`, a `.List` and a `.Queue`, and the `DELEGATE` stem variable.
`.Alarm~new` is the `USE STRICT ARG` row reached through a prologue class, as this section predicted:
oracle rc 163 `Error 93.901:  Not enough arguments for method; 2 expected.` against rc 216 `Error
40.3:  Not enough arguments in invocation of INIT; minimum expected is 2.`

**Derive the enumeration from the tree; do not choose it.** "5a's landed error and refusal sites"
names no artifact, and a walk over three sites would be complete, recorded, and would name its
enumeration. Candidates that exist and can be listed by a command: the distinct `Loud::` constructors
reachable from `rexx-exec/src`, the `pub(crate) fn` constructors in `rexx-exec/src/error.rs`, and the
`Raised::` sites 5a added. Pick one, quote the command that produced it in the report, and commit the
derived list.

**All three of those candidates were measured at Task 9 and all three are wrong as counts of the
surface**, in both directions and each for its own reason, so a later reader taking one of them as
the answer would take a number no command reproduces. `Loud::`'s grep counts `Loud::parse`, which
exists only inside two comments saying it was deleted; `error.rs`'s `pub(crate) fn` count includes
accessors that build no error; and `Raised::`'s grep misses every `Raised` built by a helper not
spelled `Raised::` at its call site, of which `run.rs` and `trace.rs` hold a family. What Task 9
derived instead is **definition sites**: every `fn` in `crates/rexx-exec/src` whose return type is
`Loud` or `Raised`, with `#[cfg(test)] mod` blocks excluded, which is a different set from any of the
three and from their union. `corpus/refusal-sites.tsv` is that list and
`crates/rexx-exec/tests/refusal_sites.rs` re-derives it on every run.

**Done when** the derived list is committed, the walk covers it, every divergence it found is closed or
has a named owner, and **a test asserts the walk covers the derived list** -- so a site added later is
red rather than unwalked. Prose saying "all of them" is what this project has shipped wrong
repeatedly; an assertion over the set is the fix that holds.

---

## Task 10: the flip

**Goal.** `5b` gates.

**Build.** `CLOSED_PHASES` in `rust/crates/rexx-exec/tests/gate_tables/mod.rs` gains `"5b"`.

**Done when** D65's criteria are met at one commit: every `5b` row in both tables `agree` with no
structural failure; every program in `corpus/phase-5b.txt` agrees on three descriptors on both
engines; Task 6's DEVIATION runs; `CLOSED_PHASES` holds `"5b"`; and the five gates pass with the
command that printed each figure quoted beside the figure.

**Re-run the eight axes** against `bench-baselines/pinned/rexx-run-f558ea501` and record the result,
interleaved rather than as two suite runs.

**The pin above was corrected on 2026-09-02 and the old name is a trap.** This section said
`rexx-run-15a1ffa98`, which `bench-baselines/PINNED.md` records as the **retired** 5a pin -- 153
commits stale by 2026-09-01 and replaced for exactly that reason. `rexx-run-f558ea501` is the
current 5b pin, built 2026-09-01 from a clean tree with `rustc 1.98.0`, and both binaries are still
present in `pinned/`, so measuring against the wrong one would have produced a plausible table
rather than an error. A sitting against the retired pin would also re-measure the whole 5a drift
this phase has already ruled on and attribute it to 5b.

**A sitting is runnable, and Task 8 declined one on a reason that does not hold.** Its report gives
"the 5a pin is retired", which is true and is not the question: the 5b pin is current and the
staleness test lists only this plan's own task commits. Task 9 caught this. Whether a sitting is
*worth* running is a judgement about what the phase touched; "there is no pin" is not available as
the reason. `~new` puts an allocation and a message send on paths that
had neither, so a regression here is invisible unless it is looked for and the figure belongs in the
record whichever way it goes.

**And 5b turns one parked bench program live -- one, not three, which is what checking the refusals
rather than the exit statuses shows.** All three of `bench-programs/dispatch.rex`, `alloc.rex` and
`heapshape.rex` exit 120 today, but at different messages:

```
dispatch.rex    method "NEW" of class "Object" is not implemented (Phase 5)
alloc.rex       method "OF"  of class "Array"  is not implemented (Phase 5)
heapshape.rex   method "NEW" of class "Array"  is not implemented (Phase 5)
```

So **`dispatch.rex` goes live with Task 1**. `heapshape.rex` clears `.array~new` with Task 8 and then
needs `.directory~new` and `Directory`'s `[]=`, neither of which 5b owes. `alloc.rex` needs `Array~of`
and `String~new`, neither of which is 5b's -- `String` is one of the twenty-three this plan sends to
5c -- and stays parked. `dispatch` and `alloc` are in `rexx-bench`'s benchmarked list already;
`heapshape` is in `NOT_BENCHMARKED` because it reports its own timing.

Two consequences:

* **A program that starts running cannot show a regression against a baseline in which it did not
  run.** Its first crate figure is a new baseline row, not a comparison, and must be recorded as one.
  Saying "no regression on eleven axes" when three of them have no prior figure is the shape this
  project calls a ratio flattered by its denominator.
* **The stand-in decision is `dispatchclass` versus `dispatch` only.** `dispatchclass`'s header says
  it "does not replace dispatch.rex or relieve whichever task unblocks it", so decide and record
  whether it stays an axis beside `dispatch` or is retired -- leaving both unexamined double-counts a
  dimension in every later comparison. `alloc4c`'s header says it "is not a substitute for a
  collection-forcing measurement once message sends land", but `alloc.rex` does not go live in this
  phase, so `alloc4c` stays until whichever phase lands `Array~of` and `String~new`. The new-baseline
  paragraph above covers one program, not three.

---

## At the close of 5b: the comment policy tightens

**Moritz, 2026-09-01.** *"I want to enforce an update to comment policy: must be brief, never more
than a sentence and a short paragraph. the code speaks for itself, and you can read code faster than
a human. no results, no reasoning, no alternatives. these ... go to design documents."*

This tightens the 2026-08-27 rule already in `rust/CLAUDE.md` rather than replacing it. That one
said what a comment may contain (one-sentence overview, parameters, returns, panics, and properties
the implementation does not show). This one adds a **length bound** and names three things that are
now out regardless of how they are phrased: **results, reasoning, and alternatives considered.**

**It takes effect at 5b's close, not before.** Tasks 6 through 10 run under the current rule; a
policy change mid-phase would have every reviewer applying a different standard to work already
briefed, and comment-only fix rounds on this project have a measured defect rate of their own.

**The scale, measured (`find crates -name '*.rs' \( -path '*/src/*' -o -path '*/tests/*' \)` piped
to an `awk` run-length count over lines matching `^[[:space:]]*//[/!]`), at 09d2e2e57:** 5,204
doc-comment blocks, of which **2,334 (45%) exceed six lines** and 1,379 exceed ten; 25,171
lines sit above a six-line cap; the longest single block is 166 lines, in
`rexx-exec/src/lib.rs`. Source doc comments are 39,670
of `crates/*/src`'s 126,537 lines, with another 7,709 in `crates/*/tests`. **This is not a task; it
is a phase's worth of work**, and it must be scheduled and scoped as one rather than attached to
Task 10's flip.

**Two things the policy needs decided before anyone edits a file.**

1. **Where the load-bearing prose goes.** Some comments in this tree are not commentary, they are
   the authority on a measured shape, and deleting them destroys evidence rather than noise. The
   worked example is `corpus/lang/uninit_instance_collected.rex`'s header, whose three-row margin
   table records which clauses are load-bearing in the oracle's save-stack window; the controller
   reconstructed that shape wrongly twice in one sitting and was one ruling away from calling a true
   finding false, and that comment is what settles it. Under the new rule that table moves to a
   design document -- it does not evaporate. The destination that already exists is
   `docs/superpowers/records/<plan>/`, beside the DEVIATION files. **The rule as written has no
   destination clause; adding one is part of adopting it.**
2. **Whether the bound is enforced mechanically.** Moritz said "enforce". A prose rule does not hold
   on this project -- three review rounds missed 104 bad rows that running the rule found in one
   pass -- and a length cap is one of the few comment properties a machine can actually check: the
   run-length count above is the whole implementation. The alternative is reviewer judgement, which
   is what the current rule already relies on and which produced the 45%.

**The counter-consideration, stated because it is real and not to argue against the rule.** Every
edit to prose is a chance to make prose wrong. A comment-only fix round on this project introduced
three false statements, one of them a new defect inside the very comment being rewritten. A pass over
2,334 blocks at that rate is a large number of new false statements in exchange for shorter files, so
**deletion is safer than rewriting**: a block that is over length and carries nothing a design
document needs should be cut to its first sentence, not paraphrased into a shorter one.
