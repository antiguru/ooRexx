# Phase 8 — the native API, L2 slice, implementation plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development to
> implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Load a shared library, call the methods it exports, and reach L2 -- `ooTest.frm` loads
and one test group executes.

**Architecture:** A new crate, `rexx-api`, holds the C boundary. Two of its modules are granted
`unsafe` and nothing else is: `load.rs` opens libraries and calls through resolved function
pointers, `ffi.rs` exports the entry points and turns a caller-supplied context pointer back into
our own state. Between them sits ordinary safe Rust -- `#[repr(C)]` interface tables, a conversion
table keyed by the extension's own declared types, and a handle registry in which a stale
`RexxObjectPtr` is a lookup miss. `rexx-exec` gains the directive paths that reach all of it and
loses the refusals that stand in for them today.

**Tech Stack:** Rust; a new crate `rexx-api`; `rexx-exec` (`lib.rs`, `dispatch.rs`,
`dispatch/native.rs`, `dispatch/package.rs`, `require.rs`), `rexx-core` (`body.rs`), and one new
dependency, `libloading` 0.8.9.

**Spec:** `docs/superpowers/specs/2026-09-14-phase-8-native-api.md`, whose evidence base is
`docs/superpowers/specs/2026-09-14-phase-8-scoping.md`.

**This plan covers the L2 slice only.** The scoping survey ruled the order and the spec's section 1
records it: the slice `rxregexp` needs comes first, and the remaining surface -- the other 211
function pointers, `testbinaries/`, and the eight ooTest API groups -- gets its own plan document
written after L2 is measured. That is not a deferral of the gate; it is a refusal to specify the
generalisation before the first real extension has run against the design. Task 11 writes it.

## Global Constraints

* **`unsafe` lives in exactly two files**, `rust/crates/rexx-api/src/ffi.rs` and
  `rust/crates/rexx-api/src/load.rs`, each block carrying a `SAFETY:` note that names the invariant
  it relies on and who establishes it (D-U1, Moritz 2026-09-14). Everything past either boundary is
  safe Rust on `ObjRef`. `crates/rexx-core/tests/unsafe_sites.rs` is the record and must be updated
  in the same commit as the first grant, never after.
* **One new dependency, `libloading` 0.8.9**, and no other. Phase 7's "nothing beyond `rustix`" was
  that phase's own constraint and does not bind here; this one replaces it.
* **The headers are frozen.** No edit to anything under `api/`. A change that seems to need one
  reopens D5 as a Section 1 decision and stops the task.
* **The C++ tree is read-only**, and so are `samples/`, `build/`, `ootest/` and `oodocs/`. Phase 8
  reads `build/lib/librxregexp.so` and must not rebuild it -- loading the oracle's own compiled
  extension is the instrument (D5 amendment, 2026-09-14), and a rebuild throws it away.
* **No process-global state is mutated**: no `std::env::set_var` / `remove_var`, no
  `set_current_dir`, no write to file descriptor 0, 1 or 2 from interpreter code. A test that needs
  a loader search path passes an absolute path or sets the variable on a child it spawns.
* **Correctness is byte-identical stdout, stderr and exit status against the C++ oracle**, read on
  **three separate descriptors, never `2>&1`**. Oracle runs use the standard wrapper
  `( ulimit -v 1048576; LD_LIBRARY_PATH=/home/moritz/dev/repos/ooRexx/build/lib /home/moritz/dev/repos/ooRexx/build/bin/rexx FILE )`
  from a fresh empty directory.
* **Probe hazards.** Read `rust/corpus/oracle-crashes.txt` before an unusual probe and never run its
  entries. Never `select; when 1 = 0 then; when 2 = 2 then nop; end`. Never `NUMERIC DIGITS` above
  1000. Never `.Package~new` on a file inside the repository. Never `::OPTIONS TRACE ?<letter>`,
  which blocks indefinitely. A symbol `x` or `b` directly followed by a quote is a hex or binary
  literal. Put `timeout` on both sides of any probe that loops or reads input.
* **A loaded library is not unloaded while anything it produced is reachable.** An extension's
  `UNINIT`, its `CSELF` block and any function pointer we hold all die with the library, so unload
  ordering is a correctness constraint and not a tidiness one.
* **Four gates, all green at the closing commit:** `cargo fmt --all --check`;
  `cargo clippy -j 4 --workspace --all-targets -- -D warnings`;
  `cargo test -j 4 --release --workspace --no-fail-fast -- --test-threads=8`;
  `REXX_CORPUS_GATE=1 cargo test -j 4 --workspace --no-fail-fast -- --test-threads=8`. Record each
  exit status unpiped and the count of `ok` result lines. **Commit before a long gate run and leave
  the tree frozen until it finishes.** Wait on a running gate in ~1-hour stretches, not 10-minute
  polls.
* **Comments are minimal** (`rust/CLAUDE.md`, Moritz 2026-08-27): a one-sentence overview,
  parameters/returns/panics, and properties the implementation does not show. No narrative, no
  history, no counts of in-repo sets.
* **Every extent claim is derived and its command committed.** No "all", "none", "every" or a count
  in prose without the enumeration that produced it.
* **Write each negative control's prediction before running it**, and mark each part confirmed,
  falsified, or unobservable.
* **A method that exists and does nothing is not implemented.** Build the body or leave the name
  refusing loudly.
* **A refusal names the phase that owes the work.** `Phase 10` for RXAPI, the external queues and
  the RexxUtil remainder; `Phase 9` for the embedding API. After the surface half closes, no
  refusal names `Phase 8`; during the L2 slice, the ones this plan does not reach still may.
* Never `git add -A`; stage explicit paths. Never amend; never force-push; never
  `git reset --hard`; never `git checkout -- <path>` on an edited file; never bare `git stash` /
  `git stash pop`. Commit messages are written to a file and passed with `-F`.
* Scratch files live in the session scratchpad, never in the repository.

---

## Task order

```
1 (crate + grant)
  ↓
2 (load a library) ──┐
3 (the C structs) ───┼──→ 5 (conversion table) → 6 (two-call invocation) → 7 (context functions)
4 (handle registry) ─┘                                                          ↓
                                                                    8 (directives) → 9 (UNINIT)
                                                                                ↓
                                                                    10 (witnesses, measure L2)
                                                                                ↓
                                                                    11 (close the slice)
```

Tasks 2, 3 and 4 are independent of each other and all three depend only on Task 1.

---

### Task 1: The crate, the grant, and the dependency

The grant is the phase's first visible act because D-U1 requires it to precede any `extern "C"`
entry point. This task writes no boundary code; it makes the boundary legal and asserts the new
policy.

**Files:**
- Create: `rust/crates/rexx-api/Cargo.toml`, `rust/crates/rexx-api/src/lib.rs`,
  `rust/crates/rexx-api/src/ffi.rs`, `rust/crates/rexx-api/src/load.rs`
- Modify: `rust/Cargo.toml` (the workspace dependency table)
- Modify: `rust/crates/rexx-core/tests/unsafe_sites.rs`

**Interfaces:**
- Produces: the crate, and the two module names every later task writes into.
- Consumes: nothing.

- [ ] **Step 1: Create the crate** with `lints.workspace = true`, `libloading = "0.8.9"`, and the
      licence header every file in this tree carries. `lib.rs` declares both modules and nothing
      else yet.
- [ ] **Step 2: Put `#![allow(unsafe_code)]` at the top of `ffi.rs` and `load.rs`** with a one-line
      comment naming D-U1 and the date, and no `unsafe` yet.
- [ ] **Step 3: Update `unsafe_sites.rs`.** The `granted` list gains the two new paths; the `uses`
      list does not change, because no block exists yet. Read the test's own comment first: the
      needles are spelled with `concat!` so the file is subject to its own rule, and that must stay
      true.
- [ ] **Step 4: The negative control.** Predict, in writing, what happens when a third
      `#[allow(unsafe_code)]` is added to any other file. Add one, run the test, confirm it fails
      naming the file, remove it. A grant record that cannot detect an ungranted site is not a
      record.
- [ ] **Step 5:** `cargo fmt`, `cargo clippy -p rexx-api --all-targets -- -D warnings`,
      `cargo test -p rexx-core --test unsafe_sites`.
- [ ] **Step 6: Commit.**

---

### Task 2: Open a library and read its package entry

**Files:**
- Modify: `rust/crates/rexx-api/src/load.rs`
- Create: `rust/crates/rexx-api/tests/load.rs`

**Interfaces:**
- Produces: `load::Library` (an opened library plus its `RexxPackageEntry`), `load::open(name) ->
  Result<Option<Library>, Failure>` where `Ok(None)` is "no such library" and is **not** an error,
  and `load::Library::method(name) -> Option<NativeMethodEntry>`.
- Consumes: Task 1's module.

The C++ path is `common/platform/unix/SysLibrary.cpp` and
`interpreter/package/LibraryPackage.cpp`, both read on 2026-09-14 and quoted in the spec's
section 3.

- [ ] **Step 1: The name search.** `lib<name>` plus the platform extension, `dlopen` with
      `RTLD_LAZY`; on failure a second attempt at `/usr/lib/lib<name><ext>`
      (`SysLibrary.cpp:88-95`). `libloading::Library::new` is the call; the two attempts and the
      decoration are ours.
- [ ] **Step 2: A failed load is not an exception** (`LibraryPackage.cpp:199-205`), and neither is
      a missing `RexxGetPackage` symbol (`:211-216`). Both answer `Ok(None)`; the caller decides
      whether that is an error. Getting this wrong turns a probe into a failure and is the reason
      the C++ carries a comment at each site.
- [ ] **Step 3: Read the package entry.** Call `RexxGetPackage`, then check `requiredVersion`: if
      non-zero and greater than ours, raise **98.982** (`Error_Execution_library_version`,
      `LibraryPackage.cpp:232-235`). Derive the interpreter version constant from
      `api/oorexxapi.h`'s `REXX_INTERPRETER_*` values rather than writing a number.
- [ ] **Step 4: The method table.** Walk `RexxMethodEntry[]` to its `REXX_LAST_METHOD` terminator,
      keyed by name. A name the table does not carry is **98.978**; a routine name, **98.979**.
- [ ] **Step 5: Tests against the oracle's own binary.** `build/lib/librxregexp.so`, by absolute
      path from the workspace root, loads; its package entry names `rxregexp`; its method table
      holds `RegExp_Init`, `RegExp_Uninit`, `RegExp_Parse`, `RegExp_Pos` and `RegExp_Match`, and
      the assertion is over the whole table rather than a membership check for each. **Do not
      rebuild the extension.**
- [ ] **Step 6: The negative controls**, predictions written first: a library name that exists
      nowhere answers `Ok(None)` and not an error; a real library with no `RexxGetPackage` -- use
      `libc.so.6` -- also answers `Ok(None)`; and the version check fires when handed a synthetic
      entry whose `requiredVersion` is above ours.
- [ ] **Step 7: Commit.**

---

### Task 3: The C structs, and the cast back to our own state

**Files:**
- Create: `rust/crates/rexx-api/src/layout.rs`
- Modify: `rust/crates/rexx-api/src/lib.rs`, `rust/crates/rexx-api/src/ffi.rs`

**Interfaces:**
- Produces: `#[repr(C)]` definitions of `RexxInstance_`, `RexxThreadContext_`,
  `RexxMethodContext_`, `RexxCallContext_`, the six interface tables, `ValueDescriptor` and
  `RexxPackageEntry`/`RexxMethodEntry`; and `ffi::owner_of(context)`, the recovery cast.
- Consumes: Task 1.

The layouts are in the spec's section 2, read from `api/oorexxapi.h` and
`interpreter/concurrency/ActivationApiContexts.hpp:58-95`.

- [ ] **Step 1: The public structs**, field for field and in order. Every field is a pointer or a
      `wholenumber_t` on this platform, so the layout is forced, but write it as the header has it
      rather than as something equivalent.
- [ ] **Step 2: The private wrappers.** Each is `#[repr(C)]` with the public struct as its **first
      field by value** and our back-reference second, exactly as `MethodContext` is
      (`ActivationApiContexts.hpp:71-75`). `ffi::owner_of` casts the public pointer to the private
      struct and reads the second field: the first `unsafe` in the crate, and its `SAFETY:` note
      names who guarantees the pointer came from us.
- [ ] **Step 3: The interface tables** as `static`s of `extern "C"` function pointers. Only the
      entries later tasks fill need a body now; the rest are a single shared stub that raises
      "not implemented (Phase 8)" so that a call to an unbuilt entry point is loud rather than a
      null dereference. **A table full of null pointers is the failure mode to design out here.**
- [ ] **Step 4: Layout assertions.** `size_of` and `offset_of` for every struct in step 1, as a
      test rather than a `const` block so a failure names the field.
- [ ] **Step 5: The negative control.** Predict, then confirm: swapping two fields of
      `RexxMethodContext_` reddens the offset test.
- [ ] **Step 6: Commit.**

---

### Task 4: A handle is a generation-tagged local reference, and a stale one misses

**Files:**
- Create: `rust/crates/rexx-api/src/handles.rs`
- Modify: `rust/crates/rexx-api/src/lib.rs`

**Interfaces:**
- Produces: `handles::Table` with `register(ObjRef) -> RexxObjectPtr`,
  `resolve(RexxObjectPtr) -> Option<ObjRef>`, `remove`, and `clear`, plus a global-reference table
  with the same shape and a different lifetime.
- Consumes: `rexx-core`'s `ObjRef` and its generation field.

D5 and the roadmap's risk register both turn on this, and the spec's section 5 says why: a bare
slot index held across a collection names whatever was allocated into the slot next, which is
memory-safe and answers the wrong object.

- [ ] **Step 1: The table**, one per activation, holding the `ObjRef`s a native call has been
      handed so the collector can see them. The C++ equivalent is
      `NativeActivation::createLocalReference` / `removeLocalReference` / `clearLocalReferences`.
- [ ] **Step 2: `resolve` checks the generation** and answers `None` when it does not match.
- [ ] **Step 3: The positive control first.** A live handle resolves to the object it was made
      from. Without this, step 4's test passes for a table that never resolves anything.
- [ ] **Step 4: The stale-handle test.** Register a handle, end the activation, force a collection,
      allocate enough to reuse the slot, and assert `resolve` answers `None`. Predict in writing
      what the test does if generations were dropped, then confirm by dropping the check locally.
- [ ] **Step 5: Roots.** The table is a root while its activation lives. Add it to
      `Interp::object_roots`'s exhaustive destructure so a future field cannot be forgotten -- that
      destructure is the project's E0027 safety net and this is exactly what it is for.
- [ ] **Step 6: Commit.**

---

### Task 5: The conversion table

**Files:**
- Create: `rust/crates/rexx-api/src/values.rs`
- Create: `rust/crates/rexx-api/tests/values.rs`

**Interfaces:**
- Produces: `values::to_native(ObjRef, code) -> Result<Value, Failure>` and
  `values::from_native(&ValueDescriptor) -> Result<ObjRef, Failure>`, driven by the `REXX_VALUE_*`
  codes.
- Consumes: Tasks 3 and 4.

`NativeActivation::processArguments` (`interpreter/execution/NativeActivation.cpp:219`) is the
reference, and its cases are the rows.

- [ ] **Step 1: Write it as a table from the start**, one row per `REXX_VALUE_*` code, even though
      this task fills five. The remaining cases are then rows for the surface half rather than a
      redesign, which is the whole reason the spec says so.
- [ ] **Step 2: The five L2 rows**: `int` as a return type, `CSTRING`, `OPTIONAL_CSTRING`, `CSELF`
      and `RexxStringObject`. `CSELF` reads the object variable of that name (`:294`) and is
      finished in Task 7; here it is a row that calls into a seam.
- [ ] **Step 3: `OPTIONAL_` tolerates an omitted argument** and the others do not: a required
      argument that is absent or unconvertible is **93.968**
      (`Error_Incorrect_method_signature`) for a method, **40.918** for a call.
- [ ] **Step 4: A `CSTRING`'s lifetime is the call**, so the bytes it points at must be reachable
      from a root for the duration. Say how in the code, and test it by collecting between the
      conversion and the use.
- [ ] **Step 5: Tests** per row, both directions, including the round trip for `RexxStringObject`.
- [ ] **Step 6: The negative control.** Predict, then confirm: a row deleted from the table makes
      exactly the tests for that row fail and no others -- if deleting one row reddens nothing, the
      table is not what drives the conversion.
- [ ] **Step 7: Commit.**

---

### Task 6: The two-call protocol

**Files:**
- Create: `rust/crates/rexx-api/src/invoke.rs`
- Modify: `rust/crates/rexx-api/src/load.rs`

**Interfaces:**
- Produces: `invoke::method(entry, receiver, args) -> Result<ObjRef, Failure>`.
- Consumes: Tasks 3, 4, 5.

`NativeActivation::run` (`:1264-1310`) is the reference and the spec's section 4 is the reading.

- [ ] **Step 1: The signature call.** Call the entry with a null `arguments` and read the returned
      `uint16_t[]` to its `REXX_ARGUMENT_TERMINATOR`: element 0 is the return type. Calling through
      the pointer is the second `unsafe` in the crate and belongs in `load.rs`, not here.
- [ ] **Step 2: Assert that the signature call observes nothing.** A test in which the receiver's
      arguments would be visible if they were passed, and are not. This is a property the C++ gets
      by construction and we do not.
- [ ] **Step 3: Fill the descriptors** from the signature via Task 5, then make the second call,
      then convert `arguments[0]` back.
- [ ] **Step 4: An entry that returns a signature longer than the argument array** is a bounds
      question, not a trust question: the C++ passes `MaxNativeArguments`. Reject rather than grow.
- [ ] **Step 5: The end-to-end test.** `RegExp_Init` from the oracle's own `librxregexp.so`, called
      with no arguments and then with one, from a test that builds a receiver by hand. This is the
      first moment the whole chain runs.
- [ ] **Step 6: Commit.**

---

### Task 7: The seven context functions, `.Pointer`, and `CSELF`

**Files:**
- Modify: `rust/crates/rexx-api/src/ffi.rs`, `rust/crates/rexx-api/src/values.rs`
- Modify: `rust/crates/rexx-core/src/body.rs` (if `.Pointer` needs a body)
- Modify: `rust/crates/rexx-exec/src/dispatch.rs` (`.Pointer`'s rows)

**Interfaces:**
- Produces: `SetObjectVariable`, `DropObjectVariable`, `WholeNumber`, `StringData`,
  `StringLength`, `NewPointer` and `RaiseException` behind the method-context table.
- Consumes: Tasks 3, 4, 5, 6.

These seven are what `extensions/rxregexp/rxregexp.cpp` calls, counted in the scoping survey's
section 2.

- [ ] **Step 1: Settle `.Pointer` first**, because the spec records it as unmeasured. The class is
      in `native_classes.rs` with `NEW` unsupported to match the oracle's 93.967. Measure what an
      instance is on the oracle -- `~string`, `~==`, what a `CSELF` pointer prints as -- and make
      the internally minted instance answer the same.
- [ ] **Step 2: `NewPointer` and `SetObjectVariable`** together are how `CSELF` gets stored, and
      the object-variable pool is `Body::Instance`'s `pools` (D40, one pool per scope). The scope a
      native method writes into is the one that matters and is easy to get wrong: measure it.
- [ ] **Step 3: The `CSELF` conversion row** from Task 5 reads that variable back
      (`NativeActivation.cpp:294`), and a missing one is not a crash.
- [ ] **Step 4: `StringData` and `StringLength`** hand out a pointer into a Rexx string. Its
      lifetime is the call, and the object must be rooted for that long -- the same constraint as
      Task 5 step 4, and the same test shape.
- [ ] **Step 5: `RaiseException`, `RaiseException0`, `RaiseException1`** raise the numbered
      condition in the caller's context. `rxregexp` uses `Rexx_Error_Incorrect_method`. Number,
      sub-number, message text and descriptor all as the oracle produces them.
- [ ] **Step 6: `WholeNumber` and `DropObjectVariable`**, which are the small two.
- [ ] **Step 7: Tests** per function, plus one that survives a collection between two calls on the
      same object -- that is the `CSELF` survival criterion from the spec's gate, and its negative
      control is running it with collection forced off.
- [ ] **Step 8: Commit.**

---

### Task 8: The directives that reach a library

**Files:**
- Modify: `rust/crates/rexx-exec/src/lib.rs` (`:785`, `:801-820`)
- Modify: `rust/crates/rexx-exec/src/dispatch.rs` (`:8311-8344`)
- Modify: `rust/crates/rexx-exec/src/dispatch/package.rs` (`load_library`, `:402-412`)
- Modify: `rust/crates/rexx-exec/src/dispatch/native.rs` (`handle_set`, `:217`)
- Modify: `rust/crates/rexx-exec/src/run/tests.rs` (the refusal expectations)

**Interfaces:**
- Produces: `::REQUIRES LIBRARY`, `::METHOD`/`::ATTRIBUTE EXTERNAL "LIBRARY ..."`,
  `::ROUTINE EXTERNAL`, `Package~loadLibrary`, `loadExternalMethod` and `loadExternalRoutine`,
  each reaching Task 2's loader.
- Consumes: Tasks 2, 6, 7.

- [ ] **Step 1: One resolution path**, used by every entry in the list above, so that a library
      named twice is loaded once and a failure reports the same way from all of them.
- [ ] **Step 2: Delete the refusals** these paths stood in for, and update the expectations in
      `run/tests.rs` that assert the refusal text. A refusal removed without its test updated
      passes silently until someone reads the test name.
- [ ] **Step 3: `handle_set`** is the one `Stream` entry point held back here; it needs the loader
      only in that it needed a library. Check whether it does, and if it does not, re-home it and
      say so.
- [ ] **Step 4: The `OPEN` list in `dispatch::native`'s
      `every_deferred_entry_point_names_an_open_phase`** keeps `"Phase 8"` until the surface half
      closes. Do not remove it here; the phase is not closed by this plan.
- [ ] **Step 5: Witnesses** in `corpus/lang/` for each directive form, and for the three load
      failures (98.978, 98.979, 98.982), each with its `sourceline_oracle` companion and a
      `phase-8.txt` row.
- [ ] **Step 6: Commit.**

---

### Task 9: A native `UNINIT`

**Files:**
- Modify: `rust/crates/rexx-api/src/invoke.rs`, `rust/crates/rexx-exec/src/` (the collector's
  uninit path)

**Interfaces:**
- Produces: a native `UNINIT` that runs at finalisation.
- Consumes: Tasks 6, 7, 8.

`RegExp_Uninit` is why this is in the L2 slice rather than the surface half.

- [ ] **Step 1: Establish the ordering constraint in code**, not in a comment: a library stays
      loaded while any object whose class it contributed a method to is reachable. State what
      happens if it does not, and make that state unreachable rather than documented.
- [ ] **Step 2: An `UNINIT` that runs during collection must not allocate.** Find out what this
      crate's existing Rexx `UNINIT` does about that and follow it; if it does nothing, that is a
      finding to record rather than a licence.
- [ ] **Step 3: A witness** in which a `RegExp` object goes out of scope, is collected, and its
      native `UNINIT` runs, compared against the oracle.
- [ ] **Step 4: The negative control.** Predict, then confirm: with the `UNINIT` dispatch removed,
      the witness diverges rather than passing quietly.
- [ ] **Step 5: Commit.**

---

### Task 10: The 20 `rxregexp` cases, and the L2 measurement

**Files:**
- Modify: `rust/corpus/phase-8.txt`, `rust/corpus/lang/`
- Create: `docs/superpowers/plans/phase-8-l2.md` (the measurement, written from the run)

**Interfaces:**
- Consumes: everything above.

- [ ] **Step 1: Run the 20 extracted cases.** `ls rust/corpus-l1/ | grep rxregexp` names them.
      Enumerate the failing set rather than counting it, and fix or record each member.
- [ ] **Step 2: Walk the L2 chain again**, the way Phase 7's close did: `ootest/testOORexx.rex`,
      then `ooTest.frm`, then one test group. Record what it stops at each time, including if it
      stops somewhere new.
- [ ] **Step 3: Write the measurement from the run**, not before it. If L2 is reached, name the
      group that executed and quote the command. If it is not, name the next blocker and which
      phase owes it -- **an unreached rung recorded honestly is worth more than a reached one
      asserted.**
- [ ] **Step 4: Commit.**

---

### Task 11: Close the slice, and plan the surface

**Files:**
- Create: `docs/superpowers/plans/2026-09-XX-phase-8-surface.md` (dated at writing)
- Modify: `docs/superpowers/plans/phase-4-exclusions.txt`,
  `docs/superpowers/plans/2026-07-27-rust-rewrite.md`

**Interfaces:**
- Consumes: Task 10's measurement.

- [ ] **Step 1: The gate readings** for the four gates, from the run, unpiped, with both failing
      sets enumerated and each member attributed to before or during this work.
- [ ] **Step 2: Write the surface plan** now that a real extension has run: the remaining 211
      function pointers grouped by what they touch, `testbinaries/`, and the six ooTest API groups
      that are not embedding. The two that are embedding are re-homed to Phase 9 with the spec's
      section 9 as the reason.
- [ ] **Step 3: Record what this slice leaves open** in `phase-4-exclusions.txt`'s KNOWN GAPS
      section, with transcripts, at the close rather than in a session message.
- [ ] **Step 4: Update the roadmap's row 8** with what is measured, and its `Rung` column with what
      L2 actually did.
- [ ] **Step 5: Commit.**
