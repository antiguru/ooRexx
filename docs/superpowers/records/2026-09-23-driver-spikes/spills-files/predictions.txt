s1 (run_ops_from answers Result<(), Failure>, the Flow handed back through Interp::escaped_flow; frees %r13 from the sret pointer)
  spill per nop 29 -> 27 (SP-1320 gone); x = y 45 -> 43
  nop -2/clause (-2.1%), assign -2/clause (-1.0%), rexxcps -0.1% (a few more per body entry for the field write and take, one register more per clause), varlookup/emptyloop/compound -0.5..-1%, arith/dispatch ~0
s2 (Chunk::position_at answers (bool, ClausePosition) with a zero position when untabled, so no Option payload is undefined on one arm)
  target: SP-876's loop-carried reload+store (the undef payload merged with last clause's), and the second positions-len compare
  spill per nop 29 -> 26..27; x = y 45 -> 42..43
  nop -2..-3/clause (-2..-3%), assign -2..-3 (-1..-1.5%), rexxcps -2/clause (-0.2%), other axes -0.2..-1%
s3 (on s2) (std::hint::cold_path() on the driver's `pc >= stop` exit, so the allocator stops placing that exit's argument reloads in the loop header)
  target: SP-1040 (chunk) and the SP+16 stack argument reloaded at the loop head and overwritten on the hot path
  spill per nop 23 -> 22 (and arg 1 -> 0); x = y 39 -> 38
  nop -2/clause (-2.2%), assign -2 (-1%), rexxcps -2..-3/clause (-0.3%: every outer op pays the head), emptyloop/varlookup -0.5..-1%
s4 (on s2) (the source-text test folded into the position table once per range: the driver looks positions up in a slice that is empty without source text, so the per-clause shortcut no longer reads `source`)
  target: the hot path's only use of `source` (%rbp's `test`), which may free %rbp for a per-clause value; the table's ptr/len become range invariants of the driver instead of chunk fields
  spill per nop 23 -> 21..23; x = y 39 -> 37..39
  nop -2..-4/clause (-2..-4%), assign -2..-4 (-1..-2%), rexxcps -0.2..-0.4%
s5 (on s2) (the region walked by `pc` itself over the whole stream, bound checked once per region, instead of a slice iterator; `pc` is already live in a callee-saved register across the function)
  target: the iterator's current pointer (SP-1320 on s2: stored per op in every arm, reloaded at the loop head) and its end (SP-1032), the largest rexxcps spill slots (10.5 per clause); c8 differed by keeping a second counter and bounds-checking every op
  spill per nop 23 -> 22..23 (the ops-len and end reloads may change); x = y 39 -> 34..36; rexxcps 86.3 -> 78..82
  nop -1..+1/clause (~0%), assign -3..-5 (-1.5..-2.5%), rexxcps -3..-6/clause (-0.3..-0.7%), varlookup/compound -1..-2%
