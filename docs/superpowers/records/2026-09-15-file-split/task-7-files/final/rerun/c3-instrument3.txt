127 of 127 units decode to identical literal lists; 813 decodable literals (string, byte string, char, byte, doc line) on the PRE side; 0 units where the tool's count and the independent lexer's disagree
whole-file literal multiset, PRE builtin/datetime.rs against POST builtin/datetime.rs + new files:
