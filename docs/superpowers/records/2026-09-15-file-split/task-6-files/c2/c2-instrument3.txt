40 of 40 units decode to identical literal lists; 325 decodable literals (string, byte string, char, byte, doc line) on the PRE side; 0 units where the tool's count and the independent lexer's disagree
whole-file literal multiset, PRE dispatch/native.rs against POST dispatch/native.rs + new files:
