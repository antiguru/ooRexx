233 of 233 units decode to identical literal lists; 474 decodable literals (string, byte string, char, byte, doc line) on the PRE side; 0 units where the tool's count and the independent lexer's disagree
whole-file literal multiset, PRE dispatch/hash.rs against POST dispatch/hash.rs + new files:
