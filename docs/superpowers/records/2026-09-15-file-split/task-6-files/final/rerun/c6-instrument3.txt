294 of 294 units decode to identical literal lists; 744 decodable literals (string, byte string, char, byte, doc line) on the PRE side; 0 units where the tool's count and the independent lexer's disagree
whole-file literal multiset, PRE dispatch.rs against POST dispatch.rs + new files:
