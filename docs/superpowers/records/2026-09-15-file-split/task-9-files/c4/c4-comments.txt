moved units: 15; names searched in backticks: 10
file tails searched: cursor.rs token.rs token/ token/cursor.rs token/cursor/

(a) comments naming a moved unit: 0

(b) comments naming a touched file: 0

(c) positional words inside the destinations and the parent: 7
  crates/rexx-parse/src/token.rs:35: /// The byte offset the error is *reported against*, which is the start of
  crates/rexx-parse/src/token.rs:169: /// A clause terminator: `;`, an uncontinued line end, or end of file.
  crates/rexx-parse/src/token.rs:187: /// An operator immediately followed by `=`, e.g. `+=`, which the
  crates/rexx-parse/src/token.rs:243: /// Whether a blank *following* this token can be significant:
  crates/rexx-parse/src/token/cursor.rs:40: /// before it reads any source, so their ids are fixed and every keyword
  crates/rexx-parse/src/token/cursor.rs:50: /// so an expression parser cannot walk off the end of its clause.
  crates/rexx-parse/src/token/cursor.rs:68: /// Index of the next token, or None at the end of the range.
