10 of 10 units decode to identical literal lists; 43 decodable literals (string, byte string, char, byte, doc line) on the PRE side; 0 units where the tool's count and the independent lexer's disagree
whole-file literal multiset, PRE run/interpret.rs against POST run/interpret.rs + new files:
