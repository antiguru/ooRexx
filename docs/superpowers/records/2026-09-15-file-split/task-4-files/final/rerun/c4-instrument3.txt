270 of 270 units decode to identical literal lists; 1254 decodable literals (string, byte string, char, byte, doc line) on the PRE side; 0 units where the tool's count and the independent lexer's disagree
whole-file literal multiset, PRE lib.rs against POST lib.rs + new files:
