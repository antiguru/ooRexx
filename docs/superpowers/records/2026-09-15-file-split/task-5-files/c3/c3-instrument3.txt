58 of 58 units decode to identical literal lists; 287 decodable literals (string, byte string, char, byte, doc line) on the PRE side; 0 units where the tool's count and the independent lexer's disagree
whole-file literal multiset, PRE builtin.rs against POST builtin.rs + new files:
