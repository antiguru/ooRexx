89 of 89 units decode to identical literal lists; 426 decodable literals (string, byte string, char, byte, doc line) on the PRE side; 0 units where the tool's count and the independent lexer's disagree
whole-file literal multiset, PRE value.rs against POST value.rs + new files:
