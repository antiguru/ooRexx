79 of 79 units decode to identical literal lists; 409 decodable literals (string, byte string, char, byte, doc line) on the PRE side; 0 units where the tool's count and the independent lexer's disagree
whole-file literal multiset, PRE parse_template.rs against POST parse_template.rs + new files:
