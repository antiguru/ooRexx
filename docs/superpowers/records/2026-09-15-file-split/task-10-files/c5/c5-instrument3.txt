153 of 153 units decode to identical literal lists; 551 decodable literals (string, byte string, char, byte, doc line) on the PRE side; 0 units where the tool's count and the independent lexer's disagree
whole-file literal multiset, PRE docs/classes.rs against POST docs/classes.rs + new files:
