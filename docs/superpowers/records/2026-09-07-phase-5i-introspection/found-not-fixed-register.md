# Found and not fixed: the register Phase 5i exits owing

Every item here was measured during Phase 5i, is **not** Phase 5i's to fix, and has an owner.
Recorded at Moritz's request so the phase cannot close by forgetting them.

| # | What | Evidence | Severity | Owner |
| --- | --- | --- | --- | --- |
| 1 | **A message send in a multi-argument builtin's argument list corrupts that call's argument run.** `say substr(v, 1, length(.Array~id))` **panics and aborts the interpreter** at `run.rs:6151`; `say right(v, .Array~id~length)` reports `RIGHT` itself as missing an argument. The oracle answers both at rc 0. **It is already the cause of the only two `diverge` rows in `corpus/method-bodies.txt`** -- `DateTime~date` and `~timeOfDay`, whose `CoreClasses.orx` bodies are `date('F', self~standardDate, 'S')` and `time('F', self~LongTime, 'L')`. | `found-defect-builtin-argument-run.md` | **panic**, plus two live `diverge` rows | Phase 4 executor |
| 2 | **D59's four consequences**, reclassified from licensed to defects when Moritz corrected D59 on 2026-09-08: `~subclasses` retaining a dropped class; a `WeakReference` to a dropped class still answering it; `Interp::class_variables` as a permanent root; a class's `UNINIT` running later than the oracle's. D60 reopens with D59. | `d59-correction-and-exit-obligation.md` | leak + four observables | **after Phase 5i, before any new work** |
| 3 | `header_number` (`run.rs:8674`), `controlled_step_wide` (`:10379`) and `RAISE ADDITIONAL` (`:5417`) refuse loudly where the oracle answers `97.1`. Since Task 5 they are the **last** places a class object refuses. Closing them means teaching `run.rs` the operator send. | Task 5's report | loud, never wrong | Phase 4 executor |
| 4 | `.VariableReference~new` diverges: oracle rc 163 / `93.967`, this crate rc 120. One line with Task 2's `Raised::unsupported_new_method`. It is no table's row, which is why it was homeless. | Task 2's report | one line | whichever phase owns `VariableReference` |
| 5 | **Upstream candidate, one signal.** An `::ATTRIBUTE ... GET`/`SET` with a body reports its `~source` **one line late**, because `LanguageParser::hasBody` consumes the first clause without restoring the scanner's line position -- a three-line body answers `Array(2)`. This crate mirrors it deliberately. | Task 4's report | upstream | needs two more signals before filing |
| 6 | `stackFrames` taken inside an `INTERPRET` sees **four** frames here and **five** on the oracle, because a fragment runs inside the enclosing activation and pushes none. `~type`'s `INTERPRET` and `COMPILE` values are unreachable here. | Task 6's pre-flight | declared divergence | executor, if ever |
| 7 | The `Supplier` from `~methods`/`~instanceMethods` iterates in **hash order** on the oracle and **name order** here. **RESOLVED as a method rather than a divergence, Moritz 2026-09-08: sort both sides' output and compare the sorted output.** See below. | Task 5's report | method exists | Phase 5i, for stdout |
| 8 | `StackFrame~executable` and `RexxContext~copy` are `Setup.cpp` rows in neither table, left loud and named rather than missed. | Task 6's pre-flight | loud | a later phase |
| 9 | **A trapped condition consumes a `~invocation` id on the oracle and none here.** Building a condition object builds stack frames, and `CONDITION('O')` is not implemented, so this crate mints nothing there. Measured, oracle rc 0, two programs differing only by a trapped `SIGNAL ON SYNTAX`: the routine's own id reads `2` without it and `3` with. **Downstream of `CONDITION('O')` and retires with it** rather than needing a fix of its own. | Task 6's report | rc 0 both sides | whoever implements `CONDITION('O')` |
| 10 | **`RexxContext~condition` refuses inside a handler.** The `.nil` arm is answered and is what both tables probe; the other arm needs the condition `Directory` that `CONDITION('O')` would build. Measured, oracle rc 0 `Directory`, this crate rc 120. Recorded in `corpus/refusal-sites.tsv` as `Loud::builtin_option_object` `diverges`, with its own witness. | Task 6's report | loud | whoever implements `CONDITION('O')` |
| 11 | **`~thread` is wrong NOW, not in Phase 6.** `Object~start` is not refused -- it runs the send -- so a `::method` that `REPLY`s is on another system thread on the oracle and on the same one here. Measured, rc 0, stderr identical: after the reply the oracle answers an id different from the main thread's `1` and this crate answers `1`. **The id is not a fixed number** (it counts threads the interpreter has touched, and two runs of the review read `2` and `3`), so the divergence is that it differs at all. `~interpreter` is the same shape one level up and stays correct while there is one instance. | Task 6's report, fix round 1 | **live divergence**, rc 0 both sides | Phase 6 / D12 |
| 12 | **`dispatch` and `dispatchclass` must be measured against the phase's starting commit, not against any task's BASE.** Task 6 moved them +1.166% and +1.092% `instructions:u`; a per-step threshold structurally cannot see a total, and Phase 5a passed every per-step gate under 1%/2% and summed to +32%. Task 6's residue is bounded from below by two isolation builds -- the clause snapshot 0.08pp, the convention snapshot 0.38pp -- so a redesign starts from "the `Rc` conversion of `CallContext.arguments` and the 48 bytes `Activation` grew". | Task 6's report | measured | Task 9 |
| 13 | **A repeated function-form call site loses the routine name under the IR engine, which is the default.** From the **second** execution of one call site onward, `RexxContext~name` and `StackFrame~name` answer the null string; the `CALL` form is unaffected and the tree-walker is correct throughout. The site is `Interp::run_call_args` (`ir/drive.rs:339`): `spelling` starts as `b""` and is filled **only** in the arm that misses the resolved-call cache, so every later execution passes the empty one into `CallContext.name`. Its own comment states the assumption that made this safe -- *"no part of that path reads it"* -- which was true until this task added the first reader. **Older than BASE** (`3f20212fc`, 2026-08-20; `git diff d8b013026 c2139a45b -- crates/rexx-exec/src/ir/` is empty), so the code is not this task's; the observable is new because the reader is. Reproducer, oracle rc 0 printing `RTN` six times: `do i = 1 to 3; say i '[' \|\| rtn() \|\| ']'; end` then the same loop with `call rtn` / `result`, over `::routine rtn; return .context~name`. Under `REXX_ENGINE=ir` the first loop prints `RTN`, `` and ``. | Task 6's fix round 1 | **wrong answer**, rc 0, default engine | Phase 4 executor |
| 14 | **A third missing-frame class: a library method this crate implements natively produces no frame where the oracle's Rexx-coded one does.** Wherever `CoreClasses.orx` implements a method in Rexx and this crate implements it in Rust, `~stackFrames` is one frame short. Measured, rc 0 both sides, a comparator's `compare` reached through `Array~sortWith`: the oracle answers **3** frames -- `METHOD COMPARE`, `METHOD SORTWITH`, `PROGRAM` -- and this crate answers **2**, without `SORTWITH`. The frames this crate *does* produce are byte-identical, so it is the same **missing**-frame shape as row 6 rather than a wrong one, and the reviewer checked the other side of the boundary: a genuinely Rexx-coded library method does produce its frame. It follows the native/Rexx boundary and moves whenever that boundary moves. | Task 6's fix round 1 | rc 0 both sides | whoever narrows the native/Rexx boundary |
| 15 | **`DO OVER` over one of the interpreter's own `Body::Native` tables binds a DEAD handle.** `Interp::hash_collection_indexes` allocates one index string per key and `push_temp`s each; that is not enough to keep one alive to the loop body. **Older than BASE**: reproduced at `59d25e938` in the gate worktree, with no `Package` method in the program. Reproducer, which PANICS at `Interp::not_in_arena` (`a live value`) under `run_program_collect_every_alloc`: `call dump 'm', .methods` / `exit 0` / `dump: procedure` / `use arg label, table` / `do name over table` / `say label '['name'] is a' table[name]~class~id` / `end` / `return`, over three `::method`s named `AVeryLongMethodNameOne`..`Three`. The **same program with the methods named `MA`, `MB`, `MC` prints all three and exits 0** -- a name at or below `INLINE_TEXT` (7) is not an allocation at all, so the boundary is where the fault becomes VISIBLE, not where it begins. Reaches `.METHODS`, `.ROUTINES`, `.RESOURCES` and every table `Package`'s ten readers answer -- any program with a directive and a loop -- and **not** a collection a program builds itself (`.StringTable~new`, `.Directory~new` and `.array~of` with equally long members all print every entry at rc 0). **Observed only under collect-on-every-allocation; whether an ordinary run can open the window was NOT ATTEMPTED, which is not the same as no.** A durable root fixes it (measured); one `Array` holding the items, `push_temp`ed, does not (measured). Where the temporary stops protecting the object was **not established** -- inferred from doc comments, never run -- so no `run.rs` site is named. A fix is `RootSet::park`/`release` tied to `LoopState::OverItems`' lifetime, in **both** engines. | `found-defect-do-over-native-table-rooting.md` | **use-after-free**, stress-mode only so far | Phase 4 executor / whoever owns loop control |
| 16 | **The `StringTable`s these rows answer are HALF-REAL: the right class with the wrong body.** A package table is a `Body::Native` -- a class, a rendering and a `HashMap` -- where `.StringTable~new` is a store-backed `Body::Instance`, and the collection methods are bound to the store-backed shape. Measured 2026-09-08, oracle rc 0 for all ten: on `p~classes`, `~items`, `~hasIndex('KA')`, `~allIndexes`, `~supplier` and `~makeArray` are each **rc 120** `method "X" of class "StringTable" is not implemented (Phase 5)`, while the same five on a `.StringTable~new` holding one entry all answer, and `DO OVER` and `[]` answer on both. **Inherited from Phase 5h** -- `.METHODS`, `.ROUTINES` and `.RESOURCES` have had this body since that phase and it was undisclosed -- but Tasks 7+8 multiply its reach: nine more readers now answer an object of this shape. **The refusal is process-terminating, not a trappable condition**, which is what it costs a user: measured 2026-09-08 on both engines, a program wrapping `p~classes~items` in `signal on syntax name caught` prints its line before the send, dies at **rc 120** with the loud message on stderr, and reaches neither the handler nor the clause after it -- where the oracle prints `answered 1` and runs on to the end at rc 0. So this is not a refusal a program can route around; every collection-returning row in this phase answers an object that ends the process when it is asked its size. **It is a fourth way an instrument here goes green over a wrong answer, and the widest**: `corpus/introspection-arity.tsv` compares `vv~string`, which is `a StringTable` for both a real one and a half-real one, so **every collection-returning row in the phase can read `agree` while the object it answers cannot be asked its size**. The three already recorded are: the value comparison being blind to an object's class; a row being green over a receiver on which the defect cannot arise; and a gate reading exit codes being blind to a wrong answer at rc 0. | Tasks 7+8's fix round 1, at the reviewer's finding | **rc 120 where the oracle answers**, on five methods of every package table | Phase 5h's owner, and flagged for **Task 9** |
| 17 | **`Object~copy` refuses on every receiver whose body is not an instance, an `Array` or a `Stem` -- a plain String among them.** `native_copy` (`dispatch.rs:7558`) answers for `Primitive::Instance`, `Primitive::Array` and `Primitive::Stem` and is `Loud::native_method(b"COPY", "Object")` for the rest. Measured 2026-09-08 at `532cbe2cf`, one program per receiver so no refusal hides another, both engines identical: `'abc'~copy`, `42~copy`, `.K~method('MM')~copy`, `.routines['R']~copy`, `.context~package~copy`, `.context~package~classes~copy`, `.context~stackFrames[1]~copy` and `.methods~copy` are **rc 120** where the oracle answers rc 0; `.RexxInfo~copy` is rc 120 where the oracle raises at rc 163; `.nil~copy` is rc 120 where the oracle raises at rc 159. It is **untrappable**: a `signal on syntax` around the send does not see it and the process ends. `.Object~new`, an `.array~of`, a `.StringTable~new`, a `.Directory~new`, a `WeakReference`, a `Supplier` and both class arms agree. **`corpus/method-bodies.txt` records `Object copy instance answers rc 0`** -- the row's receiver is `.Object~new`, the one shape that works. The refusal SITE is already recorded as diverging in `corpus/refusal-sites.tsv`'s `native_method` row, with `'abc'~copy` as one of its witness expressions; what is new is its reach and the fact that a green `answers` row sits over it. **Found by the close's adversarial sweep**: eleven `Object` sends over seventeen receiver shapes, 187 combinations, oracle and both engines on three descriptors -- every divergence in that sweep was `copy` and nothing else, which is also the bound. | Task 9's report | **rc 120 where the oracle answers**, untrappable, reaches a String | the phase that owns `Object~copy` |
| 18 | **A zero-length `Rc::from` on the hot call path, downstream of row 13.** `run.rs:1446` builds `Rc::from(&self.call_context.name[..])` for every non-method activation. From the second execution of a function-form call site onward the IR engine passes an empty name (row 13), so the common case at that site allocates a header for a zero-length slice -- the trap Task 6 measured twice and fixed twice. Re-measured 2026-09-08 at the tip: row 13's reproducer still prints `RTN`, `` and `` under `REXX_ENGINE=ir` and `RTN` three times under `tree-walker`, and that empty name is the Rc. **Fixing row 13 removes it**; a guard at the site would remove the allocation without fixing the wrong answer, so the site is not the place to change. The rest of the `(Rc\|Arc)::from\(` sweep over `crates/*/src` at `HEAD` is cold or already guarded: `Interp::shared_arguments` tests `is_empty` first, `empty_arguments` is built once per interpreter, `Rc::from(arguments)` in the program bootstrap and the `::CONSTANT` directive's `Rc::from(&[][..])` run once per program and once per directive, `Trap`'s label and `AddressState::set_bytes` take non-empty names, and `SelectorTable::intern` allocates once per new spelling. | Task 9's report | allocation on a hot path | retires with row 13 |
| 19 | **The identity operators refuse on every Phase 5i class's instance, and on an `Array`.** `=`, `==`, `<>`, `><`, `\=` and `\==` reach `eval.rs`'s operand check (`eval.rs:1831`, `dispatch.rs:2108`) rather than the operator send, so a `Body::Native` operand is `Loud` "one of the interpreter's own objects" and an `Array` operand is `Loud` "applied to an array". Measured 2026-09-08 at `532cbe2cf`, one program per combination, both engines identical: over the receivers `.Object~new`, `'abc'`, `.array~of(1,2)`, `.StringTable~new`, `.K`, `.Array`, `.K~method('MM')`, `.routines['R']`, `.context~package`, `.RexxInfo`, `.context~package~classes`, `.K~methods`, a `WeakReference`, `.context~stackFrames[1]` and `.context`, **42 of the 90 combinations agree and the 48 that differ are exactly the six operators on `Array`, `Method`, `Routine`, `Package`, `RexxInfo`, a package table, `StackFrame` and `RexxContext`** -- the oracle answers `1`/`0` at rc 0 for every one of them, this crate is rc 120. `.Object~new`, a String, a `.StringTable~new`, a user class, a built-in class, a `Supplier` and a `WeakReference` agree. **Older than the phase**: the same two programs give byte-identical refusals under a binary built at `bc9d991cc`. **Tasks 3 and 5 both named this refusal and measured it** -- Task 3 could not assert `.RexxInfo~package == .Array~package`, Task 5 recorded that its change made class handles operator receivers and nothing else. What is new here is the **width** -- row 3's "the last places a class object refuses" is true and reads narrower than the fact -- and that **both tables are green over it**: `corpus/method-bodies.txt` records `Object = instance answers rc 163` and `corpus/introspection-arity.tsv` records all six `agree rc0`, because both probe `.K~new`, the one receiver shape on which the operators work. Closing it means teaching `eval.rs` the operator send, which is register row 3's own fix. | Task 9's report | **rc 120 where the oracle answers**, on every introspection instance and on `Array` | Phase 4 executor, with row 3 |

## Licensed, not owed

**`Object~hashCode` and `~identityHash` are licensed to diverge.** Moritz, 2026-09-08: *mirroring it
is not practical and limits our choices, and users shouldn't rely on it anyway.* Both are
address-derived; measured, **the oracle does not reproduce its own answer across two runs**, so
`corpus/introspection-arity.tsv` records them `unstable` and `corpus/method-bodies.txt` records them
`unstable  the oracle`. That verdict is the licence expressed in the instrument rather than in
prose, and `every_unstable_row_is_really_unstable` polices it by running the oracle twice and
failing if its two answers agree.

This is a **licence**, not a declined row: it is accepted permanently and nothing owes a fix.


## The unstable-output method: sort both sides and compare sorted

**Moritz, 2026-09-08:** *what we do elsewhere for unstable output is an option to sort the output,
and compare the sorted output.*

It is already in the tree for `stderr`: `StderrComparison::Multiset` and `stderr_multiset`
(`crates/rexx-exec/tests/support/oracle.rs:640`, `:657`), chosen per program by
`stderr_mode` (`tests/corpus.rs:408`) from `CONCURRENTLY_TRACED`, and licensed as **DEVIATION 7** in
`corpus/phase-4-exclusions.txt` -- for the one program whose two threads write trace lines to one
descriptor, where the interleaving is not a specified observable. One implementation, one control,
opt-in by path.

**There is no stdout counterpart, and that is what the remaining work needs**, because a witness
that prints a collection prints it to stdout.

**Where it applies.**

* **The `Supplier` order** (item 7). Task 5 declined to assert it and asserted membership and count
  in Rexx instead. Sorted comparison is strictly stronger: a witness that prints *counts* passes
  while the members differ, and one that prints every member and is compared sorted does not. This
  turns a declared divergence into an asserted property.
* **`Package`'s table rows, and this is the timely one.** The merged Task 7+8 has not started and
  will witness `classes`, `routines`, `publicRoutines`, `importedClasses`, `importedRoutines`,
  `resources`, `namespaces` and `definedMethods` -- every one answering a `StringTable`, whose `~allIndexes` order is hash order on both
  sides and reproduces on neither across interpreters. Without this it writes another hand-rolled
  membership assertion or declares another divergence.

  **Done, and it landed smaller than this predicted.** Tasks 7+8 built the stdout counterpart --
  `StdoutComparison`, `stdout_multiset` and `descriptor_diff_modes` in `tests/support/oracle.rs`,
  `HASH_ORDERED_STDOUT` and `stdout_mode` in `tests/corpus.rs`, licensed as **DEVIATION 8** -- with
  a control that holds the opt-in list in **both** directions: an entry's raw stdout must differ, so
  a program that does not need the relaxation cannot sit there, and its sorted stdout must agree, so
  a content difference is not hidden. **That control took two of the three candidate programs back
  off the list**, and the licence ends the phase with one entry, `lang/package_writes.rex`. The
  other two are compared byte for byte: row 15 forced their index names below `INLINE_TEXT`, and
  short names happen to hash into the oracle's own order. `package_rexx.rex` reads its 67 names back
  from a list it carries rather than enumerating, for the same reason.

**What it must not become.** DEVIATION 7's discipline is the part to copy, not just its code: a
named licence, opt-in per program by path, with the scope and the controls written down and a
statement of what it does **not** cover. A sorted comparison hides a genuine ordering defect in any
program that opts in, so the opt-in list is the licence and each entry needs its reason. It is not a
default and no program should reach for it because its output happens to be awkward.
