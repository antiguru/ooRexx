# Phase 8 surface plan: execution records

Records of `docs/superpowers/plans/2026-09-14-phase-8-surface.md`, copied from
the git-ignored SDD workspace `.superpowers/sdd/2026-09-14-phase-8-surface/`
(ruling: SDD records are committed). `progress.md` is the ledger; each task has
its brief, report and reviews; `task-4-forge/` is the forged test extension
and its probes that Task 4 compared against the oracle (built from source by
`crate.sh` and `oracle.sh`, never committed as binaries). The review-package
diffs are not copied: they are regenerable from the commits the ledger names.
Files here are refreshed from the workspace as the plan proceeds.
