# SDD ledger -- plan: docs/superpowers/plans/2026-08-14-pre-phase-5-defects.md
Task 1: implementer DONE_WITH_CONCERNS, commit 3cf9fcaba (base 625d653fb).
Task 1: verified independently by controller -- rc 239, byte-identical to the oracle on both engines, all three descriptors.
Task 1: concern (scope) -- a second divergence at the same field (USE LOCAL first in a ::ROUTINE, 99.910/rc 157 here vs oracle 98.993/rc 158) was fixed in the same commit. Global Constraints say not to fix other divergences found. Not pre-judged; the reviewer sees it and I adjudicate after.
Task 1: concern (new defect, unfixed, reproduced by controller) -- a plain SIGNAL inside a CALLed label does not restore the trace indent; two spaces deep on both engines, rc 214 agreeing. No trap involved. Written into the plan as Task 2's instance three.
Task 1: finding that governs Task 2 -- DEVIATION 0's normalisation collapses exactly this indent class, so corpus.rs and trace_oracle.rs are blind to it. Written into the plan: Task 2's test must compare raw stderr, and its sweep count through the harness is not evidence.
Task 1: review dispatched (r1-procedure, sonnet).
Task 1: review clean on spec (Steps 1-8 all met, artefacts present) and on quality; reviewer independently re-derived owns_frame writer sites, Owner::Phase rows, the commit hash, and the absence of em-dashes and of any edit to roots.rs:409.
Task 1: RULING on the reviewer's one Important finding (USE LOCAL fixed in the same commit, against the "leave other divergences" constraint) -- ACCEPTED, not split. The constraint protects attribution, and attribution survives: each half has its own unit test and its own mutation witness (M1, M2), and the corpus sweep moved exactly one program. The alternative preserves a measured-wrong answer in a reader being rewritten and leaves a false doc claim standing. Splitting post-push costs a revert plus a re-land with the bug deliberately reinstated in between. Surfaced to Moritz with the option to overturn.
Task 1: plan amended -- Global Constraints now state the mechanically-forced-site exception and its limit, so Tasks 2-4 do not rediscover the same ambiguity (Task 2 has three instances of one defect).
Task 1: minor (deferred): run.rs:2570 method_invocation match returns false on every arm -- deliberate, forces a compile error when Entry::Method lands in Phase 5, documented at the site. For the final review to triage.
Task 1: complete (commits 625d653fb..3cf9fcaba, review clean, 1 accepted-with-ruling)
Task 2: implementer DONE_WITH_CONCERNS, commits 691266bd9 (fix) and 1638a0ea4 (record the sibling), base a48f00636.
Task 2: verified independently by controller -- signal_from_a_called_label and signal_on_syntax_raised_in_a_callee are byte-identical to the oracle on raw stderr, stdout and rc, both engines.
Task 2: it is TWO defects, not one; the brief's hypothesis is refuted in its second clause. SIGNAL must zero the indent (instances one and three); a DO/LOOP header clause ends one level deeper than it echoed when the body runs (instance two). Selective mutations are the evidence and the reviewer is asked to check the selectivity is per-case rather than asserted.
Task 2: a third direction the brief did not have -- a DO WHILE/UNTIL that ends the loop delivered its handler two columns TOO DEEP. Both directions now come from one call site.
Task 2: new defect found, not fixed, reproduced by controller on 1638a0ea4 -- a SELECT CASE expression's clause boundary delivers a handler two columns short (oracle 12 *-*     h:, ours 12 *-*   h:), both engines agreeing, stdout and rc clean. A WHEN condition's boundary agrees, which is the adjacent success. Added to the plan as Task 5, blocked on Task 2.
Task 2: review dispatched (r2-indent, opus), pointed at the two things the brief made central -- whether the two-defects evidence is selective or fitted, and whether the tests compare raw bytes and the two blast-radius counts are kept apart.
Controller error: setting task #2's owner via TaskUpdate delivered the tracker description to t2-indent as a fresh assignment. It correctly declined to redo the work. Do not assign tracker entries to live agent names.
Task 2: plan corrected -- instance one said "four spaces too deep" beside a one-call-level transcript. Measured against the oracle: the minimal form is TWO columns, and four needs the raise a further call level down. The wrong figure came from a controller probe with two call levels. Corrected, not hedged.
Task 2: review returned (r2-indent). No Critical, no behavioural defect. Reviewer independently checked the Step 1 transcripts against the committed .expected/.wrong bytes, the save/restore in invoke_call and the INTERPRET arm, every producer of Flow::Signal, the parse-time label rejection that makes clearing sufficient, that both engines share apply_flow and run_loop_with_header, and that ir_dual.rs compares stderr raw.
Task 2: reviewer's judgement on the two focus questions -- (a) the two-defects CONCLUSION is right but the per-case selectivity offered as proof is unobservable from the runs described; it survives on disjoint-path grounds instead. The hypothesis refutation is NOT fitted: the .expected/.wrong pairs show the crate wrong in both directions, which the old hypothesis cannot produce and one un-stepped counter can. (b) raw comparison is real and load-bearing, the two blast-radius counts are kept apart as the brief demanded.
Task 2: fix round 1/5 dispatched to t2-indent -- I1 (moved-case arithmetic contradicts itself, 6 cases x 2 engines is 12 not 10), I2 (sweep population claim does not match the tree; two different exclusions both land on 92 and neither is named), I3 (selectivity asserted but the harness's first failing assert ends the run), plus M1 (lossy string comparison in a file whose premise is byte-for-byte) included because it makes a claim in the tree false.
Task 2: minor (deferred): M2 every_case_ships_all_three_files enumerates *.rex so a case with no .rex is invisible to it.
Task 2: minor (deferred): M3 run.rs:6490 comment understates the site directly above the new call, whose removal turns tests red.
Task 2: minor (deferred): M4 shapes the fix repaired that nothing pins -- raise in a callee inside a loop body (four columns), SIGNAL ON handler calling a routine (ten lines).
Task 2: minor (deferred): M5 trace i pinned for instance three only.
Task 2: minor (deferred): M6 the .wrong files' provenance lives only in a gitignored report.
Task 2: minor (deferred): M7 settle_block_indent takes two same-typed indents side by side; a swapped call is exactly M2/M3, so only a type-level fix makes it unwritable.
Task 2: fix round 1/5 (4 addressed, 0 open; commit 3d4657377, harness only -- run.rs byte-identical to 691266bd9, verified by controller).
Task 2: re-review confirmed I1's root cause independently -- with five cases the sweep command really does return 92, and five cases x two engines really is 10, so one tree state reproduces both stale numbers; 103 minus 92 is 11 (bench-programs + bench-control), and BOTH of the reviewer's earlier 13-file reconstructions were wrong, which is the finding's own point restated.
Task 2: re-review checked all five mutation red sets against the source and each matches what the code predicts; M1's set and M2 union M3 are disjoint, which is Step 3's conclusion as a measurement rather than an assertion.
Task 2: fix round 1 introduced two new false counts. Sent as round 2 despite both being graded Minor, on the ground that a false claim is corrected rather than hedged whatever its impact -- (a) "{} of {} cases disagree" counts descriptors not cases and can print a numerator above the denominator, in the very artefact round 1 created; (b) Step 7's "M4 and M5 partition the loop cases by call site" is false, the union covers three of four.
Task 2: this instance of [[correction-rounds-introduce-false-statements]] -- behaviour was right on the first pass and the prose was not, both rounds.
Task 2: minor (deferred, added by re-review): git ls-files reads the index rather than HEAD and its second glob is redundant; doc-comment run-on at trace_indent.rs:244-247.
Task 2: fix round 2/5 (2 addressed, 0 open; commit a87682b4c, harness only -- run.rs still byte-identical to 691266bd9).
Task 2: re-review verified the M3/deletion claim two independent ways, including from the committed .expected bytes with nothing run -- same call site, opposite sensitivity in two cases, decided by which direction the UNTIL test took. Generalised: deletion-only mutation is blind wherever the deleted write is preceded by another write of the same value, and its red sets can look like a partition. Recorded in project memory.
Task 2: minor (deferred, from round 2): the &String in Vec<(&String, Vec<String>)> at trace_indent.rs:217 is collected and never read; "in 1 descriptors" has no plural agreement.
Task 2: complete (commits a48f00636..a87682b4c, review clean after 2 fix rounds, 0 open)
Task 3: implementer DONE, commit 84873b191, base a87682b4c. Controller verified against the oracle: do qq over zs for 1 now emits both >K> lines, byte-identical on all three descriptors, both engines.
Task 3: review clean on both verdicts. Reviewer independently re-captured all six non-OverFor variants from the live oracle and got byte-identical transcripts, reproduced the failing state, ran the golden IR test itself, re-ran the oracle on the loop-header-values row, confirmed 105 tracked .rex, and reproduced the mutation with all three catchers going red.
Task 3: the exhaustiveness claim is backed by the compiler, not by prose -- HeaderRole::keyword() is an exhaustive match with no wildcard arm.
Task 3: both forced sites checked rather than asserted -- golden_tests.rs shifts op indices because TraceKeyword emission is gated purely on role.keyword().is_some(), and loop-header-values reddens regardless of correctness once the tree-walker's output moves. Each carries its own witness.
Task 3: minor (deferred): no tracked corpus .rex contains DO OVER ... FOR, so the corpus differential never exercises the construct against the live oracle. Its only live-oracle coverage is the one pinned loop-header-values row and the new unit test's baked-in transcript. A future regression depends on those two artefacts rather than on the corpus machinery that catches most regressions here. Candidate: add a tracked corpus program exercising DO OVER ... FOR under trace. FOR THE FINAL REVIEW TO TRIAGE.
Task 3: complete (commits a87682b4c..84873b191, review clean)
Task 4: review returned (r4-pos). Code correct; reviewer re-ran everything falsifiable rather than reading it -- fmt, clippy, crate suite, corpus gate 53/53, ir_dual, all 16 corpus rows on both engines against the live oracle, its own 20x11 LASTPOS grid, and its own mutation witness (reverted find_forward -> 52 of 53, the one mismatch named as lang/pos_window.rex, diverging on exactly the predicted rows; restored from a copy, not git checkout --, back to 53/53).
Task 4: missing report graded Important, not Critical, because everything it should have carried was independently reconstructible. Step 1's POS table exists distributed across the doc comment, the unit tests and the corpus program, but as curated pairs rather than the systematic grid the brief asked for. Step 4's LASTPOS grid exists ONLY as a claim in the commit message -- no data anywhere in the repo; the reviewer's own grid stands in for it.
Task 4: BLOCKING QUESTION FOR MORITZ -- the task introduces a NEW POS-vs-oracle divergence, decided by the implementing task rather than by him. pos('a'||'00'x,'aa') is 2 on the oracle and 0 here. The Global Constraint and [[oorexx-accepted-engine-divergence]] both say MAX_EVAL_DEPTH is the single accepted divergence and that the licence stays narrow by decision. Not adjudicated by me; put to him.
Task 4: reviewer confirmed the divergence is bounded by the code's own arithmetic rather than by a hedge -- range is capped at haystack.len() - start, so the overrun reaches at most one byte past the end, so only the needle's LAST byte can ever need inventing, so the divergence can only manifest for a needle ending in '00'x searched to the end.
Task 4: the new oracle SIGSEGV (changestr('a'||'00'x,'aa','ZZZ'), rc 139) is recorded ONLY outside the repository. No in-repo crash registry exists to hold it.
Task 4: fix round 1/5, commit 6229d3776 -- find_backward's doc corrected (called false under the brief's own reading, cites StringUtil::lastPos as a fixed-count backward scan with no rescan), DEVIATION 3 recorded with the bound as arithmetic and a why-exact-agreement-is-unavailable section, and rust/corpus/oracle-crashes.txt created recording all four known oracle killers with causal accounts read from the C++ rather than by running them.
Task 4: controller verified the fix round changes no behavioural code -- the only non-comment additions to string.rs are test assertions.
Task 4: report-only addendum after that -- inert REXX_ORACLE removed from both places, concerns 3 and 4 marked closed citing the reviewer's independent work, and superseded sections marked so the document does not read as self-contradictory.
Task 4: OPEN QUESTION AGAINST MY OWN INSTRUCTION -- I told the implementer phase-4-exclusions.txt's header means a new DEVIATION row needs a set-assertion updated. It reports no such mechanism exists for DEVIATIONS (EXCLUSIONS has one via EXCLUDED/wholly_excluded() in coverage.rs and builtin_status.rs; DEVIATION 1 has no code enforcement at all). If true, that file's header claims an enforcement property the repo does not have -- a false claim in the document whose purpose is to stop quiet exclusions. Sent to r4-pos to check rather than accept.
Task 4: scoped re-review dispatched (r4-pos), also asked whether the new LASTPOS pins can fail, whether the crash file can do its job as prose, and to confirm its own witness closes the carried-open mutation item.
Task 4: fix round 1/5 re-review -- all four items ADDRESSED. Reviewer ran the gates fresh at 6229d3776 rather than relaying (fmt 0, clippy 0, crate suite 0, workspace 83 ok blocks / 0 FAILED, corpus 53 of 53), confirmed both function bodies byte-identical to 3026afc10, and PROVED the new LASTPOS pins can fail: it applied the mutation the new doc warns against (copying find_forward's overrun into find_backward), found a pre-existing assertion caught a crude version first, so moved the new assertions ahead of the old ones to isolate them and got lastpos('345','Y3Y345YYYYYY',8,4) returning 4 instead of 0. Restored from a copy, 18/18 again.
Task 4: the carried-open mutation-witness item is closed -- this round touched no line of either function body, so the reviewer's own 52-of-53 witness still stands.
Task 4: complete (commits 84873b191..6229d3776, review clean after 1 fix round + a report-only addendum, 1 pre-existing finding referred out)
Task 4: minor (deferred): oracle-crashes.txt's header names the cardinality of its own four entries. Reviewer argues same-file cardinality is low-rot-risk since a fifth entry means editing that file; the house rule carves out no same-file exception. FOR THE FINAL REVIEW TO TRIAGE.
REFERRED OUT, not Task 4's: phase-4-exclusions.txt:12-13 claims "the gate asserts the SET of both sections rather than a count". Verified false. Exactly one runtime read of that file exists in the workspace (builtin_status.rs:683) and it polices the KNOWN GAPS section, not DEVIATIONS. DEVIATIONS have no enforcement at all; DEVIATION 1 has none of any kind. EXCLUSIONS' check (coverage.rs:829-880) never reads the file -- it asserts count equality between a hardcoded Rust literal and numbers in the file's prose, so a name swapped in either source at constant counts passes. Dates to 7aff84f4a, the file's creation on 2026-07-30. Correctly out of scope for Task 4 and correctly flagged rather than patched.

## Task 5 review (r5-selectcase), and state at compaction

Task 5: implementer committed 932e6b710 (base 6229d3776) but sent no status line; report written. Controller verified against the oracle: SELECT CASE byte-identical on all three descriptors both engines, and two pre-existing PARSE probes unmoved.
Task 5: review met all six steps, no Critical. Reviewer REBUILT Task 2's control rather than believing the survey -- built a ctl binary with Task 2's behavioural change removed and a cur binary from HEAD, ran 11 probes on both engines: the eight "never had it" constructs stay clean on ctl (so not repaired probes) while do_header and loop_until go red on ctl (so the control is live). Also reproduced the raw sweep exactly (109 tracked .rex, 654 streams, 4 changed, all the two new cases x two engines), and re-ran M1, M6 and M7 with matching red sets.
Task 5: the WHEN adjacent success is pinned as a real case, and the reviewer proved it can fail (mutating condition_value reddens only that case, both engines).
Task 5: fix round 1/5 dispatched. Important -- with a handler that FAILS rather than returns, the enclosing frame's traceback line is two columns short at a SELECT CASE boundary, both engines, before AND after the fix; it is the SELECT CASE instance of the family already recorded at loop-header-boundaries:128 (a traceback quantity, not a transfer one), not caused by this fix, and written down nowhere. Plus three wording fixes in text this task added: "every call site" cardinality at run.rs:6236, "no program run so far" at run.rs:5495, and the report's false reason that interpreter/ is outside this repo.
Task 5: LESSON worth carrying -- every probe in the survey used a handler that RETURNS, so the instrument had a single shape, and a second observable found something on the first try.

## STATE AT COMPACTION -- what remains

1. t5-selectcase fix round 1 in flight (recording + three wording fixes). Then a scoped re-review by r5-selectcase over that fix diff only, then Task 5 completes and the plan's five tasks are done.
2. FINAL WHOLE-BRANCH REVIEW not yet run. Dispatch on the most capable model over review-package PLAN <merge-base> HEAD, and point it at the deferred-minor list below.
3. Then superpowers:finishing-a-development-branch.

## DEFERRED MINORS for the final review to triage

Task 1: run.rs:2570 method_invocation match returns false on every arm -- deliberate, forces a compile error when Entry::Method lands in Phase 5, documented at the site.
Task 2: case enumeration keys on *.rex so an .expected+.wrong with no .rex is invisible to every_case_ships_all_three_files.
Task 2: run.rs:6490's "boundary is unobservable on every probe tried" comment now sits above a load-bearing call whose removal reddens tests.
Task 2: shapes the fix repaired that nothing pins -- raise in a callee inside a loop body (four columns), SIGNAL ON handler calling a routine (ten lines), the do raiser() repeat-count row.
Task 2: trace i pinned for instance three only.
Task 2: .wrong provenance recorded only in the gitignored report.
Task 2: settle_block_indent takes two same-typed usize indents side by side; a swapped call is exactly mutations M2/M3, so only a type-level fix makes it unwritable. SUBSTANTIVE.
Task 2: git ls-files reads the index not HEAD, redundant second glob; doc-comment run-on at trace_indent.rs:244-247; the &String in Vec<(&String, Vec<String>)> collected and never read; "in 1 descriptors" plural.
Task 3: NO tracked corpus .rex contains DO OVER ... FOR, so the corpus differential never exercises it against the live oracle. Only coverage is one pinned ir_dual row and a transcript baked into a unit test. SUBSTANTIVE.
Task 4: oracle-crashes.txt's header names the cardinality of its own four entries.
Task 5: pre-existing set-cardinality prose at loop-header-boundaries:31 and :39.

## REFERRED OUT -- needs Moritz, not the final review

phase-4-exclusions.txt:12-13 claims "the gate asserts the SET of both sections rather than a count". Verified false and pre-existing (7aff84f4a, 2026-07-30). DEVIATIONS have zero enforcement; DEVIATION 1 has none of any kind. EXCLUSIONS' check (coverage.rs:829-880) never reads the file -- it asserts count equality between a hardcoded Rust literal and numbers in the file's prose, so a name swapped in either source at constant counts passes.
My recommendation, given to Moritz and not yet answered: (1) correct the header, mandatory, since a false claim in the document whose authority rests on that guarantee is worse than no claim; (2) build the EXCLUSIONS name-for-name check, cheap because the rows are builtin names in fixed columns with rexx_inventory::builtins::EXCLUDED as the code-side set; (3) do NOT build a DEVIATIONS set assertion -- deviations are numbered prose with no code-side list to compare against, and inventing a key to satisfy a sentence is reshaping the document to fit the claim; (4) instead ask whether each deviation names a test that pins it, since DEVIATION 1 apparently has none and a deviation nothing pins can quietly stop being true.

## After compaction

Task 5: fix round 1 committed 17d6dc6ec (base 932e6b710), two files, docs/ untouched. Controller verified all four items independently rather than relaying: built the failing-handler probe from scratch and got oracle rc 214 with exactly one differing stderr line on both engines (`     4 *-*   select case raiser()` vs `     4 *-* select case raiser()`), handler's own lines byte-identical; re-ran the DO analogue and got the identical one-line divergence on both engines; confirmed run.rs:1788-1791 really does state the "CASE or no CASE" fact the new comment cross-references; confirmed interpreter/ is git-tracked HERE (483 files) so the report's correction is itself true. Gates at 17d6dc6ec: fmt 0, clippy 0 (status read directly, not through a pipe), workspace suite exit 0 / 83 ok blocks / 0 FAILED.
Task 5: CONTROLLER ERROR worth noting -- first probe attempt used `raise user zx` inside a `procedure` with no RETURN value; the trap never fired and the oracle exited rc 0 silent, which reads exactly like "no divergence". A second attempt with a wrong binary path gave rc 127 while the stdout comparison still printed "identical" (both empty). Two different ways for a broken probe to look like a clean result, in one verification.
Task 5: scoped re-review dispatched to r5-selectcase over the fix diff only, with the verified facts handed over so it does not re-derive them, and three questions of its own: whether comment-only lines in an ir_dual case file are load-bearing or read as a pinned case that is not one, whether the rewritten 5492-5498 note dropped a measurement that was worth keeping, and any new defect.
Task 5: re-review came back with a CRITICAL, and controller reproduced every claim independently plus built a control at the plan's base 625d653fb. A plain SELECT's clause boundary DOES deliver a condition an earlier clause queued (handler ending `raise ... return`), two columns short, both engines -- so the comment fix round 1 added at run.rs:5494-5498 asserts there is nothing where there is something. Pre-existing: base and HEAD byte-identical on that probe.
Task 5: the discriminator, measured not argued -- the same program with `select case 1` DIVERGED at the plan's base and is BYTE-IDENTICAL at HEAD. The fix repaired this route for SELECT CASE because select_case settles unconditionally; plain SELECT is unfixed because nothing settles it.
Task 5: a WORSE sibling on plain `do`, which the reviewer under-called as an indent-family item. It is visible on STDOUT with no tracing at all: oracle `G ran 5 / body / after set 5`, ours `body / G ran 6 / after set 5`. Wrong delivery order AND wrong SIGL. Both engines, diverging at the plan's base too. Not caused by this plan. This is the only divergence in the whole plan observable without TRACE.
Task 5: Step 2's headline "SELECT CASE is the last of them" is refuted, and `plain_select_delayed`'s "never had it" is a probe artifact -- that probe raised the second condition inside an ordinary handler clause, which delivers within the handler and can never reach the following instruction. `raise ... return` is the carrying shape. The survey had one instrument again.
Task 5: fix round 2/5 dispatched -- correct the false comment and its pre-existing twin at run.rs:1791, correct Step 2's headline and the survey row, record both divergences, narrow the report's cmp claim, fix the transcript's implied program. NO behaviour changes: all pre-existing, Global Constraints say record not repair.
CONTROLLER ERROR: the control comparison for the plain-SELECT probe ran against filenames that did not exist and printed "DIVERGES from oracle" for both engines. A missing file reads exactly like a divergence. Caught only because the same table's other rows used a different naming scheme; redone with the right files, the answer flipped to "matches on stdout, differs on stderr, unchanged by plan".

## ORACLE IS PATCHED -- for Moritz, not for the final review

The differential oracle at /home/moritz/dev/repos/ooRexx is NOT stock. `interpreter/classes/NumberStringClass.cpp` carries an uncommitted local modification to `NumberString::copyIfNecessary()`: it forces a copy when `stringObject != OREF_NULL` and compares NUMERIC FORM via the constants rather than `isScientific() != form`. The built binary carries it -- `zn = 1e10; numeric form engineering; say zn + 0` prints `10E+9`, which is the patched answer; unpatched returns the cached string spelling. Source and binary share an mtime of Aug 5 16:02.
Consequence: every differential comparison in this rewrite has been against a patched ooRexx, and nothing in the repo or the memory index records that. Surfaced to Moritz.

CORRECTION to the block above: "nothing in the repo or the memory index records that" is FALSE. phase-4-exclusions.txt records the patch twice and at length -- lines 1788-1812 under a heading that says the oracle binary carries a local uncommitted fix and no result records which binary produced it, and lines 2325-2365 giving the sign-inversion cause at NumberStringClass.cpp:3652, the seven transcripts it changes, why only ABS reaches it, and the 6-of-92,880 measurement. It states "Moritz has confirmed the change is his and it stays." The measurement I made was right and the claim of novelty was wrong; the question I put to Moritz was already answered in the file this plan's own exclusions live in. Found only because the reviewer's cmp finding sent me to `diff -rq` and then to grep.
Task 5: fix round 2 committed 8b6989aa4. Verified by controller: run.rs and loop-header-boundaries are BOTH comment-only (0 non-comment changed lines each, checked mechanically), fmt 0, clippy 0, workspace suite exit 0 / 83 ok / 0 FAILED. The corrected sentence at run.rs:1790-1794 no longer says a plain SELECT has nothing that could have queued, and run.rs:5497-5507 now states the rule the measurement supports -- what decides is whether anything settled the boundary, never what the clause queued. Both recordings match my own probe transcripts line for line.
Task 5: residual sent back, Minor -- report:653-659's summary bullet still carried the UNNARROWED cmp claim after the primary sentence at :155-159 was correctly narrowed. Same shape as the recorded pattern: the correction lands at the finding and not at its copy. Asked t5 to sweep for other stale restatements of this round's corrections rather than just fixing the one I found.
Task 5: the residual was one of FOUR stale copies. t5 swept rather than patched and found three more, two of them inside round 1's own write-up of its corrections -- including a bullet that restated the false conclusion it was reporting the removal of, and a count of the report's own findings that later rounds had invalidated. All four corrected and marked. No commit: the report is untracked, tree unchanged, no source file touched in that pass.
Task 5: PATTERN NAMED, three rounds running -- the correction lands at the sentence the finding quotes, and the copies are wherever that sentence was SUMMARISED, which in a document that records its own fix rounds is systematically inside the section describing the fix. Written to memory.
Task 5: scoped re-review of round 2 dispatched to r5-selectcase over 17d6dc6ec..8b6989aa4, with the verified facts handed over and four questions: whether the corrected comments over-swing into a fix instruction, whether the DO recording is framed so it is ranked correctly, where THIS round's new false statement is, and whether the report's own sweep is right.
Task 5: round-2 re-review came back with one Important, and I REJECTED it after measuring. It said the DO entry's crate transcript third line should be `after set 6`; its reason -- "the only source of a 5 on that line is the delivering clause's SIGL" -- is false. That 5 is `zw`, what raiser() returned. t5's record matches my own runs exactly.
Task 5: but the reviewer was not careless, and the episode is the finding. It reconstructed a program from the recorded oracle triple and its reconstruction ALSO reproduces that triple -- ending `say 'after' zv sigl` instead of `say 'after' zv zw`. Measured both: identical oracle transcript, different crate transcripts (`after set 5` vs `after set 6`). Two programs, one oracle record. So the reviewer's Minor 1 (neither new entry names its program) is PROMOTED to the finding, with a proof that reading the entry could never have caught it.
Task 5: fix round 3/5 dispatched -- name the program in both entries, reword the "not to file beside these" heading that is then filed beside them, defer the file's drifted subject line to the whole-branch review, and record the rejected Important with both transcripts because it is a better argument for naming programs than any rule statement.
Task 5: round 3 committed 0a893bc31, both programs named in two-column layouts. Verified by REGENERATION rather than reading -- I reconstructed both programs from the record and ran them: plain SELECT has `select` on line 5 and `g:` on line 15 with `G ran 5` on both engines and stdout/rc identical; plain DO has `do` on line 5 and the body say on line 6 with the recorded stdout triples exact. The property the fix was for now holds.
Task 5: one residual sent back -- the plain-SELECT entry says "The stderr line that does not" and shows one pair, where the whole handler activation differs. False singular, and a cardinality claim against the house rule. Sharpened by contrast: the SELECT CASE traceback entry a few lines above genuinely is one line and says so, which I verified separately, so two adjacent entries would teach a reader the wrong thing about how these are written. Same shape r5 caught in the DO entry -- the record understating its own finding, twice in one file.
Task 5: 672619950 verified -- the plain-SELECT entry now shows the whole handler activation and matches my measurement line for line.
ROOT CAUSE of the DO divergence, traced not inferred, and it refutes what BOTH records said. `step`'s InstructionKind::Do arm calls run_loop, which runs the whole loop before returning, from INSIDE the clause unit step_in_temps_frame opened -- so leave_clause, the boundary that delivers, does not run until after END. The oracle's RexxInstructionDo::execute returns as soon as it has opened the block.
The discriminator: the empty-body version of the same program AGREES with the oracle, G ran 5 on both sides, SIGL naming the `do` line and not `end`'s nor the one after. So the boundary EXISTS and carries the RIGHT line; it is open across the body. "Elided boundary / no instruction to run one at" is false for this case -- it is misordered, not missing. A case that agrees is what pinned it, and nothing else in that file has that shape.
Consequence for Task 6: the two constructs are INVERTED, so probably two fixes. Plain SELECT = boundary in the right place at the wrong level (settle_block_indent lives in select_case, a caseless SELECT never reaches it). Plain DO = right level, wrong place. Fix shape for DO is to close the header clause before the body runs as Select's arm already does; that clause owns the body's temps frame, so it is larger than it sounds. Step 2 stays anyway -- a code-derived answer still owes a measurement.
Plan corrected in 111b776a2: Task 6's causal sentence, Step 2's inverted framing, and Step 1 now captures the empty-body variant as the control a fix must not break.
Task 5: causal item re-sent to t5 (it idled without acting on the first send).
Task 5: b27b950f1 lands the causal correction, and t5 went further than asked -- it found the file's HEADER BLOCK had summarised two differently-caused cases as one family, while one of the two bullets already named the real cause. Pre-existing, and the same summary-contradicts-its-own-detail shape as the four stale copies.
Task 5: t5's citation `RexxInstructionSimpleDo::execute` is right and MINE was wrong -- I wrote `RexxInstructionDo`, which does not exist. I read the C++ (SimpleDoInstruction.cpp:71-95): it traces, adds the block instruction, handles the debug pause and returns. It never runs the body. That is the oracle-side confirmation of the root cause.
Task 5: gates at b27b950f1 -- 0 non-comment changed lines across rounds 2-4, fmt 0, clippy 0, workspace suite exit 0 / 83 ok / 0 FAILED.
Task 5: final scoped re-review dispatched to r5-selectcase over 17d6dc6ec..b27b950f1, asking it to (1) check my rejection of its OWN Important, (2) try to refute the new causal account, which is load-bearing and has had no independent adversarial read, (3) judge the header-block correction including the UNTIL case neither of us re-measured, (4) find this round's new false statement.
METHOD NOTE worth carrying: a case that AGREES is what pinned the cause. Every diverging probe was consistent with both "no boundary" and "boundary too late"; only the empty-body agreement separated them. Two wrong causal attributions had survived every diverging probe in the file.
Task 5: final re-review came back. r5 CONCEDED its own Important after measuring both programs -- my rejection was right, the record was not wrong, and it says promoting its Minor was the right call because naming the program is what let it check this time.
Task 5: r5 tried to refute the causal account and could not; it verified every link on both sides (crate chain through leave_stepped_clause -> leave_clause -> deliver_pending_trap, and SimpleDoInstruction.cpp:71) and added a DISCRIMINATOR the account did not have. Nested plain DOs, outer line 5 and inner line 6: oracle `G ran 5`, both engines here `G ran 6`. I reproduced it exactly. It excludes an absent boundary (delivery would fall to the clause after the loop) and a correctly placed one (delivery would be line 5) with no argument needed. Second independent instance of the mechanism.
Task 5: the UNTIL bullet in the header block CANNOT BE CHECKED and this round re-endorsed its attribution. Three reconstructions, three results, none matching the bullet: the bullet's own numbers, r5's (two sides swapped), and mine (`do until raiser1() > 0`, oracle `after unset` / `G ran 7`, both engines `G ran 6` / `after set`). The bullet does not name its program. Disposition: withdraw the attribution as unestablished, keep the bullet, do not guess or correct its numbers.
Task 5: MY probe runs the OTHER WAY -- both engines deliver EARLIER than the oracle. Every other shape in this plan has us delivering later. Recorded in Task 6's Step 1b with its program named, flagged as not to be assumed fixable by the same change.
Task 5: plan-file findings were all mine and are fixed in 673e405b1 -- the heading still carried "is never run" four lines above the body that corrects it, `RexxInstructionDo::execute` does not exist (it is `RexxInstructionSimpleDo`, SimpleDoInstruction.cpp:71), and `Instruction::Do`/`Instruction::Select` are not paths since the variants live on `InstructionKind`. Two of the three were copies of corrections that had landed in the case file and not in the plan.
Task 5: fix round 5/5 dispatched to t5 -- withdraw the UNTIL attribution, sweep the report for the fifth instance of the stale-copy pattern (r5 named two, at :802 and :562-563), and add the engine-scope clause to the case file.
Task 5: 613983c0b lands round 5. Verified -- the UNTIL attribution is WITHDRAWN rather than replaced, the bullet and its numbers are left alone with the reason stated (choosing a program would be inventing a fourth reconstruction and recording it as the third), the zero-pass bullet was RE-MEASURED and still holds, my other-way reconstruction is recorded with a pointer to its named program in Task 6 Step 1b and an explicit "this is not the UNTIL bullet's program".
Task 5: the engine-scope claim is exact and I checked it -- compile.rs:427-430 pushes Op::LoopRun and then close_region, so LoopRun really is inside the header's own clause region.
Task 5: the report sweep found a THIRD stale copy the finding did not name -- round 4's own Additional item asserted the UNTIL attribution while being the section that corrected the DO one. All surviving occurrences are bannered corrections, none read as live claims.
Task 5: gates at 613983c0b -- 0 non-comment changed lines, fmt 0, clippy 0, workspace suite exit 0 / 83 ok / 0 FAILED.
Task 5: complete (commits 9e03e6e80..613983c0b, 5 fix rounds, review clean from r5's side; its one Important was adjudicated as rejected with measurement and it concurred).
Task 5: minor (deferred): loop-header-boundaries:1-2's subject line no longer describes what the file holds. FOR THE FINAL REVIEW TO TRIAGE.
Task 5: minor (deferred): the UNTIL bullet records an observation whose cause is now explicitly unestablished and whose program is unnamed. Closing it needs a measurement nobody has made. FOR THE FINAL REVIEW TO TRIAGE.

ALL FIVE TASKS COMPLETE. Task 6 added to the plan afterwards by Moritz's decision and is NOT part of this SDD run -- it is queued for a separate run before Phase 5.

## Whole-branch review (opus), and its disposition

Verdict: behavioural changes correct and independently re-measured against the live oracle; no Critical; merge blocked only on false prose, all correctable without touching code. It re-ran all 11 loop-header-boundaries stanzas and all 4 loop-header-values stanzas against the live oracle, all 10 trace_indent expectations on three descriptors, a 7788-row POS/LASTPOS grid, and mutation-tested the new tests with --no-fail-fast reading every catcher.
FINDING I1, which I verified myself and which is this session's own failure mode inside the correction that named it: 613983c0b's paragraph split left "Delivery then goes to whichever boundary arrives first: with a body that is the body's first clause" standing under the subject "The oracle's ...", where it is FALSE -- the oracle delivers at the DO's own boundary, as the file's own transcript twelve lines down shows.
DEFERRED MINOR STRUCK AS STALE: settle_block_indent's "two same-typed usize indents" was already fixed at 932e6b710; the signature is (open: bool, clause_indent: usize). Verified.
DEFERRED MINOR CARRIED: the DO OVER ... FOR corpus gap is confirmed but the construct is pinned three ways, one of them oracle-measured bytes the reviewer re-verified; what must be fixed is two comments that overstate the gap.
One fix dispatch sent to t5 for the five Important prose findings plus the cardinality minors. Scoped re-review after it closes the branch.
NEW PRE-EXISTING DEFECT, reproduced by controller byte for byte: empty do/end with a DOUBLE requeue. Oracle `G ran 5 / K ran 6 / after 5`; both engines `G ran 5 / after 5 / K ran 7`. The second delivery is owed at the END's own clause and this crate has no boundary there, so it falls past `say 'after'`. This is the elided-END mechanism WITH A NAMED PROGRAM -- the thing the UNTIL bullet could never supply. So DO carries TWO defects: a misplaced header boundary and an absent END boundary. Added to Task 6 as Step 1c, which now must say which of the two it closes.
Final review fixes: 7a0b31245. All five Importants addressed, and note the SHAPE -- four of the five were fixed by REMOVING the claim rather than restating it with a corrected number, on the view that the claim was the defect and its value incidental. I1's rewrite now names the subject of every clause explicitly and states that the oracle always delivers at the DO's own boundary, pointing at the transcript twelve lines below that shows it.
Final review fixes: gates at 7a0b31245 -- 0 non-comment changed lines in rust/ across both commits, fmt 0, clippy 0, workspace suite exit 0 / 83 ok / 0 FAILED.
Scoped re-review dispatched back to the whole-branch reviewer over 613983c0b..7a0b31245, asking for a merge verdict, whether the removal-rather-than-restatement shape cost a reader anything, whether the un-taken Minors are safe to carry, and -- since every round of this task introduced a false statement and I1 was introduced by the round that fixed the finding before it -- to assume this round did the same and look at the rewritten I1 paragraph first.
Re-review of the final-review fixes: I3, I4, I5 and three Minors ADDRESSED and stable. THREE new Importants, all in or beside the I1 rewrite -- exactly where I told it to look, and the prediction was made because every prior round of this task had done the same.
NEW-1: the new evidence pointer points the wrong way. "the transcript below shows `G ran 5` ahead of `body`" -- the only transcript with `body` is ABOVE at :247-248; the one below is the empty-body variant and has no `body` line. Claim true, pointer wrong.
NEW-2: "the oracle ALWAYS delivers at the DO's own boundary" is falsified by the double-requeue program I added to Step 1c the same day -- the oracle's SECOND delivery is at the END's boundary.
NEW-3: the withdrawal paragraph still says the elided-instruction attribution is unsupported because "nothing anyone can run supports it", while Step 1c names a program demonstrating that exact mechanism. 7a0b31245 edited that paragraph and left the sentence two lines later.
CONTROLLER FINDING, and it is mine: NEW-2 and NEW-3 are BOTH collisions between my commit 00fc6d4fc and t5's 7a0b31245. I added a plan step about the same subject matter while the implementer was rewriting the prose about it, and neither of us saw the other's change. Not a review failure -- a sequencing failure by me. Told t5 so it does not read them as its own carelessness.
CONFIRMED ON THE FIX SHAPE: every new defect this round landed in the finding fixed by REWRITING; none landed in the four fixed by DELETING. That is evidence for the rule, not just an argument for it.
Last fix dispatched. Merge verdict is NOT CLEAN until those three land; all prose, no behavioural change.
1f4176b47 lands the last three. Verified: NEW-1's pointer now says "above" and the transcript with `body` is at :247-248, above it; NEW-2 is bounded to "the condition that was pending when the DO ran" with a new paragraph giving the second condition its own account; NEW-3 separates the bullet's unreproducible observation from the mechanism, which Step 1c demonstrates on a different construct. Every factual claim in the new prose checked against measurements I made myself: oracle `G ran 5 / K ran 6 / after 5` with K ran 6 naming the END, ours `G ran 5 / after 5 / K ran 7` landing past the say.
Gates at 1f4176b47: one file, comment-only, fmt 0, clippy 0, workspace suite exit 0 / 83 ok / 0 FAILED, corpus gate 10 passed / 0 failed under REXX_CORPUS_GATE=1.
BRANCH CLEAN. The reviewer's stated bar was "with those three corrected the branch is clean by my reading"; the three are corrected and independently verified.
WORKSPACE DELIBERATELY NOT DELETED, against the skill's default. Task 6 is queued and this ledger is the only record of five fix rounds of adjudications, three rejected-or-promoted findings, and the controller errors. Deleting it to satisfy a step would destroy the input to the next run. Surfaced to Moritz rather than decided silently.

## Task 6 -- run by the controller inline, not dispatched

Task 6: complete (commits 1f4176b47..01f8010b7 -- e74780054 the fix and its witnesses, 01f8010b7 the plan record and one plan correction).
Task 6: run INLINE rather than by an implementer subagent. The session directive is not to call the Agent tool unless asked, and the controller held the diagnosis from the Task 5 round; recorded so the deviation from this plan's SDD header is visible rather than silent. No task reviewer has looked at it.
Task 6: BOTH mechanisms closed, and it took a third change neither of them named. run_loop's Simple arm opens the header clause (closed before run_bounded) and END's clause; leave_stepped_clause gives a DO/LOOP step no boundary of its own. The third is what the DO UNTIL needed -- its END boundary is already served by the UNTIL test, so the requeue is owed to the clause after the loop, and only removing the step's own boundary moves it.
Task 6: Step 2 answered WITH A CONTROL, not by reading code -- the plain SELECT program recorded in the case file was re-run after the fix landed and is unchanged, still two columns short on both engines. Two fixes, not one.
Task 6: PLAN CORRECTION. Step 1b recorded the oracle printing `after unset` for the DO UNTIL shape. It prints `after set`, and `unset` is not reachable on that program at all -- the handler that requeues is the one that assigns the variable. Instrumenting that handler showed the oracle's first delivery ALREADY AGREED with this crate at the END's line and only the second moved; the wrong word had made both look wrong. Corrected in 01f8010b7.
Task 6: THE STEP 1 CONTROL IS WEAKER THAN THE PLAN CLAIMED. The empty-body variant agrees because there is no body clause to move the clause line first, not because the header boundary was correctly placed. It pins "a boundary exists and carries a line", nothing about where the header clause ends.
Task 6: repeating loops with a real header (`do zi = 1 to 2`, `do while`, `do 2`) were never wrong on this route -- run_repeating already opens a per-pass clause at the DO's line and drains the queue there. Only Simple, which has no such clause, and DO UNTIL, which refills the queue after it.
Task 6: MEASUREMENT ERROR I MADE AND CAUGHT. The first "does anything else catch it?" run removed only the corpus program and left the new ir_dual stanzas in, so all three mutations were caught by `both_engines_agree_on_every_case_file` and the run said the opposite of what it was asked. Re-run with BOTH witnesses removed: the whole workspace suite exits 0 on all three. So the pre-existing suite had NO coverage of any of this, and each new witness independently catches all three. e74780054's message says "the suite without this program catches none of them" -- true of the pre-existing suite, and it reads more broadly than what that phrasing supports; the precise statement is this line.
Task 6: witnesses -- three mutations, one per change, each keeping the indent and the echo so only the boundary moved. Each reddens corpus_differential and ir_dual. Raw before/after sweep over every program in corpus/ and bench-programs/, both engines, three descriptors: nothing moved but the new program.
Task 6: FOUND AND NOT FIXED, all confirmed pre-existing by a before/after control build. (1) the plain SELECT boundary already recorded in the case file, unchanged. (2) a handler delivered at an IF's or SELECT's own step boundary traces too DEEP -- four columns for `if 1 = 1 then zr = raiser()`, six for the select/when shape, SIGL and stdout agreeing. Same structural cause as this task's, opposite sign to (1), on constructs this task did not touch. (3) a stem `do zi over zz.` is refused at rc 120 where the oracle runs it at rc 0 -- the known loop_header_plan refusal, written down because a probe reached it.
Task 6: gates at 01f8010b7 -- fmt 0, clippy 0 warm AND from a clean target directory, workspace suite exit 0 / 1509 passed / 0 failed with REXX_CORPUS_GATE=1, corpus subset 54 of 54 matching. Debug-profile rexx-exec suite also run, so enter_clause's fourth-site tripwire was live: no fire.
Task 6: NO WHOLE-BRANCH REVIEW YET over e74780054..01f8010b7. That is the outstanding SDD step for this task.
